# GitHub Actions failure ledger

- `self_host_public_auth_internal_redirect_origin` — A read-only authenticated-origin GET of the live Preview `/admin/session` at `2026-09-08T11:04:57Z` returned HTTP 307 with a Location on `https://0.0.0.0:3000`, rather than the approved public domain. The active app is dev `4bba2b78`; its normal GitHub gate used a synthetic standalone fixture and did not prove this actual reverse-proxy redirect path. Classification is a repository-controlled standalone request-origin assumption, not failed data restoration or TLS. Preserve the response evidence and keep authenticated cutover acceptance and continuous receiver activation incomplete. The schema-approval guard is an independent deployment safety change; the separate public-redirect correction must keep required-gate ordering, sanitized returnTo and same-origin protections, use the operator-pinned real-mode origin rather than untrusted forwarded headers, and pass local/remote first attempts plus actual public authentication/redirect checks. No Production changes or credential resets were made.

Final local validation for `local_receiver_release_unit_import_deadlines`: three independent complete unit executions passed 133/133 with unchanged coverage and zero retries. The corrected complete Release passed 1,861 Node tests/eight existing configured skips, 133 unit tests, production build and 103 E2E cases/retry 0. Structural inspection of all 2,416 lines/215,085 bytes found only the existing two between-admin Fast Refresh notices; no preceding runtime error or other error/retry signature occurred. This closes local verification only; remote first-attempt checks and receiver deployment remain separate gates.

Follow-up for `local_receiver_release_unit_import_deadlines`: the two-worker run still failed two initial dynamic-import deadlines and one consequent mock-call assertion (130/133 passed). Its aggregate transform/import durations dropped to 18.83/13.34 seconds, so a worker cap alone is not a complete fix. A contemporaneous read-only host check observed a one-minute load above 178 on 15 logical CPUs; this is resource-pressure evidence, not proof of a particular scheduler cause. The member-lifecycle suite has no per-case import-time configuration: use a hoisted mock and static module import, resetting only its mock between cases. The notification-preferences suite does require import-time environment selection: await that module in its normal beforeEach initialization before any case installs RPC behavior. Preserve both full failed logs, all product assertions, 5-second test deadlines, normal hook defaults and zero retries. Static source contracts prevent the asynchronous test-body imports from returning. Independently stop only completed task-owned synthetic Compose fixtures after exact project/directory checks, preserving every volume and unrelated service. Fresh complete unit repetitions and a complete final Release remain required.

- `local_receiver_release_unit_import_deadlines` — The first complete local Release for the receiver idempotency change stopped in the unit stage on 2026-09-08 after 1,858 Node passes/eight configured skips. Its 133 unit cases had 126 passes and seven failures: six 5-second deadlines across three dynamic-import suites, followed by one member-lifecycle call-order mismatch containing four identical sequences instead of one. The failed log records aggregate transform/import durations of 116.84/109.76 seconds in a 30.87-second unit invocation. These suites reset their module mocks between cases; timed-out import work can continue and consume a later case's mock. The host exposed 15 logical CPUs and the unit project had no worker cap; exact host scheduling causality is not claimed from the log alone. Preserve the failure and stop remote triggers. Bound only unit workers to two, matching the existing isolated CI CPU budget, while retaining the 5-second test limit, zero retries, all cases, coverage and product assertions. A new config contract failed before the cap. Require independent complete unit repetitions and a fresh full Release before publication; passing an unchanged retry is not the correction.

- `self_host_receiver_unchanged_identity_mismatch` — Read-only follow-up after the successful b8f918b232aaff03a5d78ca339cab4faa5bb0445 deployment found that the unchanged-release shortcut still compared a persisted registry-pull ID with the manifest's build-local ID. Those identities are intentionally different under the already-reviewed registry contract, so periodic polling could unnecessarily replace the same approved app. No repeated live deployment was invoked as a reproduction. A focused regression using distinct build/pull IDs failed on the original comparison. The correction matches the approved SHA, run, attempt, workflow, platform, source/manifest hashes and each published component digest/reference, then matches the app against its persisted pulled ID. Missing, duplicate, altered and invalid saved identities remain ineligible for the shortcut. Return the actual persisted app ID for an unchanged result. Also correct the Caddy validation invocation to specify its executable explicitly; the official image has no entrypoint. Require focused tests, final local gates, a new exact-SHA first attempt and two real receiver invocations proving unchanged app/container state before enabling periodic operation. This is a source-identified deployment/idempotency defect, not a new failed GitHub run or a data-migration failure.

- `self_host_monitoring_internal_network_port_binding_failure` — After the corrected observability bind paths were installed in control source `982d44651950d8ff6d3a1a12535ff8bd06af35c3` at `2026-09-08T15:13:01+0900`, telemetry, Prometheus, Alertmanager and Grafana reached ready state only on their container IPs; loopback probes on ports 59464, 59190, 59193 and 53100 had no `docker-proxy` listener because the services were attached only to the internal `monitoring` network. The Compose `HostConfig.PortBindings` were present but host publishing was not active without the existing edge bridge. The same operator invocation also overrode `MONITORING_SECRETS_DIR` with the parent directory instead of the env-file's `monitoring/secrets` directory, so postgres-exporter received a directory at `/run/postgres-monitor-password` and entered a restart loop; no database data or core Supabase container changed. Classification: repository-controlled Compose network contract plus operator environment sequencing. Correction: attach host-probed monitoring services to the existing `original_edge` bridge, add executable service-network regression assertions, and retry only from a new exact-SHA Actions first attempt while passing the env-file's secret/textfile paths unchanged. The current partial monitoring containers remain for diagnosis; timer stays disabled and no public ingress was changed.`

- `self_host_monitoring_compose_observability_path_failure` — The first monitoring-stack start after the successful original Preview app deployment at `2026-09-08T14:26:31+09:00` used control source SHA `6b7dc572ee18705ee0ee843efc41a449c6b21f5b` and stopped before the Prometheus/Alertmanager processes could start. Compose resolved the overlay's `./deploy/observability/...` bind sources relative to `deploy/self-host`, requesting the nonexistent `/opt/ssartnership/control-6b7dc572/deploy/self-host/deploy/observability/alertmanager.yml` path and reporting a file-versus-directory mount error. Telemetry, node-exporter, postgres-exporter and Grafana were created/started; Prometheus and Alertmanager remained created-only. The app, original Preview DB/REST/Storage/Kong containers and data volumes remained healthy and unchanged; no backup, DNS, Production or public-ingress mutation occurred. Classification is a repository-controlled Compose path defect, not an image, Docker registry, database, or application failure. Preserve the partial containers and logs; correct the tracked paths to `../observability/...`, add a source/runtime path regression, run the complete local gates and a fresh exact-SHA Actions first attempt, then perform one bounded monitoring-stack replacement. Do not retry the unchanged overlay.

- `self_host_receiver_a2c67839_deploy_health_failure` — The first receiver attempt using approved merge SHA `a2c678393dbd4bf2c4b0b0b98a52c1f3f8758c25`, Self-host Images run `34185630436`, and control `a2c67839` started at `2026-09-08T13:20:29+09:00` and ended at `13:22:22+09:00` with the fixed service result `RECEIVER_FAILED`. API/ref/run/job/artifact/manifest approval and the corrected registry-pull image contract passed; the receiver wrote the immutable release manifest and pulled the three new images. Compose then created `ssartnership-original-preview-app-1` from the new app image but it exited with status 143 during deployment health/rollback handling; receiver state was not written. A root-only uncaught replay at `2026-09-08T13:29:39+09:00` exposed `DEPLOY_FAILED_STOPPED` with cause `DEPLOY_HEALTH_FAILED`. The preserved app had OOM=false, four normal Next.js Ready log lines, no Error/ECONN/EADDR/SIGTERM/Supabase/health/auth log patterns, and three healthcheck records whose last exit code was 0. A bounded host probe at `13:31:46+09:00` returned `/api/health` 200 (15-byte JSON) and `/auth/login` 200 (72,063-byte HTML) only after the existing `ssartnership-original-preview-34141078185_edge` network was connected; the app's original_default and monitoring networks are internal-only, so the requested loopback host port was not published without the edge network. Original Preview DB/REST/Storage/Kong volumes and containers, synthetic home Preview, public ingress, DNS, backups and Production were unchanged. Classification is a repository-controlled Compose network configuration defect after image validation, not GitHub, registry, artifact, schema or data parity evidence. Preserve the failed journal and diagnostic app container; the typed Compose edge-network correction must pass a fresh exact-SHA gate before one replacement receiver attempt, and the timer remains disabled until a clean deployment receipt exists.

- `local_self_host_image_validation_runtime_floor` — The first `npm run verify:change` for the registry-pull image-ID correction on `fix/self-host-image-validation-20260908`, exact base `origin/dev` merge SHA `fcda110656b6dea672cf5f9b43d9734859e1ecad`, stopped at `check:lockfile` on `2026-09-08` because the shell selected Node `26.5.0` instead of the repository-pinned Node `24.18.1` (npm `11.17.0` instead of the reviewed `11.16.0`). Documentation and cross-platform checks passed; no build, E2E, commit, push, Actions run, server retry or Preview/Production mutation occurred. Classification is local operator runtime selection, not the image-validation correction or product behavior. Prevention is to invoke every complete gate through `fnm exec --using 24.18.1` and verify the resolved Node/npm versions before the replacement gate; preserve the failed output and require a clean `verify:change` and `verify:release` before remote mutation.

- `self_host_receiver_post_approval_failure` — The first receiver invocation using control source SHA `fcda110656b6dea672cf5f9b43d9734859e1ecad` started `ssartnership-preview-receiver.service` at `2026-09-08T12:33:26+09:00` and ended at `12:33:39+09:00` with the wrapper's fixed `RECEIVER_FAILED` result. Read-only server inspection confirmed that the current `dev` ref, first-attempt Self-host Images run `34183068312`, required jobs, release artifact `10039804882`, artifact size/digest, bounded ZIP contents, and published manifest all independently pass the receiver's approval contracts; the local replay of the exact artifact passed `parseReleaseArtifact`, `verifyRemoteJobs`, and `verifyRemoteRelease`. A root-only direct invocation at approximately `12:39 KST` exposed the post-approval code `RECEIVER_IMAGE_NOT_APPROVED`. The corrected inspect-only probe showed the first app image's pulled OCI/config ID was `sha256:2eb30d...` while the published manifest's build-time `id` was `sha256:3f593d...`; digest reference, AMD64 platform, revision label and single RepoDigest were valid. An initial probe incorrectly parsed Docker's `--format '{{json .}}'` object as an array and was discarded as an operator diagnostic error; the corrected probe isolated the actual registry-boundary mismatch. The receiver wrote only the approved release manifest, pulled/cache-populated images, and did not write receiver state or switch the application; database/Storage/public-ingress and Production were untouched. Classification is a repository-controlled image-validation contract defect: a local build image ID is not stable across OCI registry publication/pull, while the immutable digest, platform, revision and RepoDigest remain valid evidence. Prevention removes only the cross-registry local-ID equality check, requires a valid pulled OCI ID, and adds a regression with distinct build/pull IDs; a fresh local Quick/Release gate and exact-SHA Actions run are required before another receiver deployment attempt. Preserve the failed journal and do not enable the timer or claim Preview cutover until the corrected receiver produces a clean deployment receipt.

- `local_self_host_receiver_symlink_main_test_type` — The first `npm run verify:change` for the symlink main-module correction on `fix/self-host-receiver-symlink-entry-20260908`, exact base HEAD `d98157583cebce28e3881fa46c54deef62ca2177`, stopped at `2026-09-08T11:56:42+09:00` during `typecheck:ci`. The new regression passed a URL object to a helper typed for the runtime URL string, producing two `TS2345` diagnostics; focused receiver/Compose tests had already passed 8/8. No build, E2E, commit, push, Actions run, server retry, Preview or Production mutation occurred. Classification is a repository test-boundary typing defect; the receiver realpath implementation and server state were unchanged. The correction passes the URL's `.href` while retaining the symlink and missing-target assertions; a fresh complete Quick and Release gate are required before remote retry.
- `self_host_receiver_symlink_main_guard_noop` — The first retry after installing control source SHA `d98157583cebce28e3881fa46c54deef62ca2177` started `ssartnership-preview-receiver.service` at `2026-09-08T11:53:54+09:00` and systemd reported a normal oneshot completion, but the receiver main guard compared the symlinked `process.argv[1]` (`/opt/ssartnership/control/current/...`) with Node's canonical `import.meta.url` (`/opt/ssartnership/control-d9815758/...`) and never invoked `receiveRelease()`. No GitHub artifact, release state/manifest, image pull, application switch, database/Storage/public-ingress or Production change occurred; the new control archive remained installed and the old control was preserved. Classification is a repository-controlled host entrypoint defect, not a GitHub, registry, artifact, database, application-health or systemd failure. Prevention resolves both entry paths through `realpathSync` plus `fileURLToPath` and adds an executable symlink main-module regression. Require a fresh complete local Release and exact-SHA Actions checks before the next receiver retry.
- `local_self_host_receiver_zip_fixture_type` — The first full `npm run verify:change` for the Node-built-in ZIP parser correction on `fix/self-host-receiver-zip-runtime-20260908`, exact base HEAD `0f7f6ab624aa9b0f36054fdc4e42698633b2af46`, stopped at `2026-09-08T11:12:32+09:00` during `typecheck:ci`. The new test helper accepted a generic string for jszip's compression option, producing one `TS2322`; focused receiver/Compose tests had already passed. No build, E2E, commit, push, Actions run, server retry, Preview or Production mutation occurred. Classification is a repository test-boundary typing defect, not ZIP parsing, dependency installation, application, provider, schema or deployment evidence. The correction narrows the helper to jszip's `Compression` type while retaining STORE/DEFLATE coverage; a fresh complete Quick and Release gate are required before remote retry.
- `self_host_receiver_missing_runtime_dependency` — The first approved home-server receiver start for control source SHA `0f7f6ab624aa9b0f36054fdc4e42698633b2af46` failed at `2026-09-08T11:06:29+09:00` in `ssartnership-preview-receiver.service` before any GitHub artifact, image pull, application switch or state write. Node `24.18.1` reported `ERR_MODULE_NOT_FOUND` for the `jszip` package imported by `scripts/self-host-ci/receive-release.mjs`; the source archive intentionally contains tracked controller files only and the server has no controller `node_modules`. Candidate original Preview DB, REST, Storage, Kong, app, public ingress, DNS, backups and Production were unchanged; no release manifest or receiver state file was created. Classification is a repository-controlled host-runtime packaging defect, not a GitHub, registry, artifact, database, application-health or provider failure. Prevention is to keep the receiver self-contained at runtime by parsing the bounded single-file release ZIP with Node built-ins, add STORE/DEFLATE and path/CRC regression coverage, and require a fresh local Release plus exact-SHA first-attempt Actions checks before retrying the service. Preserve the failed journal and do not install an unpinned dependency tree on the server.

- `local_release_unstaged_work_unit` — The first canonical `npm run release -- --yes --lighthouse=skip --version=none --message-file=.tmp/release-message-overlay.txt` for `fix/original-preview-overlay-hardening-20260908`, exact base HEAD `adbc2c7c11b60f21fc9621b04b7f446c53517074`, stopped at `2026-09-08T10:28:46+09:00` after its internal `verify:change` completed successfully. The release script intentionally does not stage changed files for `--version=none`; the coordinator had not staged the two completed overlay files, so `git commit -m` failed with no staged changes. No commit, push, GitHub Actions run, PR, Vercel deployment, Supabase mutation, home-server mutation or Production mutation occurred; remote `dev` remained `adbc2c7c11b60f21fc9621b04b7f446c53517074` and `main` remained `228d238cfd79912acaf43a588dee51888444fc77`. Classification is local operator sequencing, not a source, test, dependency, provider, schema, security or deployment failure. Prevention is to review and stage exactly the completed work unit before invoking the canonical no-version release; the staged diff and a fresh complete Quick gate are required before one replacement commit/push.

This is the living, sanitized source for Actions failure signatures and rollout state. Read it completely before an Actions-changing work sequence and again before Production promotion or a privileged shared-state mutation. A blocking abnormal run extends this file before another attempt. Exact reviewed warning baselines and recovered external-provider transients with proven final parity may be batched before Production promotion and do not require a source change or a new SHA.

## Frozen audit baseline

Correction validation for `github_gate_private_source_mode`: the corrected AMD64 telemetry image built from mode-0600 inputs and ran as UID 1001 with read-only root, no network and no capabilities; actual HTTP health 204, unauthorized write 401, authenticated synthetic vital 204 and metrics 200 passed. Gate-module read/parse checks passed as both UID 1001 and 1002. Eleven focused contracts passed. Host Release exited 0 with 1,845 Node passes/eight configured skips, 133 unit passes, build and 103 E2E passes without retries; all 2,400 lines were inspected, retaining only four reviewed synthetic rollback diagnostics, two unchanged between-admin Fast Refresh notices and the existing development-only advisory policy. The subsequent final-source high/Quick exited 0 with the same Node/unit totals and all 94 documentation checks; its complete 2,045-line output had only the existing synthetic/advisory baseline. This closes local verification, not GitHub publication or home-server public cutover; a new commit and remote first attempt remain required. Prior provisional mode choices and pending-local notes below are chronological evidence superseded only by these results.

Telemetry Docker correction detail for `github_gate_private_source_mode`: the first mode-0600 rehearsal with COPY 0444 still failed before HTTP startup. Read-only metadata inspection proved that COPY assigned 0444 to newly created nested destination directories as well, preventing traversal. Require 0555 on telemetry copies (public code plus new parent directories), retaining non-writability and the same nonroot user; the updated source contract failed first. The gate image already creates its destination through its 0555 entrypoint copy and its eight-module reproduction passed. Preserve the failed telemetry rehearsal and require a fresh image/runtime test; source-only assertions did not prove container execution.

Local validation observation for `github_gate_private_source_mode`: the first host Release completed Quick and build but E2E refused occupied port 3100 before starting any browser case. This repeats the existing `local_dual_environment_release_preflight`/`local_visual_occupied_port` operator preflight boundary, not a product regression. Preserve that failed log and the unrelated listener; verify a free loopback port before a replacement full Release with the final two-image correction. No remote run was triggered by this local failure.

Related image review for `github_gate_private_source_mode` found the same inherited read-mode boundary in both public telemetry source copies. Its new source contract also failed before correction; normalize those copies to 0444 while keeping the nonroot runtime. The app image explicitly owns its source as nextjs and the database image owns its configuration as postgres, so their copy permissions do not need broad changes. Local gate-image reproduction from mode-0600 inputs passed all eight module read/parse checks as UID 1001 and 1002 with root-owned immutable files, read-only root, no network and no capabilities. Telemetry HTTP runtime verification and final all-change gates remain pending.

- `github_gate_private_source_mode` — Image run 34146765198, attempt 1, dev SHA fbf8c6376c6fa44eb1ea13c7a558c32bfa09efff failed in job 101820380715, step 4. Structural audit was complete with one failure annotation and one GitHub error; publication job 101821873538 was correctly skipped. Both application and isolated test-only builds compiled successfully after 1,843 Node passes/eight configured skips. Before browser tests could start, Node was denied read access to the image-owned desktop E2E entrypoint. The release process extracts public source under umask 077; three Docker COPY instructions inherited owner-only modes into root-owned image files, while the gate runs as the nonroot host UID. Pin those public module copies to 0444, preserving private source/artifact permissions, nonroot execution, read-only root, dropped capabilities and every release test. The new source contract failed first on the missing modes. Require actual Docker reproduction from mode-0600 build inputs and nonroot read/parse checks, then complete local Release and a new-SHA remote first attempt. Preserve the failed run; do not rerun it, publish its output, or claim server cutover. Correction verification remains pending.

- `local_storage_runtime_stale_pin_assertion` — The first local Release preflight after selecting the original Preview-compatible Storage image failed one Node contract: the older database Compose test still expected v1.60.4 while the new runtime test correctly required the reviewed v1.73.1 digest. Totals were 1,842 passed, one failed and eight configured skips; the Release wrapper preserved exit 1 and did not proceed to build/E2E. Update the existing assertion to the complete new digest, retain both runtime and isolation assertions, and include all self-host database tests in the focused gate. Real local Docker Storage/age rehearsal and the server's all-file HTTP parity had already passed; neither substitutes for a corrected full Release. Preserve the original local log. No Actions run or deployment was triggered by this failure; replacement full-gate verification remains pending.

Correction validation for `local_storage_runtime_stale_pin_assertion`: all 18 focused database/runtime/Storage cases passed. The replacement full Release exited 0 with 1,843 Node passes/eight configured skips, 133 unit passes, build and all 103 E2E cases without retry. Complete 2,398-line output was inspected: four unchanged synthetic rollback-path diagnostics, two unchanged between-admin Fast Refresh warnings and the explicit development-only advisory policy were the only reviewed diagnostics. No test/runtime assertion was weakened. Remote publication remains a separate gate.

Remote correction outcome for `original_preview_storage_serial_deadline`: run 34141078185, attempt 1, exact SHA e92ebcdb1315417d5354c8786728cf60cf145a82 completed all required steps. All 414 log lines and annotations were inspected with no error, warning or retry signature. Both Storage byte passes reached 823 objects; one encrypted artifact was independently verified and received by the server. This proves the corrected export, not public cutover.

Correction validation for `original_preview_storage_serial_deadline`: all 17 focused migration cases passed, the ten Storage cases passed in three independent local executions, and high/Quick passed 1,842 Node cases/eight configured skips plus 133 unit cases. The complete 2,042-line local gate output contained only the four already-reviewed synthetic rollback diagnostics and the existing development-only advisory policy. This completes local correction validation, not remote export or server restore proof.

- `original_preview_storage_serial_deadline` — Run 34138510016, job 101794946608, attempt 1, push SHA 08c762d1ab54c018b817ac113145580d245d4f5f failed at step 5; upload step 6 was correctly skipped and the artifact inventory was empty. Complete 363-line job log and structural audit had one migration failure/one GitHub error, no warnings, and complete annotation collection. DB capture finished before Storage began at 15:31:17.684Z; the Storage download failure followed at 15:51:17.686Z, matching its 20-minute deadline. Serial two-pass transfer was insufficient within the configured budget; the old diagnostic did not reveal completed-object count or underlying request details, so no provider outage or exact failing object is claimed. Keep the failed run, full dataset, two byte-hash passes, three inventories, request timeouts, and the 20-minute ceiling. The correction opts the Cloud CLI into four bounded workers, aborts and settles in-flight work on first failure without retries, retains deterministic ledger order, and emits fixed phase/count progress only. Explicit deadline diagnostics and post-inventory expiry checks prevent misleading completion. Four regression cases first failed before their fixes (bounded concurrency, fail-fast cancellation, deadline classification and final-inventory expiry); local/remote correction verification remains pending until recorded below. No original Cloud data or server database was restored or replaced by this failed run.

- `local_export_database_ca_preflight` — Credential-free TLS negotiation with the reviewed Supabase session-pooler region failed against the ordinary system trust bundle. Official dashboard source identifies the public Supabase Root 2021 CA download. Pin that exact certificate in the repository, validate its SHA256/validity, and mount only this public file read-only in the client container; retain verify-full and hostname verification, without changing host trust stores. A transcription added one trailing blank line and correctly failed the exact-file hash test; remove only that formatting difference, not the integrity assertion. The corrected file matched the official hash and a credential-free TLS 1.3 negotiation verified both chain and hostname. Session-pooler input port 6543 is deliberately mapped to that same pinned host/project's 5432 session port; raw direct-host alternate ports remain forbidden. Actual credentialed Cloud export is still a separate remote gate.

- `local_export_context_environment_inference` — The new one-time export context repeated the known augmented ProcessEnv inference signature: the default process.env argument imposed the application's required NODE_ENV on a secret-free test dictionary. Annotate an optional-string dictionary at the context boundary, keep every identity/first-attempt assertion, and rerun semantic checking. The existing local_github_release_environment_inference prevention also applies to export entrypoints; do not weaken global ProcessEnv or inject unrelated application state into these fixtures. No remote run was triggered by the failed local check.

- `local_migration_capture_preflight` — The read-only DB/Storage export modules initially failed semantic checks at an injected fetch signature and an inferred environment map; explicit narrow types restored parity without weakening typechecking. The actual Supabase Storage Docker rehearsal then completed all downloads and source comparisons but failed a hard-coded expected byte total (43 versus 45 synthetic bytes). Derive that expectation from the fixture source Buffers, preserve the failed private export/volumes, and execute the complete fixture in a new environment. Corrected real PG17 snapshot/mutation/restore and Supabase DB/Storage/age round-trip rehearsals passed. The canonical high/Quick gate passed 1,834 Node cases/eight configured skips and 133 unit cases; logs contained only the four previously reviewed synthetic rollback-path diagnostics. No Cloud mutation or remote Actions trigger occurred in this local work unit.

- `local_migration_transfer_preflight` — The new age transfer orchestration passed its behavioral tests but semantic checking inferred an optional success return through a JavaScript throwing helper. Annotate that helper as never-returning; do not weaken semantic checking or assert an unchecked result. Focused tests/type/lint and the canonical change gate then passed (1,824 Node passes/eight configured skips, 133 unit cases). Initial Docker integration failed after download/extraction when age was executed from Docker's default noexec tmpfs. Separate read-only inspection verified noexec and the failing stage. The corrected synthetic fixture uses a dedicated executable tool mount and retains noexec on data, read-only root, dropped capabilities and the same resource bounds. Exact Linux AMD64 age 1.3.2 archive hash, 2MiB round trip, wrong-key refusal, late ciphertext corruption with recomputed transport hash, and absence of partial plaintext all passed. Preserve the failed diagnostics; no Cloud Preview export, remote Actions run or live data restore occurred.

- `local_diagnostic_module_discovery` — Supplemental self-host test invocation guessed a nonexistent alias-registration path, and a separate YAML check guessed an unavailable optional package. Neither command executed product tests successfully and neither changed dependency state. Inspect the actual package scripts and lockfile first: the tracked test alias registrar and installed js-yaml parser were then used, passing all 118 focused cases and workflow parsing. No installation, retry policy change or remote trigger was used. The independently running canonical Release completed successfully with 1,819 Node passes/eight existing skips, 133 unit cases and 103 retry-free E2E cases. Operator-command failures do not invalidate unrelated evidence, but must not be reported as product-test passes.

- `local_dual_environment_release_preflight` — Issue #435 environment-copy implementation first failed semantic checking because inferred JavaScript environment/object types did not match the TypeScript fixture contract; the next Release stopped on a port already owned by the user's application. Preserve both logs. Correct the narrow types without weakening the compiler, preserve the existing service, and use a verified free E2E port. The subsequent complete local Release passed 1,814 Node tests/eight existing skips, 133 unit tests, build and 103 E2E tests without retry; complete logs contained only four intentional synthetic rollback-test diagnostics and two reviewed between-admin Fast Refresh notices. Sanitizer CHECK and integer-array errors were separately fixed with focused coverage; Storage copy preserves extended attributes and verifies every object's API hash after an ENODATA failure. Failed candidates and source data remain preserved. No remote run was triggered by these local failures.

- `local_github_release_environment_inference` — The new image-publication contract's focused tests passed, but semantic checking failed because a default process.env parameter inferred the application's augmented required NODE_ENV and a fixture indexed a typed gate with an arbitrary string. Use an explicit optional string environment dictionary and a literal-key tuple; retain semantic checking and add unsafe run-ID rejection. Five focused cases, semantic typecheck and focused lint subsequently passed. Full Release, native GitHub execution, GHCR publication and server activation remain separate gates; no remote run has been triggered by this correction.

- `local_search_visual_prefetch_classification` — After the corrected production fixture passed all 103 E2E cases in 40.5 seconds without retries, its supplemental visual script stopped at a fetch ERR_ABORTED. The first script recorded neither response nor prefetch metadata, so its screenshots are not accepted as complete QA. Preserve that run and use the existing precise speculative-GET/RSC/prefetch/HTTP-200/content-type classification in a new diagnostic output, still rejecting every other failure and verifying complete document responses for canceled targets. This changes only the supplemental evidence collector, not application source, any canonical E2E assertion, test deadline or release acceptance. Color-environment warnings in the complete fixture-suite log are terminal-format diagnostics; there were no server/browser/test failures.

- `local_search_history_fixture_preflight` — The first bounded search diagnostic requested a fresh build in a directory with a retained production-fixture marker and was correctly refused before build or E2E. No active server used that directory. The retained artifact was then tested without rebuilding: the new no-search-RSC regression failed on the original router.push implementation. Preserve that red report and move only the stopped, task-owned fixture output to a new ignored archive before the corrected build. Do not remove the freshness guard or overwrite a running output directory.

- `mac_production_directory_search_navigation` — Exact source 30184759a58ca4a65c41e340789afa2dba15c941 passed trusted install, Quick (1,807 Node passes/eight configured skips, 133 unit cases), standalone and fixture builds, then failed search clearing after an explicit submit. The full 220,552-byte/2,777-line log and trace show 25 passes, one failure, 77 unrun, retry zero, exit 1 and OOM false. The report-level error is the intentional max-failures stop. Search and clear both initiated RSC requests with HTTP 200, yet the URL retained the old query and duplicated benefits fragment after clear. This establishes a stalled client navigation, not a server timeout or a proven complete framework root cause. No image/result was published. Follow-up scope uses native history for same-directory search state, preserving intentional push/back/forward semantics and existing filter replace behavior. Add deterministic no-search-RSC and exact URL/history coverage before implementation; preserve the failed source and require a fresh full gate. Intentional unavailable-Storage unit diagnostics, Docker metadata fetches and ordinary server-ready lines are separately classified, not suppressed runtime errors. Server source/controller staging only occurred; running Preview, credentials and timers remain unchanged.

- `mac_production_fixture_build_memcg_oom` — Exact source 4e61ce3fc077ee579ccdad6506d795bb3f4f8287 passed AMD64 trusted install, Quick (1,797 Node passes, eight existing skips, 133 unit passes), and the real standalone build. The additional fixture build compiled successfully but stopped while collecting page data with 14 workers; no E2E, success receipt or deployment archives were produced. Docker Desktop kernel evidence at 2026-09-07T07:41:00Z tied CONSTRAINT_MEMCG and the killed build process (about 3.21 GiB anonymous RSS, with 14 additional workers) to the exact 5 GiB gate cgroup. The runner had removed the terminal container without persisting its inspected exit/OOM state, and the bounded Docker event ring had already rolled past it; retain the kernel-based diagnosis and do not invent the old exit code. Limit only the extra fixture compiler's static-worker concurrency to two, leaving real/native builds and container/heap/test limits unchanged. Persist only validated running/exitCode/oomKilled fields before ordinary terminal cleanup. New subprocess config and evidence projection regressions failed first; the first focused type check then caught widened NODE_ENV inference in the subprocess test, corrected with the actual production literal rather than weakening ProcessEnv. Require a fresh source-SHA complete gate; the failed source's output remains unapproved. Intentional unavailable-Storage 503 unit diagnostics are separately verified against their mocked rejection and expected response assertions, not treated as a runtime provider outage.

- `local_visual_occupied_port` — The operator launched additional screenshot QA before the full E2E process had terminated. Its new server failed with EADDRINUSE; the probe temporarily reached the existing E2E server and later failed with connection refusal after normal E2E shutdown. Preserve the failed visual output, never accept its screenshots, and require the earlier process's terminal result plus an exclusive loopback bind preflight before launching screenshot QA. The canonical suite still returned 103 single-attempt passes, but the collided visual run is not acceptance. No production/source provider data was changed; the later fresh archive gate remains mandatory.

- `local_carousel_fixture_missing_after_asset_cleanup` — The post-cleanup production suite passed 14 tests, then the unchanged carousel layout test timed out because no visible synthetic slide remained (one failed, 88 unrun). The screenshot confirmed absent carousel rather than an asset loading delay. Keep the removed production defaults removed; add a shipped-asset synthetic slide only behind mock plus explicit E2E plus local fixture policy. Do not reactivate expired campaigns, alter provider data, weaken viewport assertions, or rerun the failed source as acceptance. The new fixture boundary regression failed first. Preserve the failed build/report and require a fresh fixture build, full suite and ordinary Release.

- `local_production_visual_speculative_cancellations` — Additional visual QA observed browser-canceled speculative GET fetches with Next-Router-Prefetch=1 and RSC=1. A bounded diagnostic retained HTTP 200 and text/x-component for all six first-viewport cancellations; no browser console/page error or HTTP error remained after the deleted-asset correction. A bounded idle drain did not eliminate them, and instrumentation did not prove a JavaScript abort caller, so the exact cancellation cause/performance impact is not claimed resolved. The visual audit records this exact successful-response speculative-cancellation class separately, requires complete 200 document bodies for the affected targets, and still rejects all navigation/asset/API/HTTP errors and other transport failures. This is not a zero-network-event claim, a blanket ERR_ABORTED allowance, or a change to any of the 103 canonical tests/gate error guards. Retain counts per viewport/theme and keep prefetch efficiency as an observation for the real runtime.

- `local_production_e2e_output_lifecycle_overlap` — Operator sequencing invalidated the diagnostic host Release: the fixture-output archive move occurred while that Release's development server was still using the shared .next-e2e parent. The development log then reported missing route manifests. This is an operator-induced cache lifecycle failure, not evidence against the new production runtime. Interrupt only the identified diagnostic Playwright process, preserve its log and moved output, and run the final host Release sequentially after fixture build/visual jobs finish. Different HTTP ports do not isolate filesystem output. Never move or clean a build directory while any owned server uses it; no acceptance or deployment derives from the interrupted run.

- `local_static_promotion_deleted_assets` — Additional production-runtime visual QA found two browser resource errors despite all 103 existing assertions passing: static promotion entries still referenced SVGs removed by the earlier default-carousel removal commit. The first visual run preserved error counts; a separate read-only browser diagnostic confirmed two 404 responses for the deleted assets. Remove only those obsolete static catalog entries, consistent with the previous deletion; do not recreate the removed banners or alter provider DB rows. The new asset-existence regression failed first. Full E2E assertion success alone is not browser-network acceptance; require new visual/network and complete gate evidence after this correction. The concurrently started host Release predates the catalog correction and is diagnostic only.

- `local_production_css_standard_filter_lost` — The resolved-policy production suite passed 85 cases including authentication/reset, then failed the existing 360px toast computed-style assertion: the standard backdrop-filter was none (one failed, 17 unrun). The optimized CSS contained only the WebKit declaration. The installed @tailwindcss/node optimizer reproduced the loss from the actual two glass rules when the prefixed declaration followed the standard declaration; standard-last retained both. A source-backed optimizer regression failed before the two-rule ordering correction. No timeout, assertion, browser mode or optimizer setting is weakened. Preserve the failed build/report and require a new complete production suite plus mobile/tablet/desktop visual evidence. The prototype's separately corrected Quick also exposed an optional-env TypeScript inference error; construct the explicit string-only allowlist instead of bypassing the type gate.

- `local_production_e2e_report_root` — The first Quick check during the nested-config prototype encountered generated HTML reporter JavaScript outside the repository's ignored artifact tree and failed lint. No application lint rule was relaxed. Bind production fixture report/output paths explicitly beneath the ignored task directory, preserve the original report there, and leave the desktop gate's own absolute report paths unchanged. Inspect the replacement Quick/Release output separately; the failed Quick is not acceptance.

- `local_production_e2e_policy_resolution` — After correcting cwd, the first production fixture suite failed its unchanged reset assertion before the first behavior test (one failed, 102 unrun), with a product-event persistence error. Inspection of the actual optimized reset bundle proved that the normal production-disabled policy remained compiled despite the fixture header. A plain webpack @/ alias test was insufficient for Next's resolved path imports. The new regression uses the actual resolved source path and failed before correction. Use the supported NormalModuleReplacementPlugin with exact resolved-resource identity, and reject an unreplaced normal policy in any fixture compiler graph. Preserve the failed build and trace; header/marker presence alone is not fixture execution proof. Production's default policy and Secure cookies remain unchanged. No artifact or deployment is approved by this diagnostic.

- `local_production_e2e_config_cwd` — During Issue #435's newly approved isolated production-runtime E2E implementation, the first local launch stopped before any test because the nested Playwright config resolved its relative server command from the config directory. The fixture production build had succeeded; this was a configuration-root error, not a product navigation result. Bind cwd and testDir to the repository root derived from import.meta.url and add a subprocess config regression preserving the base projects and retry zero. Keep the failed startup log; subsequent results remain separate. No archive, deployment or remote Actions trigger occurred. The small webpack boundary unit harness separately needed minimization disabled because Next's vendored default webpack minimizer is replaced by Next's own build configuration; the actual Next production build remains fully optimized and passed.

- Repository: `MyKnow/ssartnership`
- Freeze timestamp: `2026-08-13T14:48:40Z`
- Retained run range: `2026-04-12T07:04:05Z` through `2026-08-13T14:21:15Z`
- Fully paginated retained inventory: 3,362 runs
- Conclusions after the final in-progress run settled: 2,337 success, 195 failure, 136 cancelled, 694 skipped
- Workflow totals: Sync Preview Supabase 1,149; Verify Node Lockfile 1,018; Public Readiness 723; Publish Storybook 325; Storybook and Visual Baselines 66; Measure Admin Preview Performance 54; Apply Preview Supabase Migrations 16; Apply Production Supabase Migrations 11
- Event totals: push 1,602; workflow_run 1,005; pull_request 670; workflow_dispatch 85
- Failure/cancellation audit: all 331 runs classified from metadata/jobs; 264 full logs available, 67 unavailable
- Success audit: all 2,337 run/job records inspected; 1,755 full logs available and scanned, 582 returned `410 Gone`
- Skipped audit: 694 metadata records; 684 Sync Preview workflow-run no-ops, 8 Storybook PR guards, and 2 Preview migration guards. These normally have no executed log body.
- Expired success-log boundary: 582 runs from `2026-04-12T09:26:47Z` through `2026-05-15T08:37:37Z` (225 Verify Node Lockfile, 223 Publish Storybook, 134 Sync Preview Supabase)
- Two expired-log runs were overall `success` despite a failed install job and skipped publish job: `25095791143`, `25096099514`. Their job metadata is preserved; the exact dependency error is unknowable.

The audit is complete for the retained GitHub inventory, subject to the stated expired-log boundary. GitHub cannot prove runs that were deleted before the freeze. Exact frozen IDs and category coverage are preserved in [retained-actions-audit.md](retained-actions-audit.md).

## Change-aware tiered CI policy

- `verify:change` is the normal local and GitHub gate. The shared merge-base diff classifier selects `docs`, `development`, `ui`, `standard`, or `high`; labels and manual input may escalate but never downgrade. Unknown paths, empty/oversized ranges, deletions, copies, renames, type changes, and unresolved states fail closed to `high`.
- `docs` performs classification only; `development` runs changed-file lint and semantic typecheck; `ui` adds Node/unit tests; `standard` and `high` run trusted-install, lockfile, migration, full lint/type, unit, and dependency/security checks. Main-target `ui`/`standard` PRs add a critical smoke suite, while `high` PRs add the Production build and retry-free full E2E suite.
- The always-running `CI Policy Gate` independently derives the expected event/level plan and validates the classifier plan, conditional job result, and each verify/smoke/release step outcome. It is the branch-protection aggregation boundary. Vercel remains separately required because ignored deployments surface as cancelled provider states and cannot safely replace deployment evidence.
- `verify:release` remains the explicit complete local Production gate and is required before `dev` to `main` promotion.
- Storybook interaction, accessibility, and visual baseline workflows are manual. They remain available for explicit component-contract or visual work without creating ordinary push/PR notifications.
- Production-to-Preview data and Storage sync is manual and exact-`dev`-SHA bound. Preview migration automation runs only for `dev` pushes that change migration files or the schema snapshot; ordinary changes create no migration workflow run.
- Windows/macOS validate bootstrap, doctor, cross-platform policy, and focused development-environment contracts only when their path filter matches a PR or a manual dispatch.
- Repository-controlled test, workflow, dependency, security, and schema failures remain blocking. Recovered provider transients with verified final parity are operational observations, not reasons to rebuild unchanged source under a new SHA.

## Active rollout state

- Docker-dependent lockfile verification (`31699366066`, external registry 502) is fixed on `dev` by PR #358. GitHub Linux now uses pinned Node/npm without Docker or `npx`. It is not considered Production-complete until the same contract reaches `main` and the exact main SHA passes.
- Hidden Playwright retries affected 441 successful retained runs. The current #360 change sets required Playwright retries to zero and retains failure traces. This is not effective until merged to `dev`, then `main`.
- Preview Sync no-op concurrency cancellation affected 51 retained runs. PR #359 isolates eligible mutations from no-op completions, but its latest Storybook stabilization commit is intentionally unpushed until #360 establishes this skill contract. A `workflow_run` fix is not authoritative until its workflow exists on default branch `main`.
- PR #359 run `31709729097` failed one Push Composer Story interaction. The original run is preserved. Local commit `f360e55b` scopes the dialog, verifies controlled input state, and passed retry-disabled focused repetition 5 times plus the full 462-story gate. Replacement push SHA `f360e55b87f49b07f71921b1e1659780317dd864` then started Storybook run `31758399190` and Public run `31758399208` on `2026-08-14T00:44:54Z`; the Storybook rerun is replacement evidence for the interaction fix, while the Public rerun failed earlier at dependency audit before product assertions could begin.
- #360's independent security review found three privileged-workflow trust-boundary defects and one private-log defect: an operator URL could receive Preview admin credentials, Admin Performance and Preview migration workflows selected floating `dev`, and Storage failures could print private object paths. The local #360 change pins the admin origin, binds privileged execution to exact checked-out SHAs with pre-mutation live-ref checks, and emits only structural Storage status/code diagnostics. None is effective until merged to `dev`, then `main` where workflow definitions are authoritative.
- PR #361's first Vercel Preview deployment `dpl_BxUWMDkcciGxMtEHkRV9zfmznxaM` failed before dependency materialization because the provider supplied npm 11.12.1 while the initial policy required npm 11.16.0. Source and executable review proved 11.12.1 preserves this repository's actual `ignore-scripts`/`allow-git` boundary, so the local correction accepts the reviewed 11.12.1–11.x provider range while GitHub remains exact 11.16.0. Replacement SHA `d5472bce998bbd7f46fc8732bab12bc36ed51ede` produced first-attempt deployment `dpl_Hg11FTBW3R3ZbDsk5PmXUZKBaZfb`; follow-up SHA `dd49d3db877f2e005843098df1b049f141d11315` produced first-attempt deployment `dpl_DLFqL1wVWwtWXrURUhnEfWKMVZ3Y`. Both retained provider records prove npm 11.12.1, 861 installed packages, zero dependency lifecycle execution, a successful 103-route Production build, and final `READY` state. This signature is therefore fixed on the feature PR branch only; `dev` and `main` still require their own first-attempt verification.
- PR #361's first Public Readiness run `31726987495` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/31726987495/attempts/1)), `pull_request`, exact SHA `3125f8cb3511cd306e0deb7c64accd93f0921e4f`, ran `2026-08-13T17:41:46Z` through `2026-08-13T17:52:07Z` and concluded `success`. All jobs and required steps succeeded, annotation collection was complete with none present, and the structural audit was complete. A first audit found three `timeout` and one `deprecation` matches that were passed test descriptions, plus 170 mock-server diagnostics and one nonfatal cache reservation collision. Replacement SHA `d5472bce998bbd7f46fc8732bab12bc36ed51ede` then produced first-attempt Public run `31728947732` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/31728947732/attempts/1)) with every required step successful, no annotations, and 172 `mock_server_error` signatures. Follow-up SHA `dd49d3db877f2e005843098df1b049f141d11315` produced first-attempt run `31731125230` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/31731125230/attempts/1)) from `2026-08-13T18:30:59Z` through `2026-08-13T18:41:14Z`; its required job and every step succeeded, annotation/log collection was complete with none present, and all 86 Playwright tests passed without retry. The persistence correction removed every `log_insert_failed`/`ingest_exception` match, but eight transport signatures remained: four `aborted`, four `ECONNRESET`, and two overlapping `uncaughtException`, clustered between passed test ordinals 82/83 and 85/86. SHA `404b3ecef8ae26cf20b6a58a434100d04a925456` then produced first-attempt run `31735631329` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/31735631329/attempts/1)) from `2026-08-13T19:23:55Z` through `2026-08-13T19:34:07Z`; every job and step still succeeded, annotation/log collection was complete with none present, and all 86 Playwright tests again passed without retry, but the transport signature only shrank to six matches: three `aborted`, three `ECONNRESET`, and two overlapping `uncaughtException`. The same run also emitted two `next_dev_full_reload` diagnostics, so the success log is still not clean evidence. The earlier 82/83 transport cluster disappeared, while all six remaining transport lines occurred between ordinals 85/86. This strongly implicates repeated navigation inside the responsive viewport loop: a final `networkidle` wait cannot undo responses already aborted by the next navigation. The next correction navigates once before the viewport loop, resizes the loaded page, retains the targeted final wait, and keeps the source contract that rejects a suite-wide request-drain `afterEach`; replacement-SHA Public evidence is pending. The cache collision remains recorded as nonfatal provider post-job contention rather than a test/product failure.
- The first local repetition for the `404b3ece` follow-up used one `--repeat-each=5` Playwright process. Its first administrator suite passed, but the shared development server then exited and later repetitions failed with connection refusal. That distinct long-lived-server stress signal has unresolved causality: it was preserved, not retried into green, and is not used as proof for or against the original teardown signature. Five independent fresh-server executions of the affected responsive case and one fresh full administrator suite subsequently passed with retries disabled and without the transport signature. The complete CI-shaped pre-push gate then passed, including all 86 retry-free E2E cases without the transport signature. Replacement-SHA first-attempt evidence is still pending.
- A fresh pre-merge local `npm run prepush` on exact PR #361 head `0d6562fc15a387af0e977274ebe981c780ecf28c` completed all 86 retry-free E2E cases but emitted one `aborted` and one `ECONNRESET` signature between passed page-smoke ordinals 31 and 32, followed later by two reviewed full-reload diagnostics. This changed the transport-error context from the administrator responsive flow to adjacent public smoke routes, so the run is preserved as abnormal local evidence and does not authorize merge. Adding a terminal `networkidle` to the rendered/redirect helpers passed five fresh-server reproductions of the original route pair, but a full 58-case page-smoke run then produced the same pair between ordinals 20 and 21. Network idle alone was therefore rejected as sufficient proof. Every test creates and closes a new browser context while the root `PwaProvider` registered a service worker from a late client effect even under the Next development server and explicit mock data source. The correction keeps the fail-closed terminal idle boundary and prevents PWA registration outside a Production, non-mock runtime; a source contract preserves both guards and forbids a suite-wide `afterEach`. Three consecutive, independent fresh-server executions of the complete 58-case page-smoke suite then passed with retries disabled and with zero `aborted`, `ECONNRESET`, or `uncaughtException` transport signatures. The complete prepush subsequently passed every gate, including 1,158 Node tests, 68 unit tests, the 103-route build, and all 86 retry-free E2E cases; its E2E log had zero transport signatures and retained exactly two already-reviewed Next development full-reload diagnostics. Replacement-SHA first-attempt remote evidence is still required before rollout.
- PR #361 was merged into `dev` as `f3cacb9a62ca804c833b253f8f80dec1f5b0227f` at `2026-08-14T00:45:17Z` while that replacement remained a validated local-only commit. Push Public run `31758425375` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/31758425375/attempts/1)) completed with 86 retry-free Playwright passes, zero transport or persistence signatures, two reviewed between-test full-reload diagnostics, one nonfatal cache-save collision, and no annotations. The simultaneous moving-PR #351 Public run `31758428358` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/31758428358/attempts/1)) also concluded `success` with all 86 tests and no retries or annotations, but emitted three `aborted`, three `ECONNRESET`, and two overlapping `uncaughtException` signatures: one transport line occurred between passed ordinals 83/84 and the remaining five between 84/85. It also retained the two reviewed full reloads and one cache collision. The identical SHA's divergent parallel-run evidence rejects a clean rollout claim and proves the old head remained concurrency-sensitive.
- PR #359 replacement head `ef4234c2776f6784e9d133109e0d48b1fa3e5c9b` then produced Public run `31758910145` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/31758910145/attempts/1)). Every job and step succeeded, annotations were complete with none present, and all 86 Playwright tests passed without retry, but the log retained four `aborted`, four `ECONNRESET`, and two overlapping `uncaughtException` signatures: two transport lines occurred between passed ordinals 84/85 and six between 85/86. Persistence-specific signatures remained zero and two reviewed full reloads were present. Despite that pending abnormal-success audit, and while Preview Sync `31758981252` was still mutating shared Preview state, #359 was merged into `dev` as `5c5d2ddaa9e33631464e72a56f846583ccbb06f4` at `2026-08-14T01:04:36Z`. The original runs and sequencing violation are preserved. Post-merge push Public run `31759461251` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/31759461251/attempts/1)), `push`, exact SHA `5c5d2ddaa9e33631464e72a56f846583ccbb06f4`, ran `2026-08-14T01:04:39Z` through `2026-08-14T01:14:27Z` and concluded `failure`. Job index 0 failed at step index 12 / step number 13; the following post-setup step was expectedly skipped. Annotation collection was complete with one failure annotation at line 143 whose title was absent and message/path were present; it matched no fixed content signature. The structural log retained `failed_attempt_marker` at line 1832, `test_failure` at 1941, `github_error` at 1944, and the two reviewed `next_dev_full_reload` lines at 1897/1899. The former auditor counted two `timeout` matches at 1911/1914; the corrected classifier reports no elapsed-timeout/exception signature for this run. The public partner CTA navigation was the only failed case: 85 tests passed, retries/flaky and transport/persistence/cache warnings were zero, the click completed, and the URL remained at the home root through the five-second assertion. Same-SHA moving-PR run `31759463908` passed 86/86 without retry or transport, which confirms intermittency but cannot replace the failed push first attempt. The correction waits for the existing Home directory hydration marker, arms a bounded 15-second URL wait before the click, asserts the destination, and drains terminal network activity; an executable source contract forbids force-click, sleeps, and retry. The corrected case passed five independent fresh-server executions, then the complete 10-case Home partner suite passed. The full CI-shaped pre-push gate also passed install/lock/migration/lint/type checks, 1,160 Node tests, 68 unit tests, the zero-advisory Production dependency audit, the 103-route build, and all 86 retry-free E2E tests without transport or persistence diagnostics; it retained only the two reviewed Next development full reloads. At that timestamp, replacement-SHA first-attempt evidence remained pending.
- PR #362 first-attempt Public run `31762781595` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/31762781595/attempts/1)), `pull_request`, exact SHA `79a88557575ffdb30649469e4f9e4dd9f49dd6b0`, was created at `2026-08-14T02:07:46Z`; job index 0 ran `02:07:49Z` through `02:17:09Z`, the run updated at `02:17:10Z`, and both concluded `failure`. Steps 0–11 succeeded; E2E step index 12 / step number 13 failed from `02:10:51Z` through `02:17:07Z`; post-Setup Node at index 13 / step number 25 was expectedly skipped and the remaining post steps succeeded. Annotation collection was complete with one failure annotation at line 214 whose title was absent and message/path were present; it matched no fixed signature. Log, terminal, and identity evidence were complete. The fixed log signatures were three `failed_attempt_marker` lines at 1909/1912/1914, one `test_failure` at 2022, one `github_error` at 2027, and two `next_dev_full_reload` lines at 1910/1913. The former auditor counted eight `timeout` lines at 1919/1930/1933/1957/1968/1971/1995/1997; the corrected classifier identifies six actual timeout lines at 1919/1930/1957/1968/1995/1997, while 1933/1971 were printed source-option false positives. Retry, flaky, transport reset, uncaught exception, persistence, npm warning, cache collision, and browser-console signatures were zero. All install/lock/migration/lint/type/unit/security/build gates passed, but the E2E result was 83/86: three administrator-console cases exhausted the 30-second test budget only inside unconditional global `networkidle` waits after the expected admin URL or route had already loaded. The two full reloads followed failed cases and therefore do not qualify for the reviewed between-passing-test baseline. This is a test/Next-development-lifecycle defect, not a product assertion, dependency, provider, connection-refusal, or telemetry-transport failure: the latter signatures were absent, while admin development recompilation and route prefetch are allowed to keep network activity alive after the shell is usable. The correction replaces raw admin `networkidle` with an explicit hydrated-shell marker plus route-specific URL/control assertions, and the source contract forbids reintroducing raw admin network-idle waits. The focused source contract, affected lint, and semantic typecheck passed; five independent fresh-server executions of the complete administrator suite passed 25/25 without retry, timeout, transport, or persistence signatures. The complete Node 24.18.1/npm 11.16.0 prepush then passed the trusted install, canonical lock, 168 migrations, lint, semantic typecheck, all 1,161 Node tests, 68 unit tests, zero-advisory Production dependency audit, 103-route build, and all 86 retry-free E2E tests in 2.8 minutes. Its only E2E lifecycle diagnostics were the two reviewed full reloads between successful administrator cases; timeout, transport, persistence, retry, flaky, and failed-test signatures were zero. At that timestamp, replacement-SHA first-attempt evidence remained pending and rollout remained local on the #362 branch; the failed head was preserved, and neither `dev` nor `main` contained this correction.
- PR #362 replacement SHA `910a5ac625df794f67588d2b39e06515356b0a32` produced clean first-attempt Public run `31765098625` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/31765098625/attempts/1)) and Vercel deployment `dpl_J7S5zXXFFPEcRnvXjFJg8id963oA`, but Storybook run `31765098610` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/31765098610/attempts/1)), local workflow identifier `.github/workflows/storybook.yml`, event `pull_request`, created `2026-08-14T02:53:26Z`, updated `02:58:00Z`, concluded `failure`. Job index 0 ran `02:53:29Z`–`02:57:59Z`; steps 0–5 succeeded, step index 6 / number 7 failed at `02:54:34Z`–`02:57:57Z`, step index 7 / number 13 was an expected guard skip after failure, and remaining post steps succeeded. Job index 1 was the expected PR-guard skip. Annotation collection was complete: two failure annotations, one at start/end line 434/434 with title absent/message+path present, and one local-Story annotation at start/end line 291/291 with title/message/path present; neither matched a fixed annotation signature. Structural identity, terminal metadata, log retrieval, and audit completeness all passed. Results were 149/150 files and 461/462 Stories; retry, flaky, browser console error, React act, npm warning, cache collision, deprecation, and hidden-timeout evidence were zero. Fixed log evidence was `test_failure` 1 at line 1927, `github_error` 2 at 1965/1986, unchanged browser externalization 41, and unchanged bundle warning 2. The former auditor also counted one `timeout` at line 1952 solely because a printed local source excerpt contained a Testing Library `{ timeout: 4000 }` option; the timeout classifier is now narrowed to actual elapsed-timeout/exception phrases with a synthetic regression. The failed Story opened the recipient modal and immediately typed into its controlled search input, while the shared Modal schedules its declared initial focus to the header close button on the next animation frame. That frame could steal focus between the typing helper's click and key events, leaving the controlled value empty. The same byte-identical Story passed once on earlier SHA `f360e55b87f49b07f71921b1e1659780317dd864`, proving the earlier dialog scoping/value assertion exposed but did not remove the scheduling race. The proven class is a Story scheduling/initial-focus race, not product filtering, fetch, or controlled-state logic. The correction first verifies the declared initial-focus target, explicitly focuses and verifies the intended search input, then types and verifies the controlled value; sleeps and retries remain forbidden. A source-order regression preserves that sequence through the filtered result assertion. The focused Actions-skill contract passed 15/15, the affected Story passed 2/2 in five independent retry-free processes, the full Storybook suite passed 150/150 files and 462/462 Stories with `--retry=0`, and the Storybook build, affected lint, and semantic typecheck all passed. The complete local prepush then passed the trusted install, canonical lock, 168 migrations, lint, semantic typecheck, all 1,162 Node tests, 68 unit tests, the zero-advisory Production dependency audit, the 103-route build, and all 86 retry-free E2E tests in 2.6 minutes; transport, persistence, timeout, retry, flaky, and failure evidence was zero, with only the two reviewed full reloads between successful administrator cases. At that timestamp, replacement-SHA first-attempt Storybook evidence was pending, so rollout remained blocked on #362 and absent from `dev`/`main`.
- PR #362 corrective SHA `02bc28b96fa5ddf3c08f5a113a088711037f7237` resolves every preceding feature-branch replacement-evidence marker for the #359/#361 follow-up. Public Readiness run `31767060216` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/31767060216/attempts/1)), local workflow identifier `.github/workflows/public-readiness.yml`, event `pull_request`, was created `2026-08-14T03:31:23Z`, updated `03:42:08Z`, and concluded `success`. Job index 0 ran `03:31:26Z`–`03:42:07Z`; step indices 0–15 / numbers 1–13 and 25–27 all succeeded, annotation collection was complete with zero annotations, and identity, terminal, log, and audit completeness all passed. It used Node 24.18.1/npm 11.16.0, installed 858 packages with zero lifecycle execution, validated 168 migrations, passed 1,162 Node tests with 1,154 pass and eight intentional skips, passed 68 unit tests, built 103/103 routes, and passed all 86 Playwright cases in 6.9 minutes. Retry, flaky, failure, elapsed timeout, transport, persistence, browser-console, npm/GitHub warning, cache collision, Storage retry/skip, and database-fallback signatures were zero. The only fixed signature was the unchanged `next_dev_full_reload` count of two between passed E2E ordinals 82/83 and 83/84. Storybook run `31767060223` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/31767060223/attempts/1)), local workflow identifier `.github/workflows/storybook.yml`, event `pull_request`, was created `03:31:23Z`, updated `03:36:10Z`, and concluded `success`; job index 0 and every executed step succeeded, job index 1 was the expected `workflow_dispatch`-only PR guard skip, annotation collection was complete with zero annotations, and all structural completeness checks passed. It passed 150/150 files and 462/462 Stories with retry, flaky, failure, browser-console, React-act, GitHub warning/error, timeout, and deprecation signatures all zero; browser externalization 41 and bundle warning 2 exactly matched the reviewed tooling baseline. The same SHA produced exactly one Vercel Preview deployment, `dpl_6QRKYQvMK2jA8XTdN8SCTn5xmoqS`: Node 24.x project runtime, npm 11.12.1, 861 packages, zero lifecycle execution, 103/103 static generation, complete cache upload, final `READY`, and no warning/error/retry/timeout/transport/deprecation signal. This is clean first-attempt branch evidence for the CTA hydration/navigation, mock analytics transport, PWA/page-smoke lifetime, administrator readiness, timeout-classifier, and Story initial-focus corrections. The feature branch is verified; rollout remains absent from `dev` and `main` until #362 is merged and each resulting exact SHA is audited.
- #361 post-merge Preview Sync run `31758981252` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/31758981252/attempts/1)), `workflow_run`, definition SHA `3c2528e2709a658a3ddc884ec1a88715ea994724`, ran `2026-08-14T00:55:44Z` through `2026-08-14T01:13:42Z` and concluded `success`. Job index 0 and every eligible step succeeded, the stale-only step at index 2 was expectedly skipped, annotation collection was complete with none present, and data synchronization itself completed with 168 migrations, 84 tables, 7 Storage buckets, 710/710 member credentials stripped, no storage retry/skip/fallback, and pre/post schema parity. Its complete log nevertheless contained 20 `npm_engine_warning` lines at 336–355, grouped into four engine-warning blocks. `workflow_run` loaded the old default-`main` definition, which selected Node 20 and raw dependency installation before checking out the Node 24/npm 11 contract from exact target SHA `f3cacb9a62ca804c833b253f8f80dec1f5b0227f`. The auditor now classifies `npm warn EBADENGINE` explicitly. The corrected Node 24/trusted-install workflow is present on `dev`, but cannot become the authoritative `workflow_run` definition until an authorized `main` promotion; until then, any recurrence must remain recorded and cannot be called clean.
- Status resolution: every `pending`, `blocked`, or `local` rollout statement in the preceding chronological incident bullets records the state at that incident timestamp. For the #359/#361 corrective feature branch, those historical branch-evidence markers are superseded by the clean `02bc28b96fa5ddf3c08f5a113a088711037f7237` evidence above; only `dev` and `main` rollout remain pending.
- Production dependency/security/type/unit/migration failures are genuine gates. Do not suppress them as notification noise.

## Known signatures and prevention

### Required tests and UI gates

- `hidden_playwright_retry` — 441 nominally successful runs, `2026-06-23` through `2026-08-13`. Evidence includes `✘`, `retry #1/#2`, and final `N flaky`. Prevention: `retries: 0`, `trace: retain-on-failure`, readiness/hydration fixes, deterministic state reset, and raw-log zero-marker verification.
- `public_e2e_regression_or_flake_exhausted` — 54 failures. These include partner discovery, auth recovery, admin, and route smoke defects. Prevention depends on the proven failing surface; never collapse them into a generic flaky label.
- `storybook_interaction_or_a11y_failure` — 7 failures in the frozen retained-history census, plus post-freeze PR #362 run `31765098610`. Root causes include shared draft/mock state and unverified async UI transitions. Scope queries to the active dialog, assert controlled state, reset per-story state, and run the full interaction/a11y gate.
- `storybook_modal_initial_focus_race` — PR #362 replacement run `31765098610` failed one interaction because dialog DOM presence preceded the shared Modal's next-frame initial-focus transfer. A Story that types before that declared focus target settles can lose input focus mid-sequence even when its controlled-value assertion is present. Wait for the initial-focus target, explicitly focus and assert the intended input, then type and assert its controlled value. Do not use sleep, retry, or a broader dialog query as a substitute.
- `storybook_visual_baseline_drift` — 2 failures. Keep renderer and font environment canonical; inspect images before updating a baseline.
- `public_playwright_browser_install_timeout` — 2 historical failures. Keep browser setup explicit and pinned; classify provider download failure separately from product failure.

### Lockfile, dependencies, and toolchain

- `lockfile_canonical_drift` — 20 standalone plus 6 Public failures. Run canonical lockfile verification before every push and keep the generated Linux metadata committed.
- `public_lockfile_external_docker_registry_failure` — run `31699366066`. Docker Hub returned 502. The repository removed this avoidable CI registry dependency in PR #358.
- `vercel_install_runtime_npm_floor_drift` — PR #361, immutable deployment `dpl_BxUWMDkcciGxMtEHkRV9zfmznxaM` ([provider record](https://vercel.com/myknows-projects/ssartnership/BxUWMDkcciGxMtEHkRV9zfmznxaM)), exact SHA `3125f8cb3511cd306e0deb7c64accd93f0921e4f`, created `2026-08-13T17:41:13Z`, first attempt, GitHub Vercel status `FAILURE`. The only failed provider stage was dependency installation: Vercel used Node 24.15.0 with npm 11.12.1, and the initial policy rejected it before `npm ci` or dependency code ran. This rules out lockfile drift and product/build failure. The cause is a repository/provider runtime-contract mismatch: an npm 11.16 floor was inferred without first proving the provider's bundled minor or identifying a security difference relevant to this repository. Primary-source review confirmed npm 11.12.1 passes `ignore-scripts` into installation, its directory fetcher returns before `prepare`, and `allow-git=none` is supported; the repository also rejects every Git/remote/local source except one byte-verified local package. The correction therefore removes unsupported warning-producing policy keys, enforces exact `.npmrc` plus effective `ignore-scripts=true`, `allow-git=none`, and registry-resolution retention, accepts only npm 11.12.1–11.x outside GitHub, and keeps GitHub exact at 11.16.0. The focused contract and an actual 11.12.1 trusted installation passed with 854 packages and zero lifecycle execution or warnings. Replacement deployment `dpl_Hg11FTBW3R3ZbDsk5PmXUZKBaZfb` ([provider record](https://vercel.com/myknows-projects/ssartnership/Hg11FTBW3R3ZbDsk5PmXUZKBaZfb)), exact SHA `d5472bce998bbd7f46fc8732bab12bc36ed51ede`, then used npm 11.12.1, installed 861 packages with zero dependency lifecycle execution, built 103 routes, and reached `READY` on its first attempt. There is no GitHub Actions job or annotation for this external status context; the simultaneously created Public and Storybook runs are separate evidence. Rollout: fixed on the feature PR branch; `dev` and `main` still require independent first-attempt verification.
- `publish_storybook_lockfile_npm_ci_mismatch` — 13 historical failures; `workflow_runtime_or_npm_ci_mismatch` — 1; `legacy_verify_workflow_chromatic_external_or_runtime` — 4. Preserve package/lock parity and keep concerns in dedicated workflows.
- `public_dependency_audit_prod_advisory` — 10; `public_dependency_policy_advisory` — 2; `public_security_policy_advisory` — 1. Fix or explicitly govern the cited advisory. Distinguish the blocking Production audit from informational full-tree output. PR #359 replacement Public run `31758399208` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/31758399208/attempts/1)), `pull_request`, exact SHA `f360e55b87f49b07f71921b1e1659780317dd864`, failed at `2026-08-14T00:46:38Z` in the `Dependency audit` step before lint/build/E2E assertions. The only blocking signature was the Production advisory on `node_modules/postcss/node_modules/nanoid <3.3.18`; this branch was still based on pre-`#361` `dev` and had not yet absorbed the reviewed `nanoid` override or trusted-install skill package. Prevention is to rebase or merge the latest `dev` before re-triggering `#359`, because that base already carries `nanoid: 3.3.18`, the reviewed install policy, and the current Actions ledger. Rollout: failure preserved on the original rerun SHA; rebased corrective SHA `02bc28b96fa5ddf3c08f5a113a088711037f7237` supplied clean replacement Public evidence in run `31767060216`, while `dev`/`main` rollout remains pending. PR #366 Public run `32373438387` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/32373438387/attempts/1)), local workflow identifier `.github/workflows/public-readiness.yml`, event `pull_request`, exact SHA `5a26d9d82bd61041c085e6943cb0860889e056d5`, was created `2026-08-20T13:17:34Z`, updated `13:19:19Z`, and concluded `failure`. Job index 0 / database ID `96439059060` ran `13:17:43Z`–`13:19:18Z`; steps 0–8 succeeded, dependency-audit step index 9 / number 10 failed, downstream step indices 10–13 / numbers 11–13 and 25 were expectedly skipped, and post-step indices 14–15 / numbers 26–27 succeeded. Annotation collection was complete with one failure annotation at line 17 whose title was absent, message/path were present, and fixed annotation signatures were empty. Identity, terminal, log, and audit completeness all passed; the only fixed log signature was `github_error` once at line 1170. The blocking Production advisory was again `nanoid <3.3.18`, because the Phase 7 branch was deliberately cut from current `main`, where the root override still fixed `postcss` to `nanoid: 3.3.17`; trusted installation, canonical lock, migrations, lint, typecheck, and unit tests had already passed, while build and E2E never began. This rejects runtime, installer, lock, schema, lint, type, product-build, and browser-test failures. The correction pins the root override and canonical registry lock entry to `nanoid: 3.3.18`, adds an executable package/lock identity regression, and requires `npm audit --omit=dev`, trusted install policy, lock verification, full tests, and pre-push verification before replacement. The failed head and first attempt remain immutable; replacement-SHA evidence is pending, PR #366 remains blocked, latest `dev` already contains the safe override independently, and neither `main` nor the Phase 7 branch contains the correction remotely. Residual risk is future advisory or transitive override drift, so the blocking Production audit remains mandatory.
- `cross_platform_windows_file_url_path_failure` — PR #366 Cross-Platform Development Environment run `32373438488` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/32373438488/attempts/1)), local workflow identifier `.github/workflows/cross-platform-development.yml`, event `pull_request`, exact SHA `5a26d9d82bd61041c085e6943cb0860889e056d5`, was created `2026-08-20T13:17:34Z`, updated `13:21:01Z`, and concluded `failure`. macOS arm64 job index 0 / database ID `96439058775` and all of its step indices 0–12 succeeded. Windows x64 job index 1 / database ID `96439059205` ran `13:17:39Z`–`13:20:01Z`; steps 0–7 succeeded, Test step index 8 / number 9 failed, Build step index 9 / number 10 and its Setup Node post-step index 10 / number 19 were downstream skips, and remaining post steps 11–12 / numbers 20–21 succeeded. Annotation collection was complete with one failure annotation at line 5652 whose title was absent, message/path were present, and fixed annotation signatures were empty. Identity, terminal, log, and audit completeness all passed. Fixed log evidence was `github_error` once at line 7321 and two lexical `deprecation` hits at lines 2472/5093 caused by a duplicated failing test description containing that word, not a Node or dependency deprecation warning. The Windows-only root cause is URL/path conversion and comparison: the test alias loader derived the repository root from a file URL's raw pathname, producing a drive-prefixed path that could not resolve `@/` imports, six source-contract tests repeated the same conversion and produced doubled drive paths, and one intentional terminology allowlist compared POSIX literals against backslash-separated `path.relative` output. Bootstrap, Doctor, policy, lint, and typecheck all succeeded on Windows, and the complete macOS job including build succeeded, ruling out runtime setup, native binary, dependency install, source type, and macOS regressions. The correction must use `fileURLToPath` at every file-URL boundary, normalize relative paths only for portable comparison, and add an executable source/runtime regression that rejects raw file-URL pathname roots and proves alias resolution on the host. The failed head remains immutable; replacement-SHA first-attempt Windows and macOS evidence is pending, PR #366 is blocked, and the correction is absent from `dev` and `main`. Remaining risk is another hidden separator or drive-letter assumption, so a clean Windows x64 matrix first attempt is mandatory and cannot be replaced by macOS-local evidence.
- `cross_platform_windows_vitest_absolute_glob` — PR #366 Cross-Platform Development Environment run `32375613398` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/32375613398/attempts/1)), local workflow identifier `.github/workflows/cross-platform-development.yml`, event `pull_request`, exact SHA `beb36701476f01f9f76ade145139df569f9d752a`, was created `2026-08-20T13:40:29Z`, updated `13:43:50Z`, and concluded `failure`. macOS arm64 job index 0 / database ID `96446152452` and all of its step indices 0–12 succeeded. Windows x64 job index 1 / database ID `96446152663` ran `13:40:34Z`–`13:43:09Z`; steps 0–7 succeeded, Test step index 8 / number 9 failed, Build step index 9 / number 10 and Setup Node post-step index 10 / number 19 were expected downstream skips, and post-step indices 11–12 / numbers 20–21 succeeded. Annotation collection was complete with one failure annotation at line 932 whose title was absent and message/path were present; fixed annotation signatures were empty. Identity, terminal, log, and audit completeness all passed, and the only fixed log signature was `github_error` once at line 2604. The complete Node suite passed with 797 tests, 789 passes, eight intentional skips, and zero failures, proving the prior file-URL, alias-loader, and separator correction. The following Vitest unit project found zero files because `vitest.config.ts` built its include glob with `path.join`, producing a drive-rooted backslash glob; Vitest reported `No test files found`, then the empty coverage result failed every threshold. This is a Windows glob-construction defect, not a unit assertion, coverage regression, runtime, native package, installer, Doctor, policy, lint, type, or macOS failure. The correction must keep the Vitest include as a repository-relative forward-slash glob and add an executable config-source contract; absolute host paths remain appropriate only for non-glob config inputs. The failed head and first attempt remain immutable; replacement-SHA first-attempt Windows build evidence is pending, PR #366 is blocked, and the correction is absent from `dev` and `main`. Remaining risk is another tool-specific glob receiving an OS separator, so the complete Windows x64 matrix remains mandatory.
- `cross_platform_windows_npm_cli_discovery_failure` — PR #368 Cross-Platform Development Environment run `32389619407` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/32389619407/attempts/1)), local workflow identifier `.github/workflows/cross-platform-development.yml`, event `pull_request`, exact SHA `d83c467952129019f9215e04a07c24377a971ad6`, was created `2026-08-20T16:01:34Z`, updated `16:05:47Z`, and concluded `failure`. macOS arm64 job index 0 / database ID `96492311180` and every step succeeded. Windows x64 job index 1 / database ID `96492311667` ran `16:01:38Z`–`16:05:02Z`; steps 0–8 succeeded, Test step index 9 / number 10 failed, Build and Setup Node post-steps were expected downstream skips, and the remaining post-steps succeeded. Annotation collection was complete with one failure annotation at line 1569 whose title was absent and message/path were present; identity, terminal, log, and audit completeness all passed. Fixed structural evidence was `github_error` once at line 3675; retry, flaky, transport-reset, and development-server full-reload signatures were zero. The failure is a Windows runtime-layout discovery defect: the direct-Node npm CLI resolver did not find the installed npm CLI from the hosted Windows Node layout, while bootstrap, Doctor, policy, lint, typecheck, and the entire macOS matrix succeeded. This rejects application behavior, native dependency installation, package policy, cross-platform policy, type, and macOS regressions. The correction must include the Windows parent-directory npm layout in the resolver and an executable injected-path regression; do not bypass Doctor or weaken the exact Node/npm contract. The immutable failed head remains preserved. Replacement-SHA Windows and macOS first attempts, plus Public and Storybook checks, are pending; the fix is absent from `dev` and `main`. Remaining risk is another Node distributor layout, so Windows x64 CI must stay mandatory.
- `cross_platform_windows_regex_path_assertion_failure` — PR #368 Cross-Platform Development Environment run `32392208071` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/32392208071/attempts/1)), local workflow identifier `.github/workflows/cross-platform-development.yml`, event `pull_request`, exact SHA `1f54a6c85125af86dcc6402abadae0f982ac1083`, was created `2026-08-20T16:28:45Z`, updated `16:33:06Z`, and concluded `failure`. Windows x64 job index 0 / database ID `96500751414` ran `16:28:50Z`–`16:32:09Z`; steps 0–8 succeeded, Test step index 9 / number 10 failed, Build and Setup Node post-steps were expected downstream skips, and post-steps 12–13 / numbers 22–23 succeeded. macOS arm64 job index 1 / database ID `96500751621` and every step succeeded. Annotation collection was complete with one failure annotation at line 1570 whose title was absent and message/path were present. Identity, terminal, and log evidence were complete; the only fixed signature was `github_error` once at line 1823, with retry, flaky, transport-reset, browser-console, and development-server full-reload signatures all zero. The failed assertion compared a controlled PATH prefix through a regular expression constructed from the Windows Node executable path, so path separators were interpreted as regex escapes. This is a cross-platform test assertion defect, not an npm resolver, application, install-policy, runtime, build, dependency, or macOS defect. The correction compares the controlled PATH to `dirname(process.execPath)` as a literal and retains the existing security-policy test. Replacement-SHA Windows/macOS, Public, and Storybook first attempts are pending; the correction is absent from `dev` and `main`. Remaining risk is another path value interpolated into a regular expression, so Windows x64 CI remains mandatory.
- `cross_platform_windows_output_path_literal_failure` — PR #368 Cross-Platform Development Environment run `32393734935` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/32393734935/attempts/1)), local workflow identifier `.github/workflows/cross-platform-development.yml`, event `pull_request`, exact SHA `d0811c6883dbbf220442225ff926c76cd6c9694f`, was created `2026-08-20T16:44:54Z`, updated `16:48:48Z`, and concluded `failure`. macOS arm64 job index 0 / database ID `96505638054` and all steps succeeded. Windows x64 job index 1 / database ID `96505638432` ran `16:44:58Z`–`16:48:07Z`; steps 0–8 succeeded, Test step index 9 / number 10 failed, Build step index 10 / number 11 and Setup Node post-step index 11 / number 21 were expected downstream skips, and post-steps 12–13 / numbers 22–23 succeeded. Annotation collection was complete with one failure annotation at line 1553 whose title was absent and message/path were present. Identity, terminal, and log evidence were complete; the only fixed signature was `github_error` once at line 3663, with retry, flaky, timeout, transport-reset, browser-console, and development-server full-reload signatures all zero. The auditor hostile-text contract fixed its expected temporary path as a POSIX literal, while Windows legitimately returns its native resolved path. This is a Windows-only test assertion defect, not an auditor containment defect, dependency/toolchain fault, npm resolver failure, build regression, or macOS failure. The correction verifies that the generated target is the expected relative child path using the host path module; this regression keeps the direct-child containment test while removing the separator and drive-root assumption. Replacement-SHA Windows/macOS, Public, and Storybook first attempts are pending; the correction is absent from `dev` and `main`. Remaining risk is another literal host-path assertion, so the mandatory Windows x64 matrix remains the acceptance boundary.
- `cross_platform_windows_audit_mode_failure` — PR #368 Cross-Platform Development Environment run `32395988441` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/32395988441/attempts/1)), local workflow identifier `.github/workflows/cross-platform-development.yml`, event `pull_request`, exact SHA `0fd4fc0ed7a9a6abf174b1ba68d6ceea8088e31f`, was created `2026-08-20T17:09:00Z`, updated `17:12:52Z`, and concluded `failure`. Windows x64 job index 0 / database ID `96512805555` ran `17:09:04Z`–`17:12:05Z`; steps 0–8 succeeded, Test step index 9 / number 10 failed, Build step index 10 / number 11 and Setup Node post-step index 11 / number 21 were expected downstream skips, and post-steps 12–13 / numbers 22–23 succeeded. macOS arm64 job index 1 / database ID `96512805629` and every step succeeded. Annotation collection was complete with one failure annotation at line 1553 whose title was absent and message/path were present. Identity, terminal, and log evidence were complete; the only fixed signature was `github_error` once at line 1806, with retry, flaky, timeout, transport-reset, browser-console, and development-server full-reload signatures all zero. The auditor contract asserted POSIX permission mode `0600` on a Windows-created file, where the runtime does not provide that portable mode-bit guarantee. This is a Windows-only test assertion defect, not an audit-file containment, overwrite, symlink, dependency/toolchain, npm, build, or macOS defect. The correction requires a newly created regular file on every host and retains the `0600` assertion only on hosts that expose POSIX mode semantics; existing no-overwrite and symlink-escape contracts remain executable across Windows. Replacement-SHA Windows/macOS, Public, and Storybook first attempts are pending; the correction is absent from `dev` and `main`. Remaining risk is another POSIX filesystem assumption, so the mandatory Windows x64 matrix remains the acceptance boundary.
- `cross_platform_windows_help_file_url_failure` — PR #368 Cross-Platform Development Environment run `32397252714` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/32397252714/attempts/1)), local workflow identifier `.github/workflows/cross-platform-development.yml`, event `pull_request`, exact SHA `a81ee0ca45d0bc7d45a5fcf21be8f137d9c472d5`, attempt 1, was created `2026-08-20T17:22:35Z`, updated `17:26:45Z`, and concluded `failure`. Windows job index 0 / database ID `96516854598` ran `17:22:38Z`–`17:26:03Z`; steps 0–8 succeeded, Test step index 9 / number 10 failed, Build and Setup Node post-steps were expected downstream skips, and post-steps 12–13 / numbers 22–23 succeeded. macOS job index 1 / database ID `96516854899` completed successfully after the Windows failure. Annotation collection was complete with one failure annotation at line 1536 whose title was absent and message/path were present; terminal metadata, log retrieval, and identity evidence were complete, and the only fixed signature was `github_error` once at line 1789. Retry, flaky, timeout, transport-reset, browser-console, and development-server full-reload signatures were zero. A secure in-memory diagnostic mapped the failure only to source lines 943 and 965: the help contract passed a file URL pathname directly to the Node process, leaving a drive-prefixed path invalid on Windows. This is a Windows-only test path-conversion defect, not an auditor behavior, file-write containment, dependency/toolchain, npm, build, application, or macOS defect. The correction converts that file URL with `fileURLToPath` and expands the existing cross-platform file-URL source contract to include this test file. The immutable failed first attempt remains preserved; replacement-SHA Windows/macOS, Public, and Storybook first attempts are pending, and the correction is absent from `dev` and `main`. Remaining risk is another unguarded file-URL pathname in a test boundary, so the mandatory Windows x64 matrix remains the acceptance boundary.
- `cross_platform_windows_schema_contract_file_url_failure` — PR #368 Cross-Platform Development Environment run `32399056351` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/32399056351/attempts/1)), local workflow identifier `.github/workflows/cross-platform-development.yml`, event `pull_request`, exact SHA `626c142c92cf2db930e696c6afa794ecf822b537`, attempt 1, was created `2026-08-20T17:42:01Z`, updated `17:46:09Z`, and concluded `failure`. macOS job index 0 / database ID `96522625285` completed every step successfully. Windows job index 1 / database ID `96522625689` ran `17:42:05Z`–`17:45:17Z`; steps 0–8 succeeded, Test step index 9 / number 10 failed, Build and Setup Node post-steps were expected downstream skips, and post-steps 12–13 / numbers 22–23 succeeded. Annotation collection was complete with one failure annotation at line 1478 whose title was absent and message/path were present; terminal metadata, identity, and log retrieval were complete, and the only fixed signature was `github_error` once at line 3588. Retry, flaky, timeout, transport-reset, browser-console, and development-server full-reload signatures were zero. A secure local-test correlation mapped the failure to all contract cases in two SQL contract test files; both computed the repository root from a file URL pathname, which becomes a drive-prefixed invalid path on Windows. This is a Windows-only test path-conversion defect, not a schema/migration contract, auditor, dependency/toolchain, npm, build, application, or macOS defect. The correction converts both test roots with `fileURLToPath` and adds both files to the cross-platform file-URL source contract. The immutable failed first attempt remains preserved; replacement-SHA Windows/macOS, Public, and Storybook first attempts are pending, and the correction is absent from `dev` and `main`. Remaining risk is an unlisted direct file-URL pathname test boundary, so the mandatory Windows x64 matrix remains the acceptance boundary.
- `public_typecheck_failure` — 7 and `public_unit_regression` — 9. These are product/test contract failures, not infrastructure noise.
- `typecheck_internal_retry` — the previous `typecheck:ci` wrapper retried once after a nonzero compiler result and could have turned a failed first invocation green. A retained-log re-search found its marker in 7 available abnormal runs (`29826051322`, `29829199266`, `29829836787`, `29830160392`, `29833137979`, `29833536346`, `29833894244`) and in 0 of all 1,755 available successful logs; one known typecheck failure (`29825302827`) has no available log. #360 removes the retry entirely and adds a fail-closed source contract. Any required wrapper that exits from a later successful invocation instead of preserving the first failure reopens this signature.
- `checkout_internal_retry` — successful runs `26329677499`, `26330714822`, `26332867420`. Checkout emitted `##[error]` and recovered internally. Always inspect error annotations and raw logs even on green.
- Historical Node action deprecation affected 1,628 successes and the unused `fileURLToPath` lint warning affected 360. Both disappeared after runtime/workflow cleanup; any recurrence reopens the signature.
- `passed_test_description_audit_false_positive` — PR #361 run `31726987495` produced three `timeout` and one `deprecation` classifier hits solely because passed test names described those behaviors. PR #387 Public Readiness run `32552496853` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/32552496853/attempts/1)), exact SHA `8707dba6df9e1f34b5ebf00e9f4e8346e24ef7cc`, ran `2026-08-22T04:44:34Z`–`04:55:17Z` and concluded `success`; its high-risk plan executed classification, Quick verification, the full Production build/E2E step, and the final policy gate successfully, while the critical-smoke step was the expected skip. Annotation, terminal, identity, and log collection were complete with no annotations or failed jobs/steps. The old auditor reported `visual_drift` once at fixed line 1997 and two reviewed `next_dev_full_reload` signals at fixed lines 2566/2568. A bounded in-memory classification retained no provider text and proved that line 1997 had a passed-result prefix plus visual-baseline wording, with no failed-result prefix, screenshot-comparison failure, pixelmatch, GitHub error/warning, or adjacent explicit failure marker. This is an audit-classifier false positive, not visual drift, Storybook execution, product, build, E2E, or provider failure. The auditor now suppresses timeout, deprecation, and visual-baseline wording only when the same normalized line has a proven TAP/list passed-result prefix; actual screenshot-comparison/pixelmatch diagnostics and every explicit GitHub error/warning, retry, failure, provider, or transport signal remain reportable. Synthetic regressions cover both the passed description and a real screenshot-comparison diagnostic. A corrected local re-audit of the immutable attempt completed with no annotations or failed jobs/steps and only the same two reviewed full-reload signals. Rollout of the classifier correction requires a new PR SHA before merge.

### Preview migrations, data, and Storage

- `privileged_workflow_destination_or_revision_drift` — #360 security review. Any credential-bearing workflow URL must equal the repository-pinned Preview origin, and every privileged workflow must execute the reviewed event/input SHA rather than a floating branch. Database mutations must recheck runner `HEAD` and the current live ref immediately before apply.
- `private_storage_path_ci_logging` — #360 security review. Production Storage object paths are private evidence. Retry/failure output must use fixed operation labels, numeric status, and code-presence only; raw provider messages, provider code values, and `${objectPath}` interpolation in CI diagnostics are forbidden.
- `preview_sync_workflow_run_concurrency_cancel` — 51 cancellations. Only eligible successful `dev` push completions may use the shared queue; no-op events need a unique run-ID group.
- `preview_sync_manual_or_push_cancel` — 42 cancellations. Minimize operator dispatches and preserve one deliberate mutation at a time.
- `superseded_ci_concurrency_cancel` — 43 Public/lockfile/Storybook cancellations. These can be expected after a newer SHA, but they remain recorded and must not substitute for clean evidence on the final SHA.
- `preview_sync_remote_migration_apply_failure` — 23; standalone Preview migration apply failure — 2; legacy apply failure — 7; missing relation drift — 3; workflow/tooling failure — 4. Verify ordered local history, remote ledger parity, schema-first sequencing, and exact CLI/runtime versions.
- `preview_sync_data_storage_sync_failure` — 18 failures. Inspect the exact table/bucket stage, preserve sanitization, and require post-sync migration parity.
- `storage_provider_retry_recovered` — 7 successful runs: `26331599944` (504), `28006519761` (502), `29004176142`, `29426647566`, `29983736113`, `30180865391` (504), and post-freeze run `31770526425` (504). Recovery is useful but still abnormal evidence; record the provider failure and verify final parity. Run `31770526425` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/31770526425/attempts/1)) used local workflow identifier `.github/workflows/preview-sync.yml`, event `workflow_run`, default-branch definition SHA `3c2528e2709a658a3ddc884ec1a88715ea994724`, and checked-out target SHA `c80e8ec8843573ee0f30f87da062fdcd79426bc8`; it ran from `2026-08-14T04:39:10Z` through `05:04:10Z` and concluded `success`. Job index 0 and every executed step succeeded; step index 2 / number 3 was the expected manual-ref guard skip, so no failed or unexpectedly skipped job/step remained. Annotation collection was complete with zero annotations. Step index 11 / number 12 emitted `storage_retry` once at fixed line 963: the reviewed `upload_object` operation received numeric status 504 on attempt 1 of 3 at `04:49:01.7703425Z`, waited 750 ms, and reported provider-code presence false. That retry diagnostic itself contained no provider message, code value, private object path, or member identifier; the complete run's separate credential-seed identifier line is recorded under `preview_member_identifier_log` below, so no run-wide PII-free claim is made. The existing bounded retry recovered, after which 168 migrations, 84 replaced tables, seven Storage buckets, 712/712 credential-material removals, and both pre/post database parity checks completed; object/bucket/cleanup skips, required-Storage failure, database fallback, and Preview-unavailable skip were zero. This proves an external-provider transient rather than a product, data, schema, credential-sanitization, or migration defect. The repository-controlled prevention remains bounded retry plus fixed structural diagnostics and the fail-closed contracts in `tests/preview-sync-storage.test.mts`; the reusable skill now explicitly requires ledger extension even when final parity succeeds. Rollout: learned on `docs/360-preview-sync-retry-ledger`; replacement PR, `dev`, independently triggered sync evidence, and `main` remain pending. Residual risk is another provider transient; the original run remains immutable and was not rerun, cancelled, or deleted.
- `preview_member_identifier_log` — the same Preview Sync `31770526425` emitted one legacy credential-seed success line containing a configured Preview member identifier at fixed line 957. The raw value was neither retained nor copied. Metadata, workflow identifier, SHA binding, job/step conclusions, timestamps, zero annotations, and collection completeness are the same as the immutable `storage_provider_retry_recovered` entry above; the line occurred in successful step index 11 / number 12, and the new structural auditor reports only signature ID `preview_member_identifier_log`, count 1, and its fixed line. Classification is repository-controlled privacy logging: the successful credential seed interpolated a username even though no password, hash, salt, token, or Production credential material was logged. Product authentication, credential sanitization, database parity, and provider retry handling are rejected alternatives because the seed completed and 712/712 Production credential rows remained stripped. Prevention removes the interpolation, emits one fixed non-identifying outcome, adds a source regression in `tests/preview-credential-seed.test.mts`, retains the auditor signature for future recurrence, and codifies the rule in the skill. Rollout: fixed on `docs/360-preview-sync-retry-ledger`; PR, `dev`, an independently triggered post-fix sync, and `main` remain pending. The historical log is immutable and must never be quoted; remaining risk is identifier-bearing text in another unclassified sync diagnostic.
- `storage_provider_retry_recovered` (continued) — Preview Sync `32407210125` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/32407210125/attempts/1)), a `workflow_run` using default-branch definition SHA `340e055931171d9b6fde80007a047a77cc8420c0` and current `dev` target SHA `0cf96690ebe51f69e489ffd73d8fc0f86f60eaf9`, ran from `2026-08-20T19:10:16Z` through `19:53:45Z` and concluded `success`. Its single job and all required steps—including stale-SHA validation, data/Storage synchronization, and post-sync migration verification—succeeded; the dispatch-only checkout was the expected skip. Annotation, identity, terminal, and log collection were complete. The structural audit recorded `storage_retry` count 2 at fixed lines 769 and 770 in the data/Storage step and no other classified signature. No provider message, code value, object path, member identifier, password material, token, or raw log content was retained. Because data/Storage synchronization and migration verification finished successfully, this is an external provider transient rather than a Preview availability, product, schema, migration, or credential-sanitization fault. The bounded retry and its fail-closed Storage contracts remain the preventive control. Rollout: `dev` has Preview parity with recovered-provider evidence, not a clean first attempt; `main` promotion remains blocked until an independently triggered Preview Sync completes without warning or retry. The immutable run was not rerun, cancelled, or deleted.
- `storage_skip_green` — `29896920643` (400), `30698107430` (520). An object was skipped under an overall success. Any `Skipping object`, `Skipping bucket`, or skipped cleanup is degraded and blocks clean parity unless an explicit, reviewed optional-bucket contract says otherwise.
- `preview_database_fallback_green` — 8 successful runs: `26330465086`, `27111529103`, `27813814301`, `27817340422`, `27819595233`, `27819775977`, `31451615513`, `31454056487`. A missing tenant/user error used fallback. An intended fallback must be structurally reported and audited, never silently accepted as clean.

### Persistent successful-log noise

- npm `allow-scripts` warnings affected 641 successes. #360 does not rely on that advisory allow-list as the execution boundary: every Actions and Vercel install uses `npm run install:trusted` and `npm ci --ignore-scripts --allow-git=none` with all lifecycle packages denied. It verifies the reviewed local archiver source, registry identities, common deployment-platform esbuild integrities, and directly executes only the installed pinned esbuild binary—not its installer. Static policy, root/workspace lifecycle, effective lifecycle/Git controls, registry identity, integrity drift, and unreviewed non-registry sources fail before installation. This is not effective until merged to `dev`, then `main`.
- `dependency_prepare_before_policy` — #360's security review confirmed that native allow-script preflight alone can be too late for Git/directory preparation. The trusted path does not rely on that advisory control: it requires npm 11.12.1 or newer, enforces `ignore-scripts=true` in the project file, scrubbed child environment, effective config, and CLI arguments, sets `allow-git=none`, rejects arbitrary Git/URL/file sources, verifies the sole reviewed local package byte-for-byte, and never invokes an npm lifecycle installer. Any raw Actions/Vercel `npm ci`/`npm install`, root/workspace lifecycle, unreviewed non-registry source, or explicit installer reopens this merge-blocking signature.
- `secret_bearing_dependency_install` — the frozen audit did not expose a credential leak, but #360's threat-model review found Preview Sync and Admin Performance made service-role, database, session, gateway, and login credentials job-wide before dependency installation. Checkout also persisted the Actions token, and Vercel exposes build variables to custom install processes. #360 moves Actions secrets to exact post-install steps, disables checkout credential persistence, launches install children from a narrow environment allowlist, and starts the Vercel install command under `env -i`. Later Vercel build code still sees build-time variables and remains a separate supply-chain boundary. Any future job-level secret, persisted checkout credential, or unsanitized install environment combined with dependency installation reopens this merge-blocking signature.
- `audit_raw_text_persistence` — #360's security review proved that regex-redacted Action logs and annotations could retain synthetic Wallet-key, Mattermost-key, admin-cookie, quoted authorization, and future secret-name forms. The auditor now persists no GitHub-provided text at all: only fixed signature IDs/counts/line numbers, numeric indices, validated metadata, and field-presence booleans. It requires an explicit attempt, uses an attempt-specific URL, records incomplete log/annotation retrieval as non-clean, emits fixed error text only, strips ANSI before classification, and creates a new mode-0600 direct-child file without overwrite or symlink traversal. Any durable raw or “sanitized” log/annotation/name/path field, silent retrieval failure, mutable latest-attempt audit, or overwrite-capable output reopens this merge-blocking privacy signature.
- `raw_audit_artifact_permissions` — during #360 the full-history temporary corpus was found with a mode-0755 root and 12,885 mode-0644 files. No content was printed; the root was restricted to 0700 and every file to 0600, then fully rechecked. A full-census download must start under `umask 077`, create a 0700 root, produce 0600 files, and verify every mode immediately. Any broader mode blocks further inspection and remote rollout until contained.
- Informational positive full-tree audit summaries appeared in 746 successes. The Production dependency audit remains the blocking source of truth.
- `e2e_mock_persistence_diagnostics` — E2E mock logging emitted `log_insert_failed`/`ingest_exception` in 404 retained successes. PR #361 run `31726987495` reproduced 164 persistence-only diagnostics because the mock web server intentionally has no Supabase service credentials while the telemetry boundary still tried the real admin client. Replacement run `31728947732`, exact SHA `d5472bce998bbd7f46fc8732bab12bc36ed51ede`, still concluded `success` but its structural audit found 172 aggregate `mock_server_error` signatures because it predated the bypass patch. Run `31731125230`, exact SHA `dd49d3db877f2e005843098df1b049f141d11315`, retained complete all-success evidence and removed every persistence-specific `log_insert_failed`/`ingest_exception` match, proving the environment-scoped guard works remotely. The guard returns success without opening the client only when all three reviewed conditions hold: non-Production runtime, `E2E_MOCK_MUTATIONS=1`, and mock data source. Production, Supabase, and ordinary test paths retain persistence and error reporting. New messages or non-mock occurrences remain abnormal. Rollout: fixed on the feature PR; pending `dev` and `main`.
- `e2e_navigation_teardown_reset` — network `ECONNRESET`/`aborted`/`uncaughtException` appeared in 415 retained successes. In PR #361 run `31726987495`, a transport cluster followed the successful administrator query-state GET navigation: the URL assertion passed before the streamed response settled, then test teardown closed it. Replacement run `31728947732` still carried the cluster because it predated the URL-specific wait. Run `31731125230` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/31731125230/attempts/1)), exact SHA `dd49d3db877f2e005843098df1b049f141d11315`, completed every job/step with no annotations and 86 retry-free Playwright passes, but its structural audit retained eight transport matches: four `aborted`, four `ECONNRESET`, and two overlapping `uncaughtException`, between passed test ordinals 82/83 and 85/86. Run `31735631329` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/31735631329/attempts/1)), exact SHA `404b3ecef8ae26cf20b6a58a434100d04a925456`, reduced the signature to three `aborted`, three `ECONNRESET`, and two overlapping `uncaughtException`; the 82/83 cluster disappeared and every remaining line occurred between 85/86. This shows the targeted waits removed the first observed cluster and strongly implicates repeated navigation inside the responsive viewport loop in the remaining cluster. The correction navigates exactly once before that loop, changes viewport sizes on the loaded page, and retains the targeted final wait. A source contract enforces the single navigation and continues to reject the broad `afterEach`. Five retry-disabled independent fresh-server executions of the responsive case, one full administrator suite, and the complete pre-push gate with all 86 E2E cases passed locally without the transport signature. Corrective SHA `02bc28b96fa5ddf3c08f5a113a088711037f7237` then supplied clean first-attempt Public evidence in run `31767060216`: 86/86 Playwright passes and zero transport, persistence, retry, flaky, timeout, or warning signatures. Do not suppress this signature globally because the same transport terms outside this reviewed lifecycle boundary remain abnormal; `dev`/`main` rollout remains pending.
- `e2e_page_smoke_teardown_reset` — the pre-merge local run on `0d6562fc15a387af0e977274ebe981c780ecf28c` exposed one `aborted`/`ECONNRESET` pair between adjacent public smoke cases even though all 86 tests passed. A terminal per-helper `networkidle` moved rather than eliminated the signal in the full page-smoke suite, proving that late work could begin after the load-state event. The root PWA effect registered a service worker in every short-lived E2E mock browser context; that lifecycle is unnecessary outside a Production, non-mock runtime and can outlive the visible route assertions. Prevention is the fail-closed terminal idle plus an environment-scoped service-worker guard and a source contract that forbids suite-wide cleanup. Three consecutive independent full page-smoke executions passed 58/58 with zero transport signatures after the combined correction, followed by a clean complete prepush with 86/86 retry-free E2E passes and zero transport signatures. Corrective SHA `02bc28b96fa5ddf3c08f5a113a088711037f7237` supplied clean first-attempt Public evidence in run `31767060216`, including zero transport signatures across all 86 Playwright cases. This signature is not a reviewed warning baseline and cannot be suppressed; `dev`/`main` rollout remains pending.
- `e2e_page_smoke_networkidle_timeout` — PR #368 Public Readiness run `32389619450` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/32389619450/attempts/1)), local workflow identifier `.github/workflows/public-readiness.yml`, event `pull_request`, exact SHA `d83c467952129019f9215e04a07c24377a971ad6`, was created `2026-08-20T16:01:34Z`, updated `16:11:04Z`, and concluded `failure`. Its sole job index 0 / database ID `96492310986` ran `16:01:37Z`–`16:11:02Z`; steps 0–11 succeeded, E2E step index 12 / number 13 failed, Setup Node post-step index 13 / number 25 was the expected downstream skip, and post-steps 14–15 / numbers 26–27 succeeded. Annotation collection was complete with one failure annotation at line 137 whose title was absent and message/path were present. Identity, terminal, and log evidence were complete. Fixed signatures were `failed_attempt_marker` 1 at line 1871, `test_failure` 1 at 1963, `github_error` 1 at 1966, `timeout` 2 at 1935/1937, and `next_dev_full_reload` 2 at 1925/1927; retry, flaky, transport-reset, browser-console, and React-act signatures were zero. The failed public smoke route had completed its response, expected-text, and Next-error assertions before the generic terminal `networkidle` wait exhausted the test budget. The two full-reload diagnostics followed the failed test and therefore are not the reviewed between-passing-test baseline. This is a test/readiness-boundary defect, not an application, routing, dependency, installation, schema, transport, or Storybook defect. Five independent fresh-server repetitions of that route passed, but do not replace the immutable remote first attempt. The correction replaces generic terminal `networkidle` waits in both smoke helpers with explicit document `load` completion after semantic assertions, and the source contract rejects reintroducing generic smoke `networkidle` or suite-wide cleanup. Replacement-SHA Public, Storybook, and cross-platform first attempts remain pending; the correction is absent from `dev` and `main`. Remaining risk is another non-finite generic readiness wait, so the complete retry-free E2E gate stays mandatory.
- `e2e_mock_reset_socket_hangup` — PR #368 Public Readiness run `32399056465` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/32399056465/attempts/1)), local workflow identifier `.github/workflows/public-readiness.yml`, event `pull_request`, exact SHA `626c142c92cf2db930e696c6afa794ecf822b537`, attempt 1, was created `2026-08-20T17:42:01Z`, updated `17:52:06Z`, and concluded `failure`. Its sole job index 0 / database ID `96522625555` ran `17:42:05Z`–`17:52:01Z`; steps 0–11 succeeded, E2E step index 12 / number 13 failed, Setup Node post-step index 13 / number 25 was the expected downstream skip, and post-steps 14–15 / numbers 26–27 succeeded. Annotation collection was complete with one failure annotation at line 140 whose title was absent and message/path were present; terminal metadata, identity, and log retrieval were complete. Fixed signatures were `failed_attempt_marker` 1 at line 1856, `test_failure` 1 at 1972, `github_error` 1 at 1975, and `next_dev_full_reload` 2 at 1931/1933; retry, flaky, elapsed timeout, reset, refusal, browser-console, and React-act signatures were zero. A secure in-memory diagnostic located the failure at the first authentication-suite mock-reset request and classified a socket-hangup transport error before any test assertion; no HTTP 4xx/5xx response, connection refusal, retry, or application assertion failure was present. The server readiness guard had only proved the root web server was listening, not that the authentication route had loaded before an independent mock-mutation request. This is an E2E readiness-boundary defect, not member reset behavior, product authentication, database/schema, install policy, dependency, Storybook, or Vercel deployment failure. The correction warms the login route and waits for its rendered credential control before posting the mock reset; a source contract enforces that ordering. The immutable failed first attempt remains preserved; replacement-SHA Public, Windows/macOS, and Storybook first attempts are pending, and the correction is absent from `dev` and `main`. Remaining risk is a new server-lifecycle transport signal in another mock request boundary, so independent fresh-server repetitions and the complete retry-free E2E gate remain mandatory.
- `local_e2e_runner_teardown_stall` — one local `npm run ci:local` execution on the uncommitted PR #368 replacement completed all 86 retry-free Playwright assertions, including only the two reviewed administrator-between-test full-reload diagnostics, but did not return from the Playwright web-server teardown after more than two additional minutes. It was deliberately interrupted before Storybook began; no remote command, commit, push, deployment, or GitHub Actions run occurred. Process inspection found an idle Playwright CLI with its local dev-server child still alive, but no test worker, assertion, timeout, transport, reset, refusal, browser-console, or runtime-error signature. This local execution is preserved as abnormal and is not validation evidence. After the controlled shutdown, a fresh standalone `npm run test:e2e:ci` on the same worktree and source exited 0 with 86/86 retry-free passes and the same two reviewed diagnostics. The transient was therefore classified as an isolated local runner-teardown stall rather than a member-reset, authentication, browser assertion, dependency, build, or application defect; the next full gate must still complete with a normal exit before remote mutation.
- `local_git_credential_store_stall` — the first normal push attempt for local PR #368 replacement `08fc6a22c730135ce061705ce4d710e61dba32eb` did not reach GitHub. After credential lookup succeeded, the macOS Keychain credential-store helper remained blocked while the Git transport was still waiting for remote refs; the coordinator interrupted it and confirmed that the remote feature branch remained at `626c142c92cf2db930e696c6afa794ecf822b537`. No GitHub Actions run, PR head update, deployment, Preview mutation, or Production mutation was created. This is a local credential-helper/OS integration stall, not an application, dependency, schema, test, CI, Vercel, or remote-provider failure. The next attempt must use the already authenticated GitHub CLI credential source without invoking Keychain storage, preserve noninteractive behavior, and re-check the exact remote ref before monitoring first-attempt CI. The interrupted attempt is not remote evidence and is not retried; the replacement SHA remains local until a single successful push starts its first attempts.

- `local_git_credential_store_stall` (continued) — The first push attempt for local ledger commit `f3b2b7d4ffbe7f7071dcb55d20e489d12df8cce6` emitted one macOS Keychain credential-store OSStatus `-25308` before the global ECC pre-push hook began. The coordinator immediately stopped both active local pre-push processes and the exact `git push` process before receive-pack. Live ref inspection found no remote `docs/380-preview-storage-retry-ledger` branch, and the candidate SHA had no GitHub Actions check record, workflow run, Vercel deployment, Preview mutation, or Production mutation. This is a local noninteractive Keychain-store interaction failure compounded by an overlapping local gate process, not an application, ledger, Storage retry, test, Node package, schema, GitHub, Vercel, or remote transport defect. The existing `osxkeychain` configuration is rejected for the next attempt: use the already authenticated GitHub CLI credential helper only through per-command configuration, keep the repository-pinned Node 24.18.1/npm 11.16.0 directory first on `PATH` for both the ECC hook and repository gate, and prove the branch remains absent before one new push. The repository's existing lockfile/runtime contract and the focused Preview Storage contracts remain the executable protections; no source behavior is changed by this operator-environment record. Rollout: this extension is local on `docs/380-preview-storage-retry-ledger`, the original unpushed commit is preserved as local evidence, `dev` remains `75e3a7dd55f23b6dee9c126833626a0de27ae9b1`, and `main` remains untouched. Remaining risk is another workstation credential or hook-runtime mismatch; any recurrence must stop before receive-pack and extend this entry rather than be retried silently.
- `local_git_cli_helper_path_omission` — A noninteractive push attempt for exact local PR #426 corrective SHA `5bf39084078d9eba04a59d4b8019c96e7faf1ae9` stopped at `2026-08-31T08:38:17Z` before any pre-push hook or `receive-pack`. The command correctly removed Keychain credential helpers and pinned Node `24.18.1`, but its replacement `PATH` omitted the installed GitHub CLI at `/opt/homebrew/bin/gh`, so the explicit `gh auth git-credential` helper could not start. Immediate verification proved the remote feature ref remained `484dc49bd76dcaf79b2fd9a7c25897fd63e05226`, GitHub had no commit object or check run for `5bf39084`, and no Actions run, PR-head update, Vercel deployment, Supabase Preview check, Preview mutation, or Production mutation was created. Classification is an operator command-environment defect, not a credential rejection, repository hook, source, test, schema, GitHub, Vercel, or Supabase failure. The next attempt must retain the canonical Node directory while including `/opt/homebrew/bin`, keep `GIT_TERMINAL_PROMPT=0`, and use only the explicit GitHub CLI credential helper. The original local SHA is preserved as evidence but will not be pushed; a ledger-only corrective commit becomes the replacement SHA. Remaining risk is another over-constrained command environment, so the remote ref and absence of active shared mutations must be rechecked immediately before that single replacement attempt.
- `local_ecc_prepush_node_engine_mismatch` — the subsequent safe GitHub-CLI credential push attempt for local PR #368 head `d31311bc` also stopped before `receive-pack`. The Codex-installed ECC pre-push hook inherited the workstation's default Node 26.5.0/npm 11.17.0 instead of the repository-pinned Node 24.18.1/npm 11.16.0 runtime. Its preliminary lint, test, and build sections completed, but the Node-26 test invocation emitted one module-registration deprecation diagnostic; the repository's own pre-push gate then failed closed at `check:lockfile` on the incompatible Node major version. No remote transfer occurred, and the coordinator immediately proved the feature ref still equals `626c142c92cf2db930e696c6afa794ecf822b537`; no GitHub Actions run, PR head update, deployment, Preview mutation, or Production mutation exists for `d31311bc`. This is a local shell/runtime-path mismatch and an external hook composition issue, not an application, test assertion, dependency, schema, security, Vercel, GitHub, or credential-source regression. The test alias loader already uses the supported `registerHooks` API, and the repository's executable `check:lockfile` boundary is the required fail-closed regression contract. The next replacement SHA must preserve that record, invoke Git with the canonical Node 24.18.1/npm 11.16.0 directory first on `PATH` so both hook layers resolve the pinned runtime, use only the GitHub CLI credential source, and prove the remote ref remains unchanged before the single next attempt. `dev` and `main` remain unchanged; remaining risk is a future shell or hook that resolves an unpinned Node binary.
- `local_e2e_generated_route_type_contamination` — the first local release attempt for Issue #384 on base `192c6490386833a13c735391c329384c5283eff7` stopped before commit and before `receive-pack` at `2026-08-22T11:54:44+09:00`. A complete local `verify:release` had already passed the 103-route Production build and all 86 retry-free E2E cases. The release script then began its required Quick Gate; install policy, cross-platform policy, lockfile, 169 migrations, and lint passed, but semantic typecheck read a partially written ignored `.next-e2e/dev/types/routes.d.ts` through the ignored `next-env.d.ts` import and failed with parser errors. Live inspection proved that `next-env.d.ts` had been rewritten to import the E2E development-server route output, whose partial file began mid-route map after an otherwise complete declaration block. The remote feature branch remained absent, local `HEAD` stayed at the original `dev` base, and no commit, Actions run, deployment, Preview mutation, or Production mutation was created. This is a repository-controlled gate-isolation defect caused by generated E2E shutdown output contaminating a later semantic check, not an application type error, lock drift, migration, dependency, GitHub, Vercel, database, or credential failure. The correction binds `typecheck:ci` to tracked `tsconfig.typecheck.json`, excludes ignored `next-env.d.ts`, `.next`, and `.next-e2e`, and adds an executable source/config contract. Acceptance requires the corrected typecheck to pass while the malformed generated artifact remains present, followed by a fresh complete Quick and Release Gate before the first remote commit/push. Remaining risk is another generated framework directory entering a tracked typecheck config; the dedicated include/exclude contract is the prevention boundary.
- `local_release_color_and_visual_server_reuse` — A local release attempt for issue #378 was deliberately interrupted before commit or `receive-pack` after its visual-test child emitted Node color-environment warnings: the desktop task shell supplied `NO_COLOR=1` while the visual runner enabled forced color. A first color-scrubbed repeat then began while the interrupted release's Storybook process was still winding down; it reused that default-port server, passed its first 28 visual scenarios, and then lost the reused server. These are local operator-environment/process-overlap conditions, not a source, snapshot, browser assertion, Storybook, dependency, Vercel, GitHub Actions, Preview, or Production defect. No remote branch, commit, deployment, workflow run, Preview mutation, or Production mutation was created. Prevention: invoke the release with `NO_COLOR` removed, wait until the interrupted visual server has fully exited, and prove the visual baseline on an unused explicit port before retrying the release. A fresh isolated-port visual run with `NO_COLOR` removed passed all 136 scenarios without warnings, retries, or baseline changes; the next release must repeat the same color-environment condition and complete normally before any push.
- `e2e_admin_global_networkidle_timeout` — PR #362 run `31762781595` failed three otherwise assertion-clean administrator cases because global `networkidle` never arrived before the 30-second test timeout; two adjacent full reload diagnostics followed the failed cases. Admin development compilation, hydration, and intent-prefetch lifecycles are not equivalent to UI unreadiness. Prevention is the explicit admin-shell hydration marker plus route-specific URL/control assertions, with raw admin `networkidle`, ignored timeout catches, broad `afterEach` drains, retries, and timeout inflation forbidden by an executable source contract. Any future wait must identify the exact finite response or state it proves.
- `next_dev_fast_refresh_full_reload` — the complete 1,755-log successful-run corpus contained this exact Next development-server warning 635 times across 360 Public Readiness runs, from `29159523466` (`2026-07-11T16:17:51Z`) through `31709729054` (`2026-08-13T14:21:15Z`). Per-run counts were 1 in 130 runs, 2 in 189, 3 in 37, and 4 in 4. The existing structural auditor did not classify it; #360 adds `next_dev_full_reload`. Public run `31735631329` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/31735631329/attempts/1)), exact SHA `404b3ecef8ae26cf20b6a58a434100d04a925456`, emitted two `next_dev_full_reload` matches. That count is within the retained historical distribution, but the same success log also retained teardown transport errors, so this run cannot be treated as a reviewed non-blocking baseline. It is a nonfatal development-server lifecycle diagnostic only when it appears between otherwise retry-free passing E2E cases without a browser/server error. A new context, preceding runtime error, or unexplained count change is abnormal and must be investigated before another trigger.
- `next_dev_fast_refresh_full_reload` (continued) — Public Readiness `32431646330` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/32431646330/attempts/1)), `push` event, exact `dev` SHA `29b97356f6a50032a76ed9b2966ff4f14dc123af`, ran from `2026-08-21T00:10:38Z` through `00:19:29Z` and concluded `success`. Its sole job and every step succeeded; annotation, identity, terminal, and log collection were complete and empty. The structural audit recorded `next_dev_full_reload` count 3 at fixed lines 1838, 1921, and 1923, with no retry, flaky, failure, timeout, transport, persistence, provider, or browser-console signature. Two signals retained the known between-passing-test context, while the third occurred before the first passing E2E case; it is therefore a changed count and a new lifecycle context, not the reviewed two-signal baseline. The full assertion result still passed, so this is not evidence of a product assertion, dependency, schema, installation, or browser-test failure; its precise Next development-server cause remains unproven. The existing structural classifier plus retry-disabled 86-case E2E gate remain the fail-closed detection boundary. Do not rerun this immutable attempt or promote from it. Record this evidence, then require a new independently triggered SHA whose first attempt returns to the reviewed context and count before `main` promotion. Rollout: observed on `dev`; documentation correction is local on `docs/369-fast-refresh-ledger`; `main` remains blocked. Residual risk is another pre-first-test development-server reload.
- `preview_sync_circular_fk_dump_warning` — Preview Sync `32432214185` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/32432214185/attempts/1)), local workflow `.github/workflows/preview-sync.yml`, `workflow_run` event triggered by the exact `dev` SHA `29b97356f6a50032a76ed9b2966ff4f14dc123af`, ran from `2026-08-21T00:19:31Z` through `00:51:51Z` and concluded `success`. The sole job `96626034293` and all executable steps succeeded; the selected-ref checkout was the workflow's expected skip, annotations were empty with complete collection, and terminal, identity, and log retrieval were complete. The structural auditor found no retry, Storage skip, Storage retry, database fallback, unavailable-Preview, provider, or browser signature. A bounded log inspection found three `pg_dump` circular-foreign-key warning blocks during the data-only export, each with two detail records and two restore-guidance records. Preview data replacement, Storage synchronization, and post-sync migration verification still completed, so the warning is not a failed restore or a password-reset migration failure; however, it violates the #369 requirement that Preview Sync be warning-free and must not be relabeled as clean. The pinned Supabase CLI data-dump surface does not expose the required `pg_dump --disable-triggers` option, while the invoked CLI implementation already executes its database client in the Supabase Postgres container. The correction must use that pinned client image with an explicit `--disable-triggers` data-only export, retain the current public-table exclusions and credential sanitization, and add an executable command-contract test. Do not rerun this immutable attempt. A new independently triggered `dev` SHA must complete Preview Sync without the warning before `main` promotion. Rollout: observed on `dev`; fix and ledger are local; `main` remains blocked. Residual risk is container-client parity or a newly surfaced dump diagnostic.
- `preview_sync_postgres_client_entrypoint_failure` — Preview Sync `32437455597` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/32437455597/attempts/1)), local workflow `.github/workflows/preview-sync.yml`, `workflow_run` event, ran from `2026-08-21T01:46:02Z` through `01:48:11Z` and concluded `failure`. The default-branch workflow definition remained at `340e055931171d9b6fde80007a047a77cc8420c0`; it was triggered only after the successful `dev` push Public Readiness run `32436980970` for exact target SHA `162ce60ee2aa48e0d4fa1e0e91837e95aa2d6c67`. Its sole job index 0 / database ID `96641437581` ran `01:46:05Z`–`01:48:10Z`. Steps 0–10 succeeded, including exact target checkout, stale-SHA guard, Preview preflight, pre-sync migration apply, and credential-sanitized setup; data/Storage synchronization step index 11 / number 12 failed, post-sync migration verification step index 12 / number 13 was the expected downstream skip, and post-setup/checkout steps completed or skipped as expected. Annotation collection was complete with one failure annotation at line 221 whose title was absent and message/path were present. Identity, terminal, and log evidence were complete; fixed signatures were `docker_registry` once at line 479 and `github_error` once at line 679. A bounded in-memory diagnostic proved that the pinned GHCR manifest resolves and its image config declares a nonempty default entrypoint. The direct dump command had invoked `bash -c` as image arguments without overriding that server entrypoint, so the client dump shell never became the container process. This is a repository-controlled container-command defect, not a credential, Production/Preview database, password-reset migration, Storage, stale-SHA, or registry-availability failure. The correction adds `--entrypoint bash` before the pinned image and an executable plan-order regression; the focused dump and Wallet-isolation tests pass locally. Do not rerun this immutable attempt. Rollout: the failing SHA is on `dev`, the correction and ledger are local on `fix/369-preview-sync-container-entrypoint`, and `main` remains blocked. A new independently triggered `dev` SHA must complete data/Storage sync and post-sync migration verification without the prior circular-FK warning, this entrypoint failure, retries, skips, or fallback before any Wallet-excluded `main` promotion.
- `preview_sync_disable_triggers_system_permission_failure` — Preview Sync `32439710538` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/32439710538/attempts/1)), local workflow `.github/workflows/preview-sync.yml`, `workflow_run` event, ran from `2026-08-21T02:24:00Z` through `02:26:06Z` and concluded `failure`. It was triggered by the successful `dev` Public Readiness run for exact target SHA `9418b9fe83091ee02c3bf2f17936e5bc895cec6b`; the default-branch workflow definition SHA was `340e055931171d9b6fde80007a047a77cc8420c0`. Its sole job index 0 / database ID `96647768372` completed steps 0–10, including exact target checkout, stale-SHA validation, Preview preflight, and pre-sync migration apply. Data/Storage synchronization step index 11 / number 12 failed, while post-sync migration verification step index 12 / number 13 was the expected downstream skip. Annotation collection was complete with one failure annotation at line 224 whose title was absent and message/path fields were present; identity and terminal collection were complete. Fixed structural signatures were `docker_registry` once at line 479 and `github_error` once at line 682. A bounded in-memory classifier proved that the explicit container entrypoint executed and the export reached the restore phase; it found no image-pull, source-connection, network, migration, Storage, or shell failure, but found a trigger-permission failure during restore. The `pg_dump --disable-triggers` export emits public-table `DISABLE/ENABLE TRIGGER ALL` statements, and the Preview restore role rejects system-trigger control. This is a repository-controlled dump/restore compatibility defect, not an entrypoint, credential, registry, Production database, Preview availability, password-reset migration, or Storage defect. The correction retains `--disable-triggers` and the session-level constraint envelope that prevent circular-FK warnings, strips only exact public dump trigger-control statements before restore, and tests both scope preservation and script wiring. Do not rerun, cancel, or relabel this immutable attempt. Rollout: the prior entrypoint correction is on `dev`; this replacement and ledger are local on `fix/369-preview-sync-trigger-permission`; `main` remains blocked. A new independently triggered `dev` SHA must complete data/Storage synchronization and post-sync migration verification with no circular-FK warning, system-trigger permission error, retry, skip, fallback, or other unreviewed diagnostic before any Wallet-excluded promotion.
- `preview_sync_circular_fk_dump_warning` (continued) — Preview Sync `32442250513` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/32442250513/attempts/1)), local workflow `.github/workflows/preview-sync.yml`, `workflow_run` event, was created `2026-08-21T03:07:22Z` and updated `03:40:54Z` with a `success` conclusion. GitHub reported the default-branch definition SHA `340e055931171d9b6fde80007a047a77cc8420c0`; bounded execution evidence independently confirmed the exact checked-out target was `dev` SHA `1664a3c503a6f20a248a3a9b3895e10bf1f8552e` and its stale-SHA guard passed. The sole job index 0 / database ID `96655121441` and all executable steps succeeded; step index 2 / number 3 was the expected workflow-run-only checkout skip, annotations were empty with complete collection, and identity, terminal, and log retrieval were complete. Data/Storage sync (step index 11 / number 12) and the post-sync migration verification (index 12 / number 13) both completed, confirming the current password-reset migration is present in Preview. Structural inspection found three circular-FK advisory blocks during the direct dump, with no retry, Storage retry/skip, database fallback, unavailable-Preview, image-pull failure, migration failure, or GitHub warning/error signature; the one `docker_registry` classification was a normal local-image acquisition path that completed before export. The prior `--disable-triggers`, session-replica envelope, and trigger-control sanitizer made restore functionally safe, but the retained advisory violates the explicit warning-free #369 gate. This is a repository-controlled log-surface defect, not a password-reset migration, restore, Storage, provider, registry, or stale-SHA failure. Prevention buffers only this direct dump stderr and suppresses a block only when its circular-FK header, at least one detail, and `--disable-triggers` hint all occur in order; any partial or unfamiliar diagnostic is printed unchanged and a nonzero dump exit remains fatal. The pure helper plus direct-dump wiring are executable regression coverage. Do not rerun this immutable attempt. Rollout: correction is local on `fix/369-preview-sync-circular-fk-warning`; a new independently triggered `dev` SHA must supply clean Preview Sync evidence before Wallet-excluded `main` promotion. Residual risk is a new dump diagnostic, which stays visible by design.
- `storybook_timeout_success_diagnostic` — Push Storybook run `32441598422` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/32441598422/attempts/1)), local workflow `.github/workflows/storybook.yml`, exact `dev` SHA `1664a3c503a6f20a248a3a9b3895e10bf1f8552e`, was created `2026-08-21T02:56:35Z`, updated `03:02:27Z`, and concluded `success`. Executed job index 0 / database ID `96653251137` and every step succeeded; job index 1 was the expected visual-dispatch guard skip, annotations were empty with complete collection, and identity, terminal, and log retrieval were complete. The structural audit found one elapsed `timeout` diagnostic at fixed line 5516, while the reviewed 41 externalization and two bundle signatures matched baseline and retry, flaky, test-failure, browser-console, GitHub warning/error, and deprecation signatures were zero. The simultaneous moving-PR Storybook run `32441601689` at the same SHA had the baseline-only signature set, and a full local Storybook interaction/a11y run completed without the timeout diagnostic. The cause is therefore an unproven push-only teardown/tooling condition, not a verified product, interaction, accessibility, dependency, or source-code fault; it is not accepted as a clean baseline. This entry preserves the original evidence without a rerun. The new correction SHA will provide the required independently triggered replacement Storybook first attempt; it must have zero timeout signature before `main` promotion. Rollout: observed on `dev`, no source suppression is authorized, and `main` remains blocked. Residual risk is recurrence in the next remote push, which requires fresh root-cause work rather than promotion.
- `storybook_timeout_success_diagnostic` (continued) — Moving PR #351 Storybook run `32446563107` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/32446563107/attempts/1)), local workflow `.github/workflows/storybook.yml`, `pull_request` event, exact merged `dev` SHA `5fbe85e2fa7b2123eae39245f1688a27c969fb4e`, was created `2026-08-21T04:20:00Z`, updated `04:25:53Z`, and concluded `success`. Executed job index 0 / database ID `96667159313` and all steps succeeded; job index 1 was the expected visual-dispatch guard skip. Annotations were empty, and terminal, identity, and full-log collection were complete. The structural audit recorded one elapsed `timeout` at fixed line 5530; reviewed browser externalization (41) and bundle-warning (2) counts were unchanged, while retry, flaky, test-failure, browser-console, React-act, GitHub warning/error, and deprecation signatures were zero. A bounded in-memory classification found one Storybook-associated timeout signal with no failure marker; it did not expose raw log text. The simultaneous push Storybook run `32446561433` at the identical SHA completed with baseline-only signatures, as did prior independent PR #374 Storybook evidence. This proves an event/concurrency-sensitive tooling or teardown condition remains unproven; it does not establish a product, Story interaction/a11y, dependency, runtime, or source-difference cause. Do not suppress the signal or rerun the immutable attempt. The structural auditor, clean same-SHA push comparison, and full local 150-file / 463-test Storybook gate remain the fail-closed evidence boundary; a root-cause correction and a new independently triggered first attempt are required before any Wallet-excluded `main` promotion. Rollout: observed after #374 merged into `dev`; Preview Sync may complete for parity evidence, but `main` remains blocked. Residual risk is recurrence specifically in concurrent moving-PR Storybook execution.
- `storybook_timeout_success_diagnostic` (root cause and containment) — Further bounded in-memory inspection of immutable run `32446563107` established the exact lifecycle: all Storybook tests completed, then Vitest logged `close timed out after 10000ms` and the Vite exit-block diagnostic. The enabled hanging-process reporter reported 40 residual `FILEHANDLE` resources with unknown stacks; there was no test, hook, job, or browser interaction timeout. The source of Vitest `4.1.10` confirms that this close-timeout path calls `process.exit()` without a nonzero code, so the direct `vitest run` package script was an acceptance defect: an unhealthy teardown could be shown as a successful Actions job. The unknown FileHandle stacks do not justify attributing the resource leak to a product component or Story. Correction #375 upgrades the matching Vitest browser package family to `4.1.11`, whose release includes browser Chromium garbage-collection remediation, and replaces the direct package script with the cross-platform `scripts/run-storybook-tests.mjs` wrapper. The wrapper invokes the repository-local Vitest CLI via `process.execPath`, forwards the unchanged output, and changes the result to nonzero when the exact close-timeout diagnostic appears. Its pure regression tests cover timeout promotion, benign text, signals, and existing nonzero exits; the cross-platform contract preserves the hanging-process reporter. On fixed Node `24.18.1`/npm `11.16.0`, lockfile validation and focused contracts passed, and two independent full wrapper runs passed all 150 Storybook files / 463 tests with no close-timeout diagnostic. This fixes false-green acceptance while retaining the diagnostic. A fresh remote PR first attempt must still be audited for zero timeout signatures before the Wallet-excluded `main` promotion can resume; do not accept a 0 exit code alone.
- `setup_node_cache_save_collision` — cache save collisions appeared in 101 retained successes, including one post-job reservation collision in PR #361 run `31726987495`. The job, steps, annotations, tests, and build were otherwise clean; this is nonfatal concurrent cache-writer contention, not a product failure. Preserve it as structural evidence. Prefer a single writer, unique key, or restore-only cache when changing the cache topology, but do not remove dependency caching merely to hide a nonfatal message if that would increase cold external-registry exposure.
- `remote_mutation_owner_bypass` — #361 was merged while a validated replacement commit existed only in its local worktree, and #359 was then merged while its exact-head audit had already found transport errors and Preview Sync `31758981252` was still active. The repository/user outcome authorized eventual integration, but it did not waive the skill's sequencing gates. Prevention is a single coordinator-designated mutation owner, mandatory live PR/ref plus all-worktree reconciliation immediately before each mutation, and an explicit prohibition on delegated monitors, auditors, diagnostics, reviewers, or cleanup workers inferring push/merge authority. Any active audit, local-only replacement, dirty branch worktree, head mismatch, or shared-state mutation revokes an earlier authorization until the coordinator re-evaluates it.
- `local_pre_receive_abnormal_output` — the first normal push attempt for replacement head `aaeedad4` never reached the remote. Its complete pre-push gate had passed every earlier section and 82 Playwright cases when the local Next server emitted an `aborted`/`ECONNRESET` transport cluster with two overlapping `uncaughtException` reports immediately after the administrator-home case and before the next administrator case. The mutation owner interrupted the hook before `receive-pack`; the remote branch remained absent, GitHub returned zero runs for that commit, and no Vercel deployment was created. A passing assertion count would not have made this output clean. Source tracing found every administrator render mounted four analytics clients whose common `trackProductEvent` opened fire-and-forget `keepalive` requests even in the non-Production mock runtime; the existing persistence bypass ran only after those requests reached the server, so it could not prevent page-close transport races. Corrective commit `cf9277755167a481036f1c2b82635978bd23bffc` prevents browser product-event transport only when both non-Production and mock-data conditions hold, while Production and Supabase-backed clients remain unchanged. A pure runtime contract and a source-order contract fail without that guard. Five independent fresh-server executions of the affected administrator-home case, one complete five-case administrator suite, and the complete pinned Node 24.18.1/npm 11.16.0 prepush passed with 1,161 Node tests, 68 unit tests, a 103-route build, and 86/86 retry-free E2E cases without transport errors. The only remaining E2E diagnostics were the two already reviewed Next development-server full reloads in their unchanged between-test context. Corrective SHA `02bc28b96fa5ddf3c08f5a113a088711037f7237` supplied clean first-attempt remote Public evidence in run `31767060216`, including zero transport/persistence signatures and only the same two reviewed between-test full reloads. `aaeedad4` must never be pushed. The reusable prevention rule now requires inspection of complete local gate output, immediate pre-receive interruption on any ledger signature, explicit proof of zero remote effects, ledger/skill learning, and a new SHA before another attempt; `dev`/`main` rollout remains pending.
- `preview_sync_engine_mismatch_warning` — Preview Sync `31758981252` succeeded functionally but emitted 20 `EBADENGINE` lines because `workflow_run` used default-`main`'s Node 20/raw-install definition against checked-out `dev` code whose reviewed engine contract requires Node 24/npm 11. The structural auditor records this as `npm_engine_warning`. Run `31770526425` reproduced the exact known count and context at fixed lines 342–361 while using the same default-branch definition SHA `3c2528e2709a658a3ddc884ec1a88715ea994724`; it also contained the separately recorded Storage retry, so it is not clean evidence. The repository fix already lives in `dev`; the remaining rollout boundary is an explicitly authorized promotion of that workflow to `main`, because changing only the checked-out target SHA cannot alter the definition used by `workflow_run`. Do not suppress or relabel this warning as clean in the interim.
- `storybook_hidden_console_diagnostics` — a bounded re-search of all 1,755 available successful logs found browser-module externalization plus bundle-size/plugin-timing warnings in 44 runs and a React suspended-without-awaited-`act` console error in 30 runs (`31662535934` through `31708806810`). Their run conclusions alone were misleadingly green. PR #361 Storybook run `31726987329` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/31726987329/attempts/1)), exact SHA `3125f8cb3511cd306e0deb7c64accd93f0921e4f`, and replacement run `31728947725` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/31728947725/attempts/1)), exact SHA `d5472bce998bbd7f46fc8732bab12bc36ed51ede`, each concluded `success` with complete empty-annotation evidence while still emitting one React act warning, 41 browser externalization lines, and two bundle warnings. Follow-up run `31731125232` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/31731125232/attempts/1)), exact SHA `dd49d3db877f2e005843098df1b049f141d11315`, ran `2026-08-13T18:30:59Z` through `2026-08-13T18:35:08Z`; all 150 files / 462 Stories passed without retry, annotation/log collection was complete, the visual job was expectedly skipped, and the React act warning disappeared. It retained exactly 41 browser externalization lines and two bundle warnings, establishing the reviewed non-actionable tooling baseline. Any browser `console.error`, affected Story, or count/context change reopens this signature; an exact unchanged baseline may be audited without another evidence-only push. Rollout: the React fix is verified on the feature PR, pending `dev` and `main`.
- Historical npm package deprecations appeared in 115 successes. Any current recurrence requires package-specific evaluation.

- `preview_sync_required_profile_storage_gateway_failure` — Sync Preview Supabase run `32463780424` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/32463780424/attempts/1)), local workflow identifier `.github/workflows/preview-sync.yml`, `workflow_run`, definition SHA `340e055931171d9b6fde80007a047a77cc8420c0`, and triggering current `dev` target `ce90f09a7984a6e1115341f333de750b6e49d6b5`, ran `2026-08-21T08:34:12Z` through `2026-08-21T08:54:18Z` and concluded `failure`. Job index 0 concluded failure; step index 11 / number 12 failed, step index 12 / number 13 was the expected after-failure skip, and step index 2 / number 3 was the expected workflow-run-only guard skip. Annotation collection was complete: one failure annotation with title absent, message/path present, at line 300. Structural identity, terminal metadata, and log retrieval were complete. Fixed signatures were `storage_retry` 1 at line 757, `github_error` 1 at line 759, and `docker_registry` 1 at line 480; the last is a normal client-acquisition signal, not the cause. Safe in-memory classification found `profile_image_source_list` with numeric status 504, no provider-code value, on attempt 1/3, followed by the required-profile-image terminal failure. There was no object/bucket skip, database fallback, or circular-FK diagnostic. Classification is external Storage provider 504 compounded by repository-controlled resilience and observability limits: the old policy allowed only three short linear attempts and the final required-bucket wrapper discarded safe status/code-presence metadata. Correction #380 keeps the required bucket fail-closed, changes only retryable Storage faults to five bounded exponential attempts, preserves numeric status plus code-presence without provider messages or object paths, and adds transient-504, permanent-4xx, and safe-wrapper regressions. The original run is preserved and was not rerun, cancelled, or deleted. Rollout is blocked pending a new-SHA first attempt on `dev`, including clean Preview Sync and post-sync migration parity; `main` is untouched.

- `storage_provider_retry_recovered` (continued) — Sync Preview Supabase run `32468529642` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/32468529642/attempts/1)), local workflow identifier `.github/workflows/preview-sync.yml`, `workflow_run`, definition SHA `340e055931171d9b6fde80007a047a77cc8420c0`, and triggering current `dev` target `75e3a7dd55f23b6dee9c126833626a0de27ae9b1`, ran `2026-08-21T09:34:15Z` through `2026-08-21T09:57:51Z` and concluded `success`. The sole job index 0 / database ID `96730224003` and every executable step succeeded: the workflow-run-only checkout guard at step index 2 / number 3 was the expected skip, stale-SHA validation and pre-sync migration apply completed, data/Storage synchronization at step index 11 / number 12 completed, and post-sync migration verification at index 12 / number 13 completed. Annotation collection was complete and empty; identity, terminal metadata, and log retrieval were complete. Fixed signatures were `docker_registry` once at line 479, the normal local client-acquisition signal, and `storage_retry` once at line 759. A bounded in-memory classifier found one numeric status 504 recovery in the fixed required profile-image source-list operation, no provider-code value, no object/bucket skip, database fallback, circular-FK diagnostic, GitHub error, or GitHub warning. The target-SHA and post-guard execution evidence prove the current `dev` revision was synchronized; the workflow metadata's default-branch definition SHA is not the data target. Classification is an external Storage-provider transient recovered by the #380 bounded five-attempt exponential policy, not a product, member-authentication, schema, migration, credential-sanitization, Preview-database, or required-Storage fail-open defect. The existing executable prevention is `tests/preview-sync-storage.test.mts`, which covers retryable 5xx recovery, permanent 4xx non-retry, five-attempt exponential bounds, safe metadata preservation, and required-bucket fail-closed behavior. The immutable run was not rerun, cancelled, or deleted. Rollout: #380's correction is merged in `dev` at `75e3a7dd55f23b6dee9c126833626a0de27ae9b1`; this ledger extension is local on `docs/380-preview-storage-retry-ledger`; `main` remains untouched and promotion is blocked. A new independently triggered `dev` SHA must complete Preview Sync and post-sync migration parity with zero Storage retry, skip, fallback, circular-FK, error, or unreviewed-warning signature before Wallet-excluded Production promotion. Remaining risk is a fresh external Storage transient, which must extend this entry again rather than be suppressed by final parity.

- `storage_provider_retry_recovered` (continued) — Sync Preview Supabase run `32548260170` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/32548260170/attempts/1)), local workflow identifier `.github/workflows/preview-sync.yml`, `workflow_run`, definition SHA `340e055931171d9b6fde80007a047a77cc8420c0`, and triggering `dev` target `dd69b10838e7bd0649e5442aeac543e82f8d2aa9`, ran `2026-08-22T03:10:42Z` through `2026-08-22T03:35:25Z` and concluded `success`. The sole job index 0 / database ID `96970570371` and every executable step succeeded: step index 2 / number 3 was the expected workflow-run-only checkout skip, while stale-SHA validation, pre-sync migration apply, data/Storage synchronization at step index 11 / number 12, and post-sync migration verification at index 12 / number 13 all completed. Annotation collection was complete and empty; identity, terminal metadata, and log retrieval were complete. Fixed signatures were `docker_registry` once at line 479, the normal local client-acquisition signal, and `storage_retry` once at line 760. Bounded in-memory inspection classified the retry as one numeric status 504 recovery in the fixed required profile-image source-list operation with no provider-code value; object/bucket skip, database fallback, circular-FK diagnostic, GitHub error, and GitHub warning signatures were zero. Exact target checkout plus the stale-SHA guard and successful post-sync migration step prove the intended `dev` revision reached Preview; the workflow metadata's default-branch definition SHA is not the data target. This is another external Storage-provider transient recovered by #380's bounded five-attempt exponential policy, not a product, authentication, schema, migration, credential-sanitization, Preview-database, or required-Storage fail-open defect. `tests/preview-sync-storage.test.mts` remains the executable contract for retryable 5xx recovery, permanent 4xx non-retry, bounded backoff, safe metadata, and required-bucket fail-closed behavior. The immutable run was not rerun, cancelled, or deleted. Rollout: the recurrence occurred on a `dev` ancestor, this ledger extension is on `docs/380-preview-storage-retry-recurrence`, and `main` remains untouched. Wallet-excluded Production promotion is blocked until a manually dispatched Preview Sync against the exact current `dev` SHA completes data, Storage, and post-sync migration parity with zero retry, skip, fallback, circular-FK, error, or unreviewed-warning signature. Any further provider retry extends this entry again and stops promotion.

- `supabase_preview_sql_syntax_invalid_extract_qualification` — PR #403 external Supabase Preview check run `97153009352`, exact feature SHA `18aa9f8b9935195d57bdc08e4471b09f529d02b4`, started `2026-08-23T06:17:46Z`, completed `06:19:09Z`, and concluded `failure`. This was the Supabase GitHub App check rather than a repository GitHub Actions workflow, so no local workflow identifier, Actions attempt number, job/step indices, or Actions log was available. The check output collection was complete with zero annotations and one provider summary pointing to migration statement 8; its safe structural signature was one PostgreSQL parser failure (`SQLSTATE 42601`) inside `reserve_member_email_verification_challenge`. Source inspection proved the repository-controlled cause: PostgreSQL `extract` is special SQL syntax and cannot be schema-qualified as `pg_catalog.extract`; the invalid qualification appeared once in the forward migration and once in the schema snapshot. Migration ordering, database permissions, provider connectivity, and runtime application code were rejected because parsing stopped before the function could be created or executed. The correction replaces both qualifications with `extract(epoch from ...)` and extends the executable migration contract to require the valid form and reject `pg_catalog.extract` in both sources. The failed check was not retried, cancelled, or deleted. Replacement SHA `f97068941e109e2e1cf526b7b676f66faa8016f7` produced a first-attempt Supabase Preview check `97154333773` from `2026-08-23T06:31:06Z` through `06:31:25Z`; it concluded `success` with zero annotations, proving the corrected migration parsed and applied in the provider branch. The same SHA's Vercel deployment `dpl_4afCCrd556rfPfnrLMWwdzSVSfhn` reached `READY`; its Public Readiness run failed only in the separately recorded unrelated E2E readiness defect. Rollout remains blocked on PR #403 pending that defect's replacement-SHA evidence; `dev` and `main` remain unchanged. Remaining risk is another PostgreSQL grammar error that static source contracts cannot fully parse, so the post-merge Preview migration workflow remains mandatory.

- `e2e_auth_signup_networkidle_timeout` — PR #403 Public Readiness run `32623113260` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/32623113260/attempts/1)), local workflow `.github/workflows/public-readiness.yml`, `pull_request`, exact SHA `f97068941e109e2e1cf526b7b676f66faa8016f7`, was created `2026-08-23T06:30:39Z`, updated `06:41:48Z`, and concluded `failure`. Job index 0 / database ID `97154246037` succeeded. Job index 1 / `97154277290` failed at step index 6 / number 7 after the intended full-profile smoke skip at index 5 / number 6; post-Setup Node index 7 / number 13 was the expected after-failure skip and the remaining post steps succeeded. Job index 2 / `97155393743` then failed closed at policy step index 3 / number 4; its post-Setup Node step was the expected after-failure skip. Annotation collection was complete with two failure annotations: job 1 at line 397 and job 2 at line 19, both with message and path present and title absent, neither matching a fixed annotation signature. Identity, terminal metadata, full log retrieval, and audit completeness all passed. Fixed signatures were `failed_attempt_marker` 1 at line 2520, `test_failure` 1 at 2636, `github_error` 2 at 2651/2873, actual `timeout` 2 at 2609/2611, and `next_dev_full_reload` 3 at 2521/2599/2601; retry, flaky, transport, persistence, browser-console, npm-warning, and cache-collision signatures were zero. The only failed case was retry-free ordinal 5 in `auth-ops.spec.ts`: `/auth/signup` had loaded, but an unconditional global `networkidle` wait at source line 137 exhausted the 30-second test budget before any Mattermost form assertion; the other 85 cases passed. The first full-reload diagnostic followed that failed case and is not a reviewed clean baseline; the latter two retained their prior between-passing-admin-test context. The same test passed on preceding SHA `18aa9f8b9935195d57bdc08e4471b09f529d02b4`, and Supabase plus Vercel succeeded on the failed SHA, rejecting an email-flow, migration, dependency, build, route, or product-assertion defect. Classification is a repository-controlled E2E readiness-boundary defect: development-server background work is not equivalent to form unreadiness. The correction removes only that raw network-idle wait and uses the already-present finite generation-option attachment assertion as the semantic readiness boundary. A source regression requires navigation before that assertion and forbids network idle, sleeps, retry, slow mode, and timeout inflation inside the case. The original run was not rerun, cancelled, or deleted. The source contract passed, five independent fresh-server executions of the failed case passed without retry, and the complete local release gate passed 1,239 Node tests with 1,231 pass and eight intentional skips, 68 unit tests, the 103-route Production build, and all 86 retry-free E2E cases in 4.1 minutes. Timeout, failure, retry, flaky, and transport signatures were zero; only the two previously reviewed between-passing-admin-test full reloads remained. Replacement-SHA first-attempt Public, Supabase, and Vercel evidence are pending; PR #403 remains unmerged and `dev`/`main` are unchanged. Remaining risk is another generic readiness wait elsewhere, which must be fixed from its own evidence rather than suppressed or retried.

- `e2e_auth_signup_networkidle_timeout` (replacement evidence) — Corrective SHA `9394704f4d617ddfb9ef2da1c92cfc0c414f65b1` produced Public Readiness run `32624066888` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/32624066888/attempts/1)), `pull_request`, created `2026-08-23T06:52:36Z`, updated `07:02:58Z`, and concluded `success`. Job indices 0–2 and every executed step succeeded; the critical-smoke step index 5 / number 6 was the intended full-profile skip, while full release step index 6 / number 7 succeeded. Annotation collection was complete with zero annotations, and identity, terminal metadata, full-log retrieval, and audit completeness all passed. The run used Node `24.18.1` / npm `11.16.0`, validated 172 migrations, passed 1,239 Node tests with 1,231 pass and eight intentional skips, passed 68 unit tests, built 103/103 routes, and passed all 86 Playwright cases without retry in 6.2 minutes. Timeout, failure, retry, flaky, transport, persistence, browser-console, npm-warning, cache-collision, and GitHub-error signatures were zero; the only fixed signature was the reviewed `next_dev_full_reload` count of two at lines 2599/2601 between passing administrator cases. The same SHA's external Supabase Preview check `97156669650` ran `06:53:10Z`–`06:53:27Z` and succeeded with zero annotations, and Vercel deployment `dpl_FrnsgC8Lw1ipvcYCdtnE1gwGVSRr` reached `READY`. This closes both the signup-readiness correction and the earlier migration-parser correction on the feature branch without rerunning either failed attempt. PR #403 remains unmerged while this evidence-closing ledger commit receives its own branch-head checks; `dev` and `main` are unchanged at this point.

- `github_pull_request_opened_actions_event_absent` — PR #412 was opened against `dev` at exact feature SHA `fd082ac949d6ce4d58cb7bb968db76b8f749f5a4` on `2026-08-26T17:08:51Z`. The expected `pull_request` Public Readiness workflow produced no Actions run object, run ID, job, step, annotation, log, queued state, or skipped conclusion through `17:10:48Z`; collection was complete across the exact-SHA Actions API, workflow-specific run list, PR check rollup, and commit check-runs API. Repository Actions were enabled for all actions, the active base-branch workflow contained an unconditional `pull_request` trigger for `dev`, the PR was mergeable and non-draft, and recent PRs had used the same workflow, which rejects a disabled workflow, path exclusion, invalid base target, draft state, repository permission policy, or explicit job skip. The same immutable SHA independently produced Vercel Preview deployment `DGYhSKzTnwHL5PSx9C2XHDAtv3Xo`, which reached `SUCCESS` at `17:11:11Z`; both Supabase Preview checks were expected skips because the change had no migration or branch-schema operation. Classification is an external GitHub pull-request event ingestion omission with no run available to retry or audit, not a repository test, build, provider deployment, or product failure. The handling is to preserve the original SHA and successful Vercel record, commit this evidence on the feature branch, and use that new SHA's ordinary `synchronize` event as the first actionable Actions attempt; closing/reopening the PR, an empty commit, workflow rerun, and manual dispatch are not substitutes. Rollout remains pending on PR #412 and absent from `dev` and `main`. Acceptance requires the replacement SHA's first-attempt Public Readiness run and Vercel Preview to conclude cleanly, with exact-run audit before merge; remaining risk is another absent GitHub event, which must stop the rollout rather than be treated as a successful skip.

- `e2e_partner_detail_canonical_return_path_stale_expectation` — PR #412 Public Readiness run `32992951906` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/32992951906/attempts/1)), local workflow `.github/workflows/public-readiness.yml`, `pull_request`, exact feature SHA `6a9fb379b2957a2ce57bfa4217adc4465eed544f`, was created `2026-08-26T17:14:29Z`, updated `17:26:11Z`, and concluded `failure`. Job index 0 / database ID `98254821581` succeeded. Job index 1 / `98254963325` failed at step index 6 / number 7 after the intended critical-smoke skip at index 5 / number 6; post-Setup Node index 7 / number 13 was the expected after-failure skip and the remaining post steps succeeded. Job index 2 / `98258683469` then failed closed at policy step index 3 / number 4; its post-Setup Node step was the expected after-failure skip. Annotation collection was complete with two failure annotations: job 1 at line 416 and job 2 at line 19, both with message and path present and title absent, neither matching a fixed annotation signature. Identity, terminal metadata, full-log retrieval, and audit completeness all passed. Fixed signatures were `failed_attempt_marker` 1 at line 2346, `test_failure` 1 at 2482, `github_error` 2 at 2497/2719, and the two reviewed `next_dev_full_reload` diagnostics at 2433/2435; retry, flaky, timeout, transport, persistence, browser-console, npm-warning, and cache-collision signatures were zero. The only failed case was `auth-ops.spec.ts`'s partner-detail login return-path assertion: the updated product correctly redirected the obsolete `?returnTo=` partner URL to `/partners/health-001` and produced `/auth/login?returnTo=%2Fpartners%2Fhealth-001`, while the stale E2E expectation still required the removed nested directory return path. The other 91 cases passed without retry, and local Node/unit/type/security/build gates had already succeeded. Classification is a repository-controlled test-contract defect introduced by the same canonical-URL change, not a product navigation, redirect-loop, authentication, dependency, provider, build, or deployment failure. The correction renames the scenario around canonical partner navigation and requires both the login link and decoded `returnTo` value to contain only `/partners/health-001`; the scenario itself remains the executable regression contract. The original run was not rerun, cancelled, or deleted. Replacement-SHA focused E2E, first-attempt Public Readiness, and Vercel Preview evidence are pending; PR #412 remains unmerged and `dev`/`main` remain unchanged. Remaining risk is another test or consumer that intentionally depended on the removed nested partner query, which must be found by the full retry-free suite rather than restored to the public URL.
- `local_partner_hero_contract_stale_path` — A local `npm run verify:change` on `fix/public-carousel-ui-regressions`, exact pre-commit HEAD `3dd0361efdef8f23d1691d376d81d15923533f47`, failed at `2026-08-26T23:25:58+0900` before commit and before `receive-pack`. The high-risk Quick profile passed install policy, cross-platform policy, canonical lock verification, 172 migration validation, lint with one non-blocking ignored-prototype warning, and semantic typecheck, but the Node result was 1,254 pass, one failure, and eight intentional skips out of 1,263 tests. The sole failure was an `ENOENT` in the responsive source contract because it still read deleted `PartnerDetailHeroMeta.tsx` after the partner header responsibility moved to `PartnerDetailHeroContent.tsx`; this is a repository-controlled test co-change defect, not a product render, dependency, migration, type, GitHub, Vercel, Preview, or Production failure. The correction points the contract at the new owner and continues to require 32px visual actions with 44px pseudo-element touch targets. The remote feature branch and PR were absent, no Actions run or deployment was created, and `dev`/`main` remained unchanged. Acceptance requires the focused contract and a fresh complete `verify:change` with no retry, hidden error, or blocking ledger signature before the first release push.

- `github_pull_request_opened_actions_event_absent` (replacement evidence) — The ordinary `synchronize` event for exact replacement SHA `6a9fb379b2957a2ce57bfa4217adc4465eed544f` created Public Readiness run `32992951906` at `2026-08-26T17:14:29Z`, proving the prior omission did not recur. That run's retry-free E2E failed only for the separately recorded stale canonical-return-path expectation, so it is ingestion recovery evidence rather than clean release evidence. Corrective SHA `f6cb890d8bac6b81d312dff6dcb977ab2455ef87` then produced first-attempt Public run `32994642514` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/32994642514/attempts/1)) and a successful Vercel Preview status at `https://vercel.com/myknows-projects/ssartnership/ARwwqsk2nSwzxNoeKyeYngBSfHuf`. This closes the external event-ingestion omission without reopening or rerunning the PR; the original no-run SHA remains preserved.

- `e2e_partner_detail_canonical_return_path_stale_expectation` (replacement evidence) — Corrective SHA `f6cb890d8bac6b81d312dff6dcb977ab2455ef87` produced Public Readiness run `32994642514` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/32994642514/attempts/1)), `pull_request`, created `2026-08-26T17:31:23Z`, updated `17:43:36Z`, and concluded `success`. Every job and executed step succeeded; the critical-smoke step was the intended full-profile skip, 92 Playwright cases passed without retry in 7.1 minutes, annotation collection was complete with none present, and identity, terminal metadata, log retrieval, and audit completeness all passed. Failure, timeout, retry, flaky, transport, persistence, browser-console, npm-warning, cache-collision, and GitHub-error signatures were zero; the only fixed signature was the reviewed `next_dev_full_reload` count of two at lines 2433/2435. The same SHA's Vercel Preview status succeeded and its Supabase Preview PR check was the expected no-schema skip. PR #412 merged to `dev` as `db98a0fe0037820786bfeef75347adb2fafe66f3`; exact-SHA push run `32995910943` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/32995910943/attempts/1)) then succeeded with complete empty annotations and zero fixed signatures, while Vercel and Supabase Preview checks also succeeded. This closes the canonical-return-path test correction through `dev`; `main` remains unchanged pending the combined Production promotion.

- `local_partner_hero_contract_stale_path` (replacement evidence) — On `refactor/409-partner-detail-ui`, the corrected focused Node contracts passed 17/17, the partner-introduction Playwright scenario passed 1/1 without retry, and the fresh high-risk `npm run verify:change` passed install/cross-platform/lock/migration/lint/type checks, 1,264 Node tests with 1,256 pass and eight intentional skips, 68 unit tests, and the zero-advisory Production dependency audit. The only lint diagnostic was the pre-existing ignored prototype warning. This closes the local stale-path correction before any PR #409 remote trigger; first-attempt Public Readiness and Vercel Preview evidence for the rebased feature branch remain pending, and `dev`/`main` do not yet contain the partner-detail UI work.

- `local_typecheck_static_image_module_excluded` — A local `npm run verify:change` on `fix/public-carousel-ui-regressions`, exact committed HEAD `e3c79fb551969f83fbedb0e5c537ba3541bbe107`, failed at `2026-08-27T01:57:49+0900` before the first release push and before `receive-pack`. The high-risk Quick profile passed install policy, cross-platform policy, canonical lock verification, and validation of 172 migrations; lint emitted the single already reviewed non-blocking warning from the ignored `.tmp/ui-prototypes/transactional-email-gallery/gallery-data.js`, then the CI-specific semantic typecheck failed with seven `TS2307` diagnostics for the newly imported PNG install-guide captures. The preceding focused Node tests, changed-file ESLint, ordinary Next-aware `tsc`, and all 487 Storybook interactions had passed, which rejects invalid route logic, platform detection, interaction behavior, image-file absence, and lock or migration drift. The repository-controlled cause is that `tsconfig.typecheck.json` deliberately excludes `next-env.d.ts`, so Next's global static-image module declaration is unavailable to the CI typecheck even though the application compiler accepts the imports. The correction moves the captures to `public/install-guides`, references stable public URL strings, and allowlists that path in `images.localPatterns`, preserving actual simulator images without depending on the excluded ambient declaration; the image-presence and image-config contracts follow the public asset owner. No GitHub Actions run, Vercel deployment, PR, `dev`, or `main` mutation occurred. Acceptance requires the focused PWA contract, CI-specific typecheck, rendered route, and a fresh complete `verify:change` on a replacement commit with no retry, hidden error, or new blocking signature; residual risk is a source asset path that exists in Git but is mistyped in UI, so the rendered mobile/tablet/desktop screenshots and build remain required.

- `local_verify_change_orchestrator_timeout` — The first final `npm run verify:change` for Issue #418 on `feat/418-required-member-email-gate`, exact pre-commit HEAD `df28e998bb3d64fec9b4a5c95a3005077d8afc6d`, was externally terminated at `2026-08-27T17:45:07+09:00` before commit and before `receive-pack` because the desktop shell call was mistakenly given a one-second orchestration timeout. The shell returned exit 124 after about 4.6 seconds and the verification wrapper reported that `verify:quick` exited 1; the run ended before a product, test, type, migration, dependency, or build result could be established. A process-age check found no surviving Node process from the interrupted invocation. The remote feature ref remained absent, the exact HEAD had zero GitHub Actions runs, and no PR, Vercel deployment, Preview mutation, Production mutation, or branch update was created. This is an operator-controlled command-lifetime defect, not repository validation evidence. The original invocation is preserved and must not be relabeled as a test failure or pass. The fresh replacement `verify:change` then completed normally under pinned Node 24.18.1/npm 11.16.0 in about 250.5 seconds: install/cross-platform/lock/migration/lint/type checks passed, 1,285 Node tests reported 1,277 passes and eight intentional skips, all 68 unit tests passed, and the Production dependency audit had no advisories. This closes the local command-lifetime incident before commit; the release gate still remains mandatory, and any new abnormal output must stop before remote transfer and extend the ledger again.

- `local_release_windows_multiline_argument_spawn_failure` — The first `npm run release` invocation for Issue #418 on exact pre-commit HEAD `df28e998bb3d64fec9b4a5c95a3005077d8afc6d` stopped at `2026-08-27T17:50:56+09:00` before the release script, gate, version update, commit, or `receive-pack` began. The desktop Windows batch launcher rejected the inline multi-line `--message` argument and returned exit 1 with its fixed spawn/argument diagnostic in about 0.3 seconds. Git status and HEAD remained unchanged, the remote feature ref remained absent, and the exact SHA had zero Actions runs; no PR, Vercel deployment, Preview mutation, Production mutation, package-version change, or branch update was created. This is a local Windows process-argument transport defect, not a release-script, validation, commit-message, product, test, dependency, GitHub, or provider failure. Prevention is to pass the same reviewed multi-line Korean conventional commit message through an ignored direct-child message file, which the release script already validates and reads without shell argument splitting. Acceptance requires the fresh release invocation to complete its pinned gate, patch version, commit, and single push normally; any new abnormal output must stop further mutation and extend this ledger first.
- `e2e_home_partner_card_back_navigation_hydration_race` — PR #423 Public Readiness run `33308685128` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/33308685128/attempts/1)), local workflow `.github/workflows/public-readiness.yml`, `pull_request`, exact feature SHA `6fd8e9b715de5de1f795f553befeae5bf05d6d58`, was created `2026-08-30T11:20:36Z`, updated `11:32:09Z`, and concluded `failure`. Job index 0 succeeded. Job index 1 / database ID `99249705899` failed at step index 6 / number 7 after the intended full-profile smoke skip at step index 5 / number 6; post-Setup Node step index 7 / number 13 was the expected after-failure skip. Job index 2 / database ID `99250942727` then failed closed at policy step index 3 / number 4. Annotation collection was complete with two failure annotations: job 1 at line 417 and job 2 at line 19, both with message and path present and title absent, neither matching a fixed annotation signature. Identity, terminal metadata, full-log retrieval, and audit completeness all passed. Fixed signatures were `failed_attempt_marker` 1 at line 2404, `test_failure` 1 at line 2516, `github_error` 2 at lines 2531/2753, and the two reviewed `next_dev_full_reload` diagnostics at lines 2472/2474; retry, flaky, timeout, transport, persistence, browser-console, npm-warning, and cache-collision signatures were zero. The build generated 103/103 routes and 95 retry-free Playwright cases passed; the sole failed public-partner discovery case returned twice from a detail page, confirmed only the server-rendered search value and result count, then clicked a JavaScript-only card surface before the restored document's hydration marker was ready, so the click completed without changing the `/?q=...#benefits` URL. Local reproduction on the same clean SHA passed, while source and log inspection proved that initial navigation used the existing finite directory-hydration guard and both post-history traversals omitted it. Classification is a repository-controlled E2E readiness-boundary race, not a product router, card target, partner data, build, dependency, deployment, or network failure. The correction reuses `waitForDirectoryControls(page)` immediately after each `page.goBack()`; it adds no force click, sleep, retry, timeout inflation, or `networkidle`. The failed case passed four independent fresh-server executions on separate ports without retry, the complete `home-partners.spec.ts` file passed 13/13 on another fresh server, and the pre-push E2E gate contract passed 2/2. The immutable failed attempt was not rerun, cancelled, or deleted. Replacement-SHA first-attempt Public Readiness and Vercel Preview evidence are pending; PR #423 remains unmerged, and `dev`/`main` are unchanged. Remaining risk is another history-restored interactive surface that lacks an explicit hydration contract, so the complete retry-free E2E gate and exact-log audit remain mandatory before merge.
- `local_build_partner_notification_server_contract_leak` — A local `git push origin HEAD:refs/heads/refactor/partner-detail-header-meta` from clean worktree branch `refactor/partner-detail-header-meta-devsync`, exact pre-commit HEAD `fca12a39bfea712440a6496df89140a67988896e`, failed on `2026-08-31T00:01:09+09:00` inside the local pre-push hook before `receive-pack`. The remote feature ref stayed at `6fd8e9b715de5de1f795f553befeae5bf05d6d58`, and no new Actions run or Vercel deployment was created. The hook passed lint, 1,365 retry-free Node tests, and 69 unit tests, then `next build --webpack` failed while compiling `src/components/partner/partner-notifications/PartnerNotificationCenter.tsx`: a client import of the runtime scope-label constant pulled in `src/lib/partner-notifications.ts`, which eagerly imported the repository index and reached `src/lib/repositories/supabase/ad-package-repository.supabase.ts`'s `node:crypto` dependency. Classification is a repository-controlled client/server boundary leak, not a GitHub, Vercel, migration, dependency-install, or test-flake failure. The correction splits the shared notification types and scope-label constant into `src/lib/partner-notification-contract.ts` and redirects client, story, mock-scenario, and UI-helper imports there while keeping server aggregation in `src/lib/partner-notifications.ts`. Executable regression evidence is the focused `tests/partner-notification-ui.test.mts`, a fresh `next build`, and a fresh `npm run verify:change`, all passing locally on the replacement commit with Node `24.18.1` / npm `11.16.0`. Replacement-SHA first-attempt PR and Vercel evidence are pending; PR #423 remains unmerged, and `dev`/`main` are unchanged. Remaining risk is another client-facing module importing a mixed server aggregation file for a runtime constant instead of a shared contract.

- `preview_migration_missing_legacy_function_revoke` — PR #426 head `6ee70fc23b98f9e084d10199f15b9913aa3dab24` produced external check run `99371080611`, `Supabase Preview`, first attempt, started `2026-08-31T03:17:47Z`, completed `03:19:14Z`, and concluded `failure` while the same SHA's Public Readiness run `33353356160` and cross-platform run `33353356158` both later concluded `success`. The Supabase check emitted a schema-replay failure before Preview verification could complete: the public-access-hardening migration tried to revoke execution on legacy helper `public.sync_partner_benefit_cache_version()` even though no current migration or schema snapshot in the repository defines that function anymore. Repo-wide source inspection found only the unconditional revoke statements in `supabase/migrations/20260831090039_harden_partner_public_table_access.sql` and mirrored `supabase/schema.sql`, while `tests/partner-public-table-security.test.mts` enforced that stale unconditional text and therefore protected the broken replay contract. Classification is a repository-controlled migration/idempotency defect, not an external provider outage, GitHub Actions failure, Vercel issue, lockfile/runtime mismatch, or application-path regression. Corrective commit `ba4deb5efbd632f31c98d83dcbe5550bc17525ef` guards that revoke behind `to_regprocedure('public.sync_partner_benefit_cache_version()') is not null` in both the migration and schema snapshot and updates the security contract test to require the conditional guard while still requiring revocation for all three roles when the function exists. Replacement local evidence on the corrective SHA is clean: `node --test tests/partner-public-table-security.test.mts` passed 2/2, `npm run validate:migrations` validated 190 files, `npm run audit:security` passed with only the reviewed development-only `image-size` advisories, and exact-runtime `Node 24.18.1` / `npm 11.16.0` `npm run verify:change` passed with 1,591/1,599 Node tests, 98/98 unit tests, canonical lockfile, and zero production dependency advisories. Rollout state: fixed locally on `refactor/partner-detail-header-meta-final`; replacement-SHA first-attempt PR checks, Supabase Preview, and Vercel Preview are pending; `dev` and `main` are unchanged. Remaining risk is another historical helper name hardened by raw `revoke` after the function has been removed, so Preview replay remains the authoritative remote check before merge.

- `local_focused_test_alias_loader_omitted` — A local conflict-resolution check for Issue #418 on merge-in-progress `feat/418-required-member-email-gate`, pre-merge HEAD `555f8ca5c21040aa32da93142697cf3e89412da9`, failed at `2026-09-03T00:53:07+09:00` before a merge commit, push, PR, deployment, or GitHub Actions trigger. The coordinator invoked `node --test tests/member-email-required-gate.test.mts tests/member-required-gates.test.mts` without the repository-required `tests/alias-register.mjs` import. The source-contract file passed both assertions, while the runtime gate file stopped at module loading with `ERR_MODULE_NOT_FOUND` for the `@/lib` alias. Concurrent changed-file ESLint and `typecheck:ci` both exited 0. This is a local operator invocation defect, not a product, type, assertion, dependency, GitHub, Vercel, Preview, or Production failure. Prevention is to use the canonical Node test command with `--import ./tests/alias-register.mjs`; acceptance requires the same two focused files to pass under that loader before the merge commit. The remote feature ref remains `555f8ca5c21040aa32da93142697cf3e89412da9`, `dev` remains `51e093c66d3d9fb81223d44e8acb3a36fe7d77ad`, and no remote state changed.

- `local_user_session_snapshot_contract_stale_after_gate_merge` — The first post-merge `npm run verify:change` for Issue #418 on local HEAD `f8e455e2` failed at `2026-09-03T00:56:40+09:00` before commit, push, PR, deployment, or GitHub Actions trigger. The high-risk Quick profile passed docs, install policy, cross-platform policy, canonical lock verification, 197 migration files, lint, and CI typecheck, then the Node suite failed in `tests/user-session-request-cache.test.mts`. The test still required the exact pre-email-gate projection `id,auth_session_version,must_change_password`, while conflict resolution intentionally preserved the latest `dev` request-cache optimization and extended that same single member query with `email_verified_at` and `mattermost_login_disabled_at` so `requiresEmailRegistration` can be derived without restoring a second query in `getUserSession`. Classification is a repository-controlled stale source-contract assertion exposed by the integration merge, not a runtime, type, dependency, schema, GitHub, Vercel, Preview, or Production failure. The correction updates the contract to require the full single-query projection and the email-gate derivation while retaining the existing assertion that `getUserSession` performs no member query. Acceptance requires the focused request-cache and email-gate contracts plus a fresh complete `verify:change`. The remote feature ref remains `555f8ca5c21040aa32da93142697cf3e89412da9`, `dev` remains `51e093c66d3d9fb81223d44e8acb3a36fe7d77ad`, and no remote state changed.

- `local_auth_reset_e2e_networkidle_policy_regression` — The second post-merge `npm run verify:change` for Issue #418 on local HEAD `f8e455e2` failed at `2026-09-03T00:59:21+09:00` before commit, push, PR, deployment, or GitHub Actions trigger. Docs, install policy, cross-platform policy, canonical lock verification, 197 migrations, lint, CI typecheck, and the previously corrected request-cache contract passed. The Node suite then failed the repository E2E source policy because the older feature branch's password-reset scenario still used `page.waitForLoadState("networkidle")`, which the latest `dev` explicitly forbids for authentication flows. Classification is a repository-controlled stale finite-readiness contract exposed by the integration merge, not a product assertion, browser runtime, dependency, schema, GitHub, Vercel, Preview, or Production failure. The correction replaces global network idle with the shared `waitForPageReady(page, memberTab)` boundary already used by the merged authentication suite, without retry, sleep, force click, or timeout inflation. Acceptance requires the focused pre-push E2E policy and password-reset contract plus a fresh complete `verify:change`. The remote feature ref remains `555f8ca5c21040aa32da93142697cf3e89412da9`, `dev` remains `51e093c66d3d9fb81223d44e8acb3a36fe7d77ad`, and no remote state changed.

## Mandatory entry for every new blocking abnormal run

- `local_self_host_rotation_operator_preflight` — The corrected 2026-09-07 credential-hardening Release passed Quick and build, then Playwright refused occupied port 3100 before running tests. Docker's existing ssartnership-local app and a separate Node listener already own that port; neither is stopped. Select and verify a free E2E_PORT before the next complete invocation. A separate focused self-host glob also omitted the repository alias-register import and failed loading mock promotions; its earlier passing files are not a complete focused pass. Preserve both failed logs, use the canonical alias loader, and verify process/port ownership before starting a fresh Release. These are operator invocation failures, not permission to reuse another service, loosen assertions, or claim E2E success.

- `local_self_host_logging_test_yaml_types` — The first approved credential-hardening local Release on 2026-09-07 stopped at semantic typecheck with TS7016: the new Compose regression imported the existing js-yaml package without bundled declarations. Focused tests and actual AMD64 credential rotation had passed, but no build/E2E or deployment was accepted from that Release. Use the installed CommonJS loader with an explicit unknown-returning parser contract in this test; do not disable the type gate or introduce a new dependency for a test-only declaration. Preserve the first failed log and run the corrected semantic and full Release gates before deploying.

- `local_self_host_initial_ddl_credential_logging` — A value-presence-only audit of the new synthetic Preview's complete container stdout/stderr on 2026-09-07 found its PostgreSQL password in three DB log entries. No secret value or matching statement was printed or copied to local evidence. The pinned image's shipped configuration uses `log_statement=ddl` during initialization; the three matches are LOG-level ALTER USER statements from the local role setup and upstream administrator setup. The later operator session reports `log_statement=none`, so a post-bootstrap SHOW alone cannot certify the earlier initialization boundary. App, Storage, gateway, observability and exporter logs contained none of the scanned current credentials. Full backup through the physical approved controller path succeeded; automatic maintenance was stopped and disabled, and recovery export/Keychain custody/restore were not attempted. Keep the deployment synthetic and loopback-only. Root-only log access narrows exposure but does not make plaintext credential logging acceptable. The remaining security work is a reviewed initialization logging boundary that preserves error severity while protecting SQL/parameter contents, replacement of affected synthetic DB credentials with service parity, and a new backup before export/recovery. No Production data, public ingress, cloud state or access-account lifecycle changed. Do not mark migration, backup recovery or maintenance acceptance complete from this checkpoint.

- `local_self_host_current_link_silent_entrypoint` — On 2026-09-07, the freshly installed maintenance service and manual backup command returned exit 0 with no receipt or actual operation. Node resolves the `current` symlink to the physical controller module, while the CLI guard compared it with unresolved argv. Both maintenance and recovery export skipped their entrypoint. The operator stopped only the six new maintenance timers; the verified eleven-service synthetic Preview remained running. Two subprocess regressions reproduce link-path false success under a forced unprivileged identity, even in root CI. Resolving argv before comparison fixes both entrypoints without changing root checks, approved state, locks, command allowlists or secret handling. Both tests fail before and pass after; maintenance/Mac focused tests pass 13/13. Preserve the empty logs and terminal results: they are not backup or scheduler acceptance. Complete local verification, Linux link-invocation proof and real receipts are required before resuming timers. Application/database/telemetry images remain the gated `5b807483` artifacts; the maintenance-only controller upgrade has a separately recorded identity.

- `local_self_host_operator_ledger_name_assumption` — After the successful synthetic Preview bootstrap of `5b807483` on 2026-09-07, a separate read-only PostgreSQL verification failed because the operator assumed the managed Supabase migration-history relation. This repository's self-host runner owns `self_host.migration_ledger`; source lookup and the corrected read-only query confirmed PostgreSQL 17.6 and all 199 entries. Bootstrap itself exited 0 and the independent eleven-service HTTP/monitoring/image/fixture-boundary audit passed. No migration was rerun or changed, no rollback was needed, and the failed read is not product or migration failure evidence. Use the repository's status command or actual ledger contract rather than inferring provider metadata names.

- `local_self_host_image_packaging_diagnostics` — The first complete Mac Docker AMD64 execution of `5b807483af416c22fd0c2ccd9118758e74b6fec5` finished normally on 2026-09-07: terminal exit 0, OOM false, 103 E2E passes with zero failures/errors/skips/retries, unchanged deployable fingerprint, and all three hashed image archives. The entire 247,032-byte / 3,089-line log was audited. The two image-upload diagnostics are intentional mocked 503 unit contracts; four registry lexical matches are successful image metadata acquisition. Packaging additionally emitted one account UID-range warning, two PostgreSQL directory-selection warnings, two absent manual-directory notices and two absent APK-index-cache warnings. Offline inspection of the resulting images confirms app UID 1001 (not root), no fixture tree/output/header, PostgreSQL server and pg_dump 17.6 resolved through the original Nix profile, and pgBackRest 2.57.0. Alpine's pgBackRest package also installs its PostgreSQL 18 dependency; its selector preserves the existing non-symlink upstream directories, and `--no-cache` intentionally leaves no index cache. This is packaging/tooling output, not a shown failed build, missing backup binary, running PostgreSQL 18 service, or E2E error. Preserve the exact warnings and original image identities; do not suppress all package diagnostics. Synthetic Preview may proceed using immutable images, with actual DB/WAL/backup/restore parity still required. Removal of the redundant Alpine server package and supply-chain minimization remain pre-Production hardening work; this record does not establish security or recovery acceptance for that extra package. No Actions run, push, cloud deployment or Production mutation occurred.

- `github_self_host_publish_legacy_repositories_entry` — Self-host Preview Images run `34172014552` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/34172014552/attempts/1)), local workflow `.github/workflows/self-host-preview.yml`, `push` to `dev`, exact merge SHA `45cb751106526776a6e35feb5492cabdc9126d98`, was created `2026-09-08T00:02:39Z`, started `00:02:43Z`, completed `00:13:27Z`, and concluded `failure`. Job index 0 / database ID `101893990067` (`Isolated AMD64 Release Gate`) completed successfully, including both required build/upload steps. Job index 1 / database ID `101895613488` (`Publish Verified Preview Images`) failed at step index 5 / number 6 (`Verify archives and current dev before publication`); its publication step index 6 / number 7 was skipped, while registry logout succeeded. Annotation collection was complete: one failure annotation at the failed job with line 8, message/path fields present and title absent; no fixed annotation signature was added. Safe signature counts from the complete workflow audit are `github_error` 1 at line 3770, `docker_registry` 4 at lines 201/224/3103/3115, and `deprecation` 2 at lines 3744/3745; the registry and deprecation matches are non-causal setup/warning output. The downloaded artifact digest matched the expected `sha256:3ced0a36cc866cdebad9b264d96acc7b61302870bee33f8edbf07cdacc0a63d9` and contained the exact three archives plus bundle. Local replay of `sanitizeDockerArchive` on the app archive failed closed with `CI_TAR_PATH_INVALID` because Docker's valid OCI-backed save includes the top-level `repositories` metadata file, which the parser's allowlist omitted; no image load, push, manifest publication, server deployment, Preview mutation, or Production mutation occurred. Classification is a repository-controlled archive parser compatibility defect, not a GitHub, registry, artifact-integrity, provider, or source-build failure. The correction allowlists `repositories` as ignored legacy metadata while continuing to copy only the manifest/config/layer closure, and adds a regression fixture asserting a valid Docker save containing `repositories` is accepted without importing its tags. The failed attempt is preserved and will not be rerun; acceptance requires a fresh replacement-SHA first-attempt Self-host Preview Images run with all jobs/steps complete and exact immutable manifest verification. Rollout state: current branch `feat/self-host-receiver-20260908` is uncommitted, `dev` remains `45cb751106526776a6e35feb5492cabdc9126d98`, and `main` is unchanged. Remaining risk is another Docker/OCI save metadata variant outside the explicit allowlist; the sanitizer regression and fresh exact-SHA publication gate remain mandatory.

- `local_self_host_receiver_typecheck_fixture` — The first local `npm run verify:change` for the receiver/parser/overlay work on exact pre-correction HEAD `45cb751106526776a6e35feb5492cabdc9126d98` stopped at `typecheck:ci` on 2026-09-08 09:39 KST before build/E2E, commit, push, GitHub Actions, deployment, or Preview/Production mutation. The focused archive/receiver tests and Compose contract had passed, but the new receiver test left callback arrays and URL/argument parameters implicit and its injected test config was rejected by the frozen literal type inferred from `RECEIVER_CONFIG`; the deploy callback also returned an incomplete shape. Classification is a repository test-boundary typing defect, not product/runtime, Docker, GitHub, registry, provider, data, or deployment evidence. The correction removes the unnecessarily narrow frozen config inference, adds explicit callback/input types and a complete deployment receipt, and preserves the production receiver contracts. Acceptance requires the focused tests plus a fresh complete `verify:change`; the failed invocation is retained in the turn log and no remote state changed. Replacement-SHA first-attempt Actions and deployment evidence remain pending.

Append or extend the matching signature before the next remote trigger with:

- Run ID and immutable URL
- Local allowlisted workflow identifier, event, exact SHA, attempt, timestamps, and GitHub conclusion
- Numeric indices and conclusions for every failed or unexpectedly skipped job/step; each annotation's structural level/field-presence/line/signatures and an explicit collection-complete flag
- Fixed signature IDs/counts/line numbers plus an operator-authored safe cause summary; never raw or regex-redacted GitHub text
- Proven classification and rejected alternatives
- Repository fix or external-provider handling
- Executable regression/fail-closed contract
- First-attempt verification on the replacement SHA
- Rollout state on branch, PR, `dev`, and `main`
- Remaining risk or expired/unavailable-log boundary

Never delete or rewrite the original evidence. A rerun cannot replace a new-SHA first attempt for a repository-controlled correction. An unchanged SHA may be retried after a proven external-only outage because there is no source correction to encode; retain the original provider incident and verify the recovery separately.

- `supabase_preview_ad_campaign_rollups_bigint_coalesce_type_mismatch` — PR #426 external Supabase Preview check run `99420036634`, `pull_request`, exact feature SHA `0236c905f679917ddc74fc830c5bce4ed8bd8472`, started `2026-08-31T07:53:57Z`, completed `07:54:13Z`, and concluded `failure`. The check had zero annotations, complete structural metadata, and no GitHub Actions job or step surface because this was an app-owned Preview database validation rather than a repository workflow. Safe structural evidence showed one provider parser failure while applying the new `public.get_admin_ad_campaign_rollups(uuid)` definition, specifically in the final rollup `select` list. Repository inspection on the same immutable SHA proved the new migration `20260831145630_aggregate_admin_ad_campaign_rollups.sql` and matching `supabase/schema.sql` snapshot used `pg_catalog.coalesce(<bigint expression>, 0)` for six bigint result columns, leaving the fallback literal integer-typed. Classification is a repository-controlled Preview-schema contract defect, not a Supabase provider outage, migration ordering issue, privilege or RLS rejection, runtime data problem, or GitHub event omission. The correction is to type each fallback as `0::bigint` in both the forward migration and schema snapshot, and to extend `tests/admin-partner-campaign-performance.test.mts` so the rollup SQL contract now requires bigint-typed coalesce fallbacks. The immutable failed check was not rerun, cancelled, or deleted. Replacement-SHA first-attempt Supabase Preview, Public Readiness, and Vercel evidence are pending on PR #426; `dev` and `main` remain unchanged. Remaining risk is another SQL function that passes local text-only schema checks while failing provider-side parse validation, so the contract test and fresh first-attempt Preview evidence are both required before merge.

- `supabase_preview_ad_campaign_rollups_bigint_coalesce_type_mismatch` (root-cause correction) — Replacement feature SHA `484dc49bd76dcaf79b2fd9a7c25897fd63e05226` produced external Supabase Preview check run `99422107423`, `pull_request`, started `2026-08-31T08:02:49Z`, completed `08:03:02Z`, and concluded `failure`. The app-owned check again had zero annotations and no GitHub Actions job or step surface. Its bounded provider evidence proved that both arguments were now `bigint`, but PostgreSQL still attempted to resolve `pg_catalog.coalesce(bigint, bigint)` as a normal catalog function. This corrects the preceding entry's incomplete root-cause classification: `coalesce` is special SQL syntax and cannot be schema-qualified, so the `pg_catalog.` prefix—not the fallback integer type—was the blocking parser contract. The immutable first and replacement failures were not rerun, cancelled, or deleted. The repository correction removes only the invalid `pg_catalog.` qualifier from every rollup `coalesce` expression in the forward migration and schema snapshot, retains explicit `0::bigint` result typing, and extends `tests/admin-partner-campaign-performance.test.mts` to reject any reintroduced `pg_catalog.coalesce(` form. Vercel, Windows x64, and macOS arm64 succeeded for `484dc49b`; Public Readiness was still active when this failure was classified. A new corrective SHA must provide clean first-attempt Supabase Preview, Public Readiness, and Vercel evidence before merge. `dev` and `main` remain unchanged. Remaining risk is another PostgreSQL special form incorrectly schema-qualified in a new migration, so provider replay remains authoritative in addition to the focused text contract.

- `preview_migration_provider_check_concurrency_collision` — PR #426 merged to `dev` as exact SHA `59196dc1b140005148afefe8fe98fc360956d8f5`, whose `.github/workflows/preview-migrations.yml` push run `33375215384` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/33375215384/attempts/1)) was created `2026-08-31T08:56:01Z`, updated `08:56:54Z`, and concluded `failure`. Job index 0 / database ID `99435045770` failed: step indices 0–8 / numbers 1–9 succeeded, step index 9 / number 10 failed while applying pending Preview migrations, step index 10 / number 11 was the expected after-failure verification skip, step index 11 / number 20 succeeded, step index 12 / number 21 was the expected post-setup skip, and step indices 13–14 / numbers 22–23 succeeded. Annotation collection was complete with one job-level failure annotation at line 23 whose title was absent and message/path were present; it matched no fixed annotation signature. Identity, terminal metadata, full-log retrieval, and audit completeness all passed. Fixed signatures were `migration_failure` seven times at lines 39/53/325/333/605/609/610 and `github_error` once at line 610. Exact-SHA and live-`dev` guards, migration-file validation, CLI setup, and the pre-apply migration listing all succeeded. The repository workflow then began its database push while the same commit's app-owned `Supabase Preview` check run `99435248236` was still executing against Preview; that provider check ran `08:56:31Z`–`08:56:53Z` and concluded `success`, overlapping the workflow's list at `08:56:42Z`–`08:56:44Z` and push at `08:56:44Z`–`08:56:50Z`. The workflow reached a unique-key collision while recording migration version `20260831144254`, proving two concurrent migration writers rather than a SQL parser, migration filename, stale-ref, credential, connection, or provider-availability defect. The immutable failed run is preserved and will not be rerun. The repository correction makes push-triggered Preview migration runs wait, for at most five minutes, for exactly one same-SHA `Supabase Preview` check from pinned Supabase GitHub App ID `330661` to complete successfully before listing or mutating migrations; absence, publisher mismatch, ambiguity, provider failure, or timeout fails closed, while the explicit exact-SHA manual recovery path remains independent. A source contract requires the check-read permission, exact-commit and pinned-app query, bounded semantic wait, publisher identity, success conclusion, ordering before database access, and absence of workflow rerun commands. Rollout state: the failure is present on `dev`; Issue #420 remains open, `main` is unchanged, and replacement-branch first-attempt Public, provider, Vercel, plus corrected `dev` migration-workflow evidence are pending. Remaining risk is a delayed or missing provider check, which now stops automatic mutation and requires the existing explicitly confirmed exact-`dev` recovery path instead of permitting concurrent writers.

- `local_preview_migration_workflow_contract_bootstrap` — The first focused local contract run on `fix/preview-migration-provider-race` at `2026-08-31T18:07+09:00` completed 31/32 tests and failed only the new Preview workflow assertion because its regular expression expected the bounded loop after the provider-query text while the workflow correctly opens the loop before performing each query. A simultaneous changed-file ESLint command did not start because the newly created clean worktree had not yet installed the repository's pinned dependencies and therefore could not resolve the tracked Storybook plugin. No commit, push, PR, GitHub check, Vercel deployment, Supabase check, or database mutation resulted. Classification is a local test-authoring order defect plus a local dependency-bootstrap omission, not a workflow semantic, application, schema, provider, or remote-run failure. The correction reorders only the contract expression to match the intended loop/query structure, installs dependencies through `npm run install:trusted`, and reruns the focused contract and changed-file lint before any remote trigger. Remaining risk is an assertion that merely matches text without exercising the GitHub-hosted check API, so exact replacement-SHA PR and post-merge `dev` workflow evidence remain mandatory.

- `local_dependency_advisory_database_advance` — A complete local `npm run verify:change` on `fix/preview-migration-provider-race`, exact pre-commit HEAD `59196dc1b140005148afefe8fe98fc360956d8f5`, failed on `2026-09-02T22:55+09:00` at the blocking security-audit stage before any commit, push, PR, GitHub check, Vercel deployment, Supabase check, or database mutation. Before that stage, the gate passed documentation governance for 81 documents, trusted-install and cross-platform policy, canonical lock verification, validation of 196 migrations, lint, CI typecheck, 1,676 Node tests with 1,668 passes and eight intentional skips, and 103/103 Vitest tests with 98.5% statement and 94% branch coverage. The newly refreshed advisory database identified direct Production dependency `sanitize-html@2.17.6` under `GHSA-g8qq-57p8-ggw5` and transitive development dependency `browserslist@4.28.2` under `GHSA-c83g-rgw3-j3cx` and `GHSA-73wf-gq98-2v4g`; the two pre-existing Storybook `image-size` advisories remained the reviewed no-fix baseline. Classification is dependency/advisory-database drift after the prior August 31 gate, not an application assertion, migration, workflow, runtime, provider, or install-policy failure. The narrow correction raises the direct `sanitize-html` floor to patched `2.17.7` and refreshes the compatible transitive lock entry to `browserslist@4.28.8` without broad Storybook or dependency-tree churn. Acceptance requires package/lock identity, focused email sanitization behavior, zero Production audit findings, the governed full-tree audit, and a fresh complete `verify:change` before the first remote trigger. Replacement-SHA evidence is pending; `dev` still contains the vulnerable lock versions and `main` is out of scope. Remaining risk is a future advisory-database advance between local verification and remote execution, so the remote first attempt must retain the same fail-closed audit.

- `local_focused_email_test_alias_bootstrap_omission` — A focused local Node invocation on `fix/preview-migration-provider-race`, unchanged exact pre-commit HEAD `59196dc1b140005148afefe8fe98fc360956d8f5`, failed at `2026-09-02T22:56:46+09:00` because the operator called `node --test` directly instead of the repository contract `node --import ./tests/alias-register.mjs --test`. The email test modules therefore could not resolve the established `@/lib` alias; 32 unrelated workflow and skill assertions passed, package identity and both dependency audits passed, and canonical lock verification passed. No source defect, package regression, commit, push, PR, provider check, deployment, or database mutation resulted. Classification is a local focused-command bootstrap omission, not an email sanitizer, alias-loader, runtime, workflow, or dependency failure. The replacement invocation must include the tracked alias register and pass the same focused email and workflow files before the complete gate. Remaining risk is none beyond using the canonical test bootstrap consistently.

- `local_storybook_audit_installability_drift` — A fresh high-risk `npm run verify:change` on `fix/preview-migration-provider-race`, unchanged exact pre-commit HEAD `59196dc1b140005148afefe8fe98fc360956d8f5`, failed at `2026-09-02T23:02+09:00` only in the final governed full-tree dependency audit. Documentation governance for 81 documents, trusted-install and cross-platform policy, canonical lock verification, validation of 196 migrations, lint, CI typecheck, 1,676 Node tests with 1,668 passes and eight intentional skips, and 103/103 Vitest tests with 98.5% statement and 94% branch coverage all passed first. The Production audit remained empty and the earlier `sanitize-html` and `browserslist` findings were absent. npm then changed both existing Storybook-only `image-size@2.0.2` advisories from `fixAvailable: false` to `fixAvailable: true`; direct registry inspection still showed `image-size@2.0.2` as latest, and both reviewed GitHub advisories still list no patched version. The apparent replacement path is not coherently installable: `@storybook/nextjs-vite@10.6.0` now expects `storybook@^10.6.0`, while the installable root `storybook` release remains `10.5.10`, and an immutable `npm audit fix --dry-run --ignore-scripts` diagnostic failed with that peer-resolution conflict before changing files. Classification is npm advisory remediation-metadata drift around a partially published development-only Storybook release, not a Production exposure, application assertion, workflow, migration, provider, or lock-integrity failure. The correction keeps the two exact advisory URLs allowlisted but permits npm's boolean fix signal only through `2026-09-16T00:00:00.000Z`; the exception automatically fails closed at expiry, an unknown advisory still fails immediately, and Production advisories remain unconditionally blocking. No commit, push, PR, provider check, deployment, or database mutation occurred. Acceptance requires focused policy tests for active and expired states, a passing live governed audit, and a fresh complete `verify:change`. Remaining risk is a coherent stable Storybook fix becoming available before the exception expires, so dependency remediation must be revisited no later than the encoded deadline and before Production promotion.

- `public_storybook_audit_installability_drift_pr429` — PR #429 exact head `6f10cfd82e84a07661a67e95fd63857bfa53c88d` produced Public Readiness run `33639175959` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/33639175959/attempts/1)), local workflow `.github/workflows/public-readiness.yml`, `pull_request`, created `2026-09-02T13:59:45Z`, updated `14:03:05Z`, and concluded `failure`. Job index 0 succeeded. Job index 1 / database ID `100277723099` failed at step index 4 / number 5 after setup steps 0–3 succeeded; step indices 5–7 / numbers 6, 7, and 13 were expected after-failure or profile skips, while post steps 8–9 / numbers 14–15 succeeded. Job index 2 / database ID `100278716608` then failed closed at step index 3 / number 4; step index 4 / number 7 was skipped and post steps 5–6 / numbers 8–9 succeeded. Annotation collection was complete with two failure annotations, job 1 at line 1929 and job 2 at line 19; both had message and path present, title absent, and matched no fixed annotation signature. Identity, terminal metadata, full-log retrieval, and audit completeness passed. Safe fixed-pattern inspection found the blocking development-advisory marker once at line 2526 and the two exact Storybook `image-size` advisory URLs once each at lines 2527–2528; GitHub-error signatures appeared twice at lines 2542/2759, while retry, flaky, timeout, Next full-reload, migration, and test-summary signatures were absent because the fail-closed audit stopped the gate before those later phases. The sibling Cross-Platform run `33639176010`, app-owned `Supabase Preview` check, and Vercel Preview status all succeeded on the same SHA, rejecting platform bootstrap, migration replay, deployment, and provider availability as causes. Classification is the same repository-governed npm remediation-metadata drift recorded by `local_storybook_audit_installability_drift`: both official advisories still expose no patched `image-size` version, while npm temporarily advertises a not-yet-coherent Storybook replacement. The failed attempt is immutable and will not be rerun. The correction on independent branch `fix/preview-migration-provider-race` preserves unconditional Production blocking, allows only the two exact development advisory URLs with npm's boolean fix signal through `2026-09-16T00:00:00.000Z`, fails unknown or expired exceptions closed, and is covered by active/expired policy tests. PR #429 remains open and untouched; replacement-branch first-attempt Public, Cross-Platform, provider, and Vercel evidence are pending, `dev` remains at `59196dc1b140005148afefe8fe98fc360956d8f5`, and `main` is out of scope. Remaining risk is a coherent Storybook remediation becoming installable before expiry, so the exception must be removed when that occurs or no later than its encoded deadline.
- `supabase_setup_cli_migration_failure_false_positive` — Apply Production Supabase Migrations run `33604488497` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/33604488497/attempts/1)), local workflow `.github/workflows/production-migrations.yml`, `workflow_dispatch`, exact reviewed `main` SHA `228d238cfd79912acaf43a588dee51888444fc77`, ran from `2026-09-02T07:37:17Z` through `07:38:22Z` and concluded `success`. Apply job index 0 / database ID `100165272213` and all 17 executed steps succeeded; recovery-guidance job index 1 / database ID `100165534468` was the expected success-path skip. Annotation collection was complete with zero annotations, and identity, terminal metadata, full-log retrieval, and audit completeness all passed. The structural auditor reported `migration_failure` twice at fixed lines 305 and 313, both inside setup-cli step index 6 / number 7. A bounded in-memory classification retained no provider text and proved both matches were echoed action implementation control-flow source, not an emitted migration failure; the same count, positions, step, and source category appeared in prior successful Production migration run `32930707927`. The reviewed-target guard, migration validation, pre-apply list, dry run, immediate live-`main` recheck, apply, and post-apply list all succeeded, and read-only Production verification confirmed migration `20260902150504` plus the corrected six-argument approval function. This is an exact non-actionable third-party action source-text baseline, not a schema, migration, database, credential, network, or product failure. Until the classifier can distinguish action source from runtime output without weakening detection, accept only this exact two-match setup-step context when every downstream migration step and final parity succeed; any changed count, location, action version, failed/skipped required step, annotation, or parity result is abnormal. Rollout: the hotfix is complete on `main`; this sanitized baseline record accompanies the required back-merge to `dev`. Remaining risk is a real migration error sharing the same wording, so downstream step conclusions and database parity remain mandatory.

- `local_json_ld_windows_file_url_path_failure` — The first local `npm run verify:change` for the Issue #427 `dev` back-merge, exact pre-correction HEAD `f9b0dd7fb0cf65dce47225d3bd52ec8245852a78`, failed on `2026-09-02T22:51+09:00` before commit and before `receive-pack`. Repository Knowledge, install policy, cross-platform policy, canonical lock verification, 197 migrations, lint, and semantic typecheck passed; the Node suite reported 1,670 passes, one failure, and eight intentional skips out of 1,679 tests. The sole failure was the JSON-LD sink security contract on Windows: it returned file URL pathnames and reused them as native paths, producing a duplicated drive prefix before reading the first sink. This is the same repository-controlled URL/path conversion class as `cross_platform_windows_file_url_path_failure`, not a JSON-LD serializer, application sink, dependency, migration, GitHub, Vercel, Preview, or Production defect. The correction converts both discovered file URLs and the repository root with `fileURLToPath`, while preserving native `relative` display paths. The focused JSON-LD contract passed 2/2, changed-file lint passed, and the cross-platform policy passed locally. The fresh high-risk `npm run verify:change` then passed Repository Knowledge, install and cross-platform policy, canonical lockfile, all 197 migrations, lint, semantic typecheck, 1,671/1,679 Node tests with eight intentional skips, 103/103 unit tests, coverage thresholds, and the zero-advisory Production dependency audit; only the tracked unpatched Storybook `image-size` advisories remained. No remote branch, workflow run, deployment, or database mutation was created by the failed attempt. Rollout remains local on `hotfix/427-backmerge-dev`; `dev` is unchanged. Remaining risk is another test returning `.pathname` across a native filesystem boundary, so the remote Windows matrix remains required.

- `public_storybook_image_size_audit_metadata_transition` — PR #429 Public Readiness run `33639175959` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/33639175959/attempts/1)), local workflow `.github/workflows/public-readiness.yml`, `pull_request`, exact feature SHA `6f10cfd82e84a07661a67e95fd63857bfa53c88d`, ran from `2026-09-02T13:59:45Z` through `14:03:05Z` and concluded `failure`. Classifier job index 0 succeeded. Change-aware verification job index 1 failed at step index 4 / number 5; the release and downstream post-setup steps at indices 5–7 were expected after-failure skips, while post steps 8–9 succeeded. CI Policy Gate job index 2 then failed closed at step index 3 / number 4; its post-setup step was the expected after-failure skip. Annotation collection was complete with two failure annotations: job 1 at line 1929 and job 2 at line 19, both with title absent and message/path present, and neither matched a fixed annotation signature. Identity, terminal metadata, full-log retrieval, and audit completeness all passed; the only fixed signature was `github_error` twice at lines 2542 and 2759. Repository Knowledge, trusted install, cross-platform policy, canonical lockfile, all 197 migrations, lint, semantic typecheck, 1,671/1,679 Node tests with eight intentional skips, and 103/103 unit tests passed before the development dependency audit failed. Bounded dependency inspection proved that the two already tracked development-only `image-size@2.0.2` advisories changed from npm `fixAvailable: false` to `true` after a newly published Storybook adapter line replaced that transitive parser, while Production dependencies remained unaffected. This is an external advisory/registry metadata transition exposing a repository dependency-policy update requirement, not an approval RPC, schema, test assertion, Supabase Preview, Vercel, Windows, macOS, or Production-runtime failure. The first correction candidate pinned the matching Storybook `10.6.0-beta.3` family and removed `image-size`, but a clean-browser full Storybook run produced seven interaction regressions, so the candidate was rejected and its package changes were fully removed before commit. Stable `storybook@10.6.0` was still unpublished even though the adapter package existed, making npm's boolean fix signal temporarily non-installable as a compatible stable family. The repository correction therefore keeps the exact two development-only advisory keys, permits npm's patchable boolean only until `2026-09-09T00:00:00Z`, and fails closed again at the deadline; production advisories, unknown advisories, and any other patchable development advisory remain blocking. Focused policy coverage passed 4/4 and proves both pre-deadline acceptance and deadline failure; the standalone dependency audit and canonical lockfile check passed. A fresh high-risk `npm run verify:change` then passed Repository Knowledge, install and cross-platform policy, canonical lockfile, all 197 migrations, lint, semantic typecheck, 1,671/1,679 Node tests with eight intentional skips, 103/103 unit tests, coverage thresholds, zero Production advisories, and only the two exact time-bounded development advisories. The original run was not rerun, cancelled, or deleted. Supabase Preview, Vercel Preview, Windows x64, and macOS arm64 all succeeded on the failed SHA; replacement-SHA first-attempt Public Readiness and provider evidence are pending, PR #429 remains unmerged, and `dev` is unchanged. Remaining risk is delayed stable Storybook publication, so the bounded grace cannot be extended without a new audit and the dependency must be upgraded before expiry.

- `preview_migration_provider_check_concurrency_collision` (recurrence and recovery) — PR #429 merged to `dev` as exact SHA `2e472fb7d78cd824485787456a2ef4b4340e5988`; Apply Preview Supabase Migrations run `33643161836` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/33643161836/attempts/1)), local workflow `.github/workflows/preview-migrations.yml`, `push`, was created `2026-09-02T14:36:39Z`, updated `14:37:34Z`, and concluded `failure`. Job index 0 / database ID `100291085412` failed: step indices 0–8 / numbers 1–9 succeeded, step index 9 / number 10 failed, step index 10 / number 11 was the expected after-failure skip, step index 11 / number 20 succeeded, step index 12 / number 21 was the expected post-setup skip, and step indices 13–14 / numbers 22–23 succeeded. Annotation collection was complete with one failure annotation at line 22 whose title was absent and message/path were present; fixed annotation signatures were empty. Identity, terminal metadata, full-log retrieval, and audit completeness all passed. Fixed signatures were `migration_failure` seven times at lines 39/53/320/328/600/604/605 and `github_error` once at line 605. The same SHA's pinned Supabase GitHub App check run `100291407641` started `14:37:13Z`, completed successfully at `14:37:31Z`, and overlapped the repository workflow's migration list at `14:37:21Z`–`14:37:24Z` plus apply at `14:37:24Z`–`14:37:31Z`. This is an exact recurrence of the previously proven concurrent-writer class, not a migration parser, filename, stale-ref, credential, provider-availability, dependency, or application failure; the old `dev` workflow still lacked the pending provider-wait correction. The failed attempt was preserved, but an external operator reran the unchanged SHA as [attempt 2](https://github.com/MyKnow/ssartnership/actions/runs/33643161836/attempts/2), created `14:42:56Z`, updated `14:43:47Z`, after the provider check had finished. Its replacement job `100293420446` and every step succeeded, annotation collection was complete and empty, post-apply migration parity passed, and four structural `migration_failure` lexical matches at lines 39/53/325/333 were unchanged echoed control-flow source rather than a runtime failure because no required step, annotation, or GitHub-error signature failed. This rerun proves Preview recovery but cannot replace the failed first attempt or prevent recurrence. The repository prevention remains PR #430's bounded exact-SHA wait for exactly one `Supabase Preview` check from pinned App ID `330661`, with fail-closed publisher, ambiguity, conclusion, absence, and timeout handling before any database access. Rollout: the migration itself is recovered on current `dev`; the concurrency prevention and this ledger extension remain on `fix/preview-migration-provider-race`, PR #430 is open, and `main` is out of scope. Remaining risk is another migration-bearing `dev` push before PR #430 merges, which can still race and must not be retried into clean first-attempt evidence.

- `e2e_canonical_partner_fixture_expired_wall_clock_pr430` — PR #430 exact head `e3a6e358756fd98771caee99241297368c51f4a7` produced Public Readiness run `33641241303` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/33641241303/attempts/1)), local workflow `.github/workflows/public-readiness.yml`, `pull_request`, created `2026-09-02T14:18:57Z`, updated `14:32:19Z`, and concluded `failure`. Job index 0 succeeded. Job index 1 / database ID `100284666990` passed setup and classified verification at step indices 0–4 / numbers 1–5, took the intended high-profile skip at step index 5 / number 6, then failed at step index 6 / number 7 during the Production build and retry-free full E2E; post-Setup Node step index 7 / number 13 was the expected after-failure skip and post steps 8–9 / numbers 14–15 succeeded. Job index 2 / database ID `100289429829` failed closed at policy step index 3 / number 4; step index 4 / number 7 was skipped and post steps 5–6 / numbers 8–9 succeeded. Annotation collection was complete with two failure annotations, job 1 at line 501 and job 2 at line 19; both had message and path present, title absent, and matched no fixed annotation signature. Identity, terminal metadata, full-log retrieval, and audit completeness passed. Fixed signatures were `failed_attempt_marker` three times at lines 2798/2829/2880, `test_failure` once at line 3016, `github_error` twice at lines 3033/3250, and the two reviewed Next development full-reload diagnostics at lines 2889/2891; retry, flaky, connection-refused, browser-console, audit, migration, and deployment signatures were absent. Bounded in-memory classification mapped the three retry-free failures to the repository-owned canonical partner login-return, public smoke, and detail-introduction scenarios: the smoke response stayed below 500 but rendered the not-found state, the login assertion saw a noncanonical header link, and the detail hero was absent. An exact local repository probe on the immutable SHA showed that canonical mock partner `health-001` ended on `2026-08-31` and `getPartnerById` returned null on `2026-09-02`, proving a wall-clock fixture expiry rather than a product component, router, dependency, build, hydration, server availability, provider, or test-retry defect. Same-SHA Cross-Platform run `33641241334` passed Windows and macOS with complete empty annotations and zero signatures, the Vercel Preview deployment reached READY, and the app-owned Supabase check was the expected no-schema skip. The failed run is preserved and will not be rerun. The correction extends only this canonical mock fixture to `2099-12-31`, adds a Node contract that requires the exact far-future end and a non-null public detail lookup, and records the reusable wall-clock fixture rule in the Actions skill. PR #430 remains open on the failed SHA; a replacement commit and clean first-attempt Public Readiness, Cross-Platform, provider, and Vercel evidence are pending, `dev` remains at `59196dc1b140005148afefe8fe98fc360956d8f5`, and `main` is out of scope. Remaining risk is another canonical mock route backed by a short-lived fixture, so future expiring scenarios must inject a fixed clock rather than depend on execution date.

- `public_humanfs_node_symlink_copy_advisory` — PR #430 exact head `b9bc1c1f4d6c541ade5776787dc8306f91c1e7fa` produced Public Readiness run `33645040761` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/33645040761/attempts/1)), local workflow `.github/workflows/public-readiness.yml`, `pull_request`, base `2e472fb7d78cd824485787456a2ef4b4340e5988`, created `2026-09-02T14:53:53Z`, updated `14:56:36Z`, and concluded `failure`. Job index 0 / database ID `100297467599` and all seven recorded steps succeeded. Job index 1 / `100297558135` succeeded at step indices 0–3 / numbers 1–4, failed at classified-verification index 4 / number 5, expectedly skipped downstream indices 5–7 / numbers 6, 7, and 13, and succeeded at post steps 8–9 / numbers 14–15; Production build and both E2E paths never ran. Job index 2 / `100298368552` succeeded at indices 0–2 / numbers 1–3, failed closed at policy index 3 / number 4, expectedly skipped index 4 / number 7, and succeeded at post steps 5–6 / numbers 8–9. Annotation collection was complete with two failure annotations, job 1 at line 1934 and job 2 at line 19; both had message/path present, title absent, and no fixed annotation signature. Identity, terminal metadata, full-log retrieval, and audit completeness passed. Fixed signatures were only `github_error` twice at lines 2554/2771; retry, flaky, timeout, transport, browser, Next full-reload, migration/provider, Storage skip, and GitHub-warning signatures were absent from the executed log. Repository Knowledge for 81 documents, install and cross-platform policies, the canonical Node `24.18.1` / npm `11.16.0` lock, 197 migrations, lint, typecheck, 1,672/1,680 Node tests with eight intentional skips, and 103/103 Vitest tests passed before the fail-closed development audit found newly published moderate advisory `GHSA-p498-v437-472g` on `@humanfs/node@0.16.7`; Production advisories remained zero and the two existing Storybook `image-size` advisories matched their explicit policy. GitHub's reviewed advisory marks versions below `0.16.8` vulnerable and `0.16.8` patched; the lock's `eslint@9.39.4` dependency accepts `@humanfs/node@^0.16.6`, so the narrow correction refreshes only the compatible transitive HumanFS lock family and keeps ESLint unchanged. This is dependency/advisory-database drift after the preceding local gate, not an application, test assertion, build, migration, provider, deployment, or Production-runtime failure. Independent source reconciliation also found that this SHA had reintroduced a superseded `2026-09-16` Storybook exception while resolving the latest `dev`; the replacement restores `dev`'s stricter `2026-09-09` policy, but that source drift did not cause this run's failure. Same-SHA Cross-Platform run `33645040721` passed Windows and macOS on attempt 1 with complete empty annotations and zero signatures, Vercel deployment `4eJbkvDwBPsWpU7EcgkBBa4pjXMH` reached READY, and the one pinned Supabase App ID `330661` check was the expected no-schema skip. The corrected tree then completed a trusted zero-lifecycle install, canonical lock verification, 32 focused workflow/fixture/audit-policy tests, a zero-finding Production audit, and the governed full-tree audit with only the two exact Storybook exceptions expiring `2026-09-09`. The failed Public attempt is preserved and will not be rerun. Acceptance requires a fresh complete high-tier Public first attempt on a new SHA. Rollout: correction is local on `fix/preview-migration-provider-race`; PR #430 remains open, `dev` stays at `2e472fb7d78cd824485787456a2ef4b4340e5988`, and `main` is out of scope. Remaining risk is another advisory-database advance between local and remote execution, which remains fail-closed.

- `public_auth_signup_mm_placeholder_contract_drift_pr431` — PR #431 exact head `54a0020077ad356171736e3b362148fc18e89ebd` produced Public Readiness run `33653101441` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/33653101441/attempts/1)), local workflow `.github/workflows/public-readiness.yml`, `pull_request`, base `51e093c66d3d9fb81223d44e8acb3a36fe7d77ad`, created `2026-09-02T16:08:59Z`, updated `16:21:35Z`, and concluded `failure`. Classifier job index 0 / database ID `100324804122` and all steps succeeded. Change-aware job index 1 / `100324900434` passed setup, trusted install, classified verification, and Production build, then failed at step index 6 / number 7 during the retry-free full E2E; its post-setup skip and cleanup steps were expected. CI Policy Gate job index 2 / `100329176940` failed closed at step index 3 / number 4. Annotation collection was complete with two failure annotations, both with title absent and message/path present; identity, terminal metadata, full-log retrieval, and audit completeness passed. Fixed signatures were `failed_attempt_marker` once, `test_failure` once, `github_error` twice, and the two reviewed Next development full-reload diagnostics. The full suite reported 96 passes and one failure: `tests/e2e/auth-ops.spec.ts` still expected the signup Mattermost input placeholder `예: myknow`, while the reviewed UI source renders `예시: myknow`. This is a stale E2E text contract introduced while reconciling the current UI wording, not an application, authentication, signup routing, build, dependency, migration, provider, Vercel, or cross-platform defect. The failed attempt is immutable and will not be rerun. The correction updates only the exact placeholder assertion, then requires focused auth E2E plus the complete local change gate and a fresh first-attempt Public run on a new SHA. Same-SHA Cross-Platform and Vercel checks succeeded; Supabase Preview was the expected no-schema skip. Rollout remains on `feat/418-required-member-email-gate`; `dev` and `main` are unchanged. Remaining risk is another stale copy assertion in the full auth journey, so the complete retry-free auth suite remains mandatory before merge.

- `public_e2e_three_route_timeout_cluster_pr431` — PR #431 replacement head `834715ab3abfc5397debfaf3f231ede3a180e47d` produced Public Readiness run `33655156602` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/33655156602/attempts/1)), local workflow `.github/workflows/public-readiness.yml`, `pull_request`, base `51e093c66d3d9fb81223d44e8acb3a36fe7d77ad`, created `2026-09-02T16:28:45Z`, updated `16:40:09Z`, and concluded `failure`. Classifier job index 0 / database ID `100331766273` succeeded. Change-aware job index 1 / `100331867618` passed setup, trusted install, classified verification, and Production build, then failed at step index 6 / number 7 during the retry-free full E2E; CI Policy Gate job index 2 / `100335578135` failed closed as designed. Annotation, identity, terminal, full-log, and audit collection were complete. Fixed signatures were `failed_attempt_marker` three times, `test_failure` once, `github_error` twice, `timeout` three times, and the two reviewed Next development full-reload diagnostics. The suite reported 94 passes and three independent 30-second timeouts with no assertion call log or stack: public smoke `/auth/reset`, mobile partner-image swipe, and standalone PWA shell. The failures were separated across ordinals 42, 88, and 92; adjacent scenarios passed, including `/auth/reset/complete`, the following carousel breakpoint case, and the ordinary-browser PWA case. The unrelated surfaces and absence of a failed assertion indicate a browser/server lifecycle or cold-compilation timeout cluster rather than one shared product contract; the affected reset route still requires isolated reproduction because it is in this PR's scope. Same-SHA Cross-Platform run `33655156892` passed Windows and macOS on attempt 1 with complete empty annotations and zero signatures; Vercel and Supabase provider checks were non-blocking and no schema mutation was requested. The failed Public attempt is immutable and will not be rerun. Before any new remote trigger, preserve this record, run the three exact scenarios against a fresh local server, and require the complete retry-free E2E suite plus a fresh first-attempt Public run on a new SHA. Rollout remains on `feat/418-required-member-email-gate`; `dev` and `main` are unchanged. Remaining risk is a deterministic slow readiness boundary hidden by the aggregate timeout, so any local recurrence must be corrected instead of widening or retrying the test timeout.

- `local_mock_promotion_image_null_response_pr431` — The fresh-server local reproduction for the three `public_e2e_three_route_timeout_cluster_pr431` scenarios on exact source `834715ab3abfc5397debfaf3f231ede3a180e47d` plus its uncommitted ledger record passed all three retry-free cases: `/auth/reset` in 8.3 seconds, mobile carousel swipe in 7.4 seconds, and standalone PWA shell in 7.0 seconds. The process exited 0 after 41.5 seconds, but the mock web server emitted one non-blocking invalid-image diagnostic when a public promotion-slide proxy request returned no usable image body. No assertion, navigation, timeout, browser-console, dependency, schema, provider, or production request failed, and the affected test does not use that image as its readiness contract. This is a mock fixture/external-media diagnostic, not acceptance evidence for the prior remote timeout cluster and not a reason to suppress image-proxy failures globally. Before the next remote trigger, preserve this record and require a complete local retry-free E2E run; any recurrence that blocks page readiness or changes the process exit must be treated as a product/test failure. Rollout remains local on `feat/418-required-member-email-gate`; `dev`, Preview data, `main`, and Production are unchanged. Remaining risk is external mock media latency contributing to aggregate suite variance, so semantic locators—not remote image completion—must remain the route readiness boundary.

- `local_mock_promotion_image_null_response_pr431` (full-suite continuation) — The required fresh-server `npm run test:e2e:ci` on the same source completed normally with all 97 retry-free tests passing in 7.1 minutes. The three remote-timeout scenarios passed inside their original suite positions: `/auth/reset` in 4.1 seconds, mobile carousel swipe in 1.8 seconds, and standalone PWA shell in 2.1 seconds. The server again emitted repeated non-blocking invalid-image diagnostics for the same public promotion-slide object and the two already reviewed administrator-transition Next development full reloads; no test, worker, route assertion, navigation, teardown, or process exit failed. This rejects a deterministic regression in the PR's authentication UI and preserves the mock-media signal without treating a successful local run as replacement for failed remote evidence. The next remote trigger must use a new ledger-bearing SHA and a fresh first attempt; any timeout recurrence blocks merge and requires correcting the readiness or fixture boundary rather than rerunning. `dev`, Preview data, `main`, and Production remain unchanged.

- `local_graduate_cohort_promotion_contracts_issue432` — Local preflight on 2026-09-05, branch `fix/graduate-recovery-ui` over `cb78cf313b2942ed7ae03e47034cdf254e4fa20c`, before any remote trigger, found an optional-campus TypeScript mismatch and unsupported plan frontmatter (`execution-plan`/`supporting`); the first `verify:change` stopped at documentation validation. Review-first regressions also exposed the 2018-12 first-cohort boundary, lost field-specific error copy, primitive JSON causing the legacy-key guard to throw, and file-only correction reporting the incoming rather than saved cohort. These are local implementation/test-contract defects, not provider failures. The correction shares optional-campus validation, uses registered `implementation-plan`/`normative` metadata, validates object shape, preserves the historical boundary and field errors, and reports persisted generation. New route/domain/service tests cover both request kinds, valid education correction, locked file-only education, invalid cohorts, and expired/unverified challenges. The initial service fixture used an unsupported campus and was corrected to the canonical directory; array JSON cases are wrapped to avoid Vitest table-argument spreading. Rollout remains local; rerun focused checks and the full change/release gates before the first push. No remote attempt was rerun, cancelled, or deleted.

- `local_graduate_cohort_promotion_contracts_issue432` (schema-order continuation) — The next local Quick gate passed docs, migration filenames, lint and semantic typing, then stopped at the existing production-migration hygiene regression: appending ALTER TABLE after the read-model parity section violated snapshot dependency order. Preserve this failure separately from the initial documentation failure. The final phase-1 snapshot block is moved before the parity section without changing an applied migration or weakening the ordering test. The full Node suite had 1,683 passes, one failure and eight configured skips; focused schema tests and the complete gate must pass before publication. The pre-existing ignored transactional-email prototype produces one unused-variable lint warning and is outside this change. No remote trigger occurred.

- `local_storybook_stale_dependency_cache_issue432` — On 2026-09-05 the first local 36-case graduate visual run reused a Storybook process started before `install:trusted`. Its first case timed out with an empty root and a failed dynamic import of a generated Storybook dependency. The next case was interrupted after the root cause was visible; both error contexts were inspected, and exit 130 plus fixed failure metadata were retained under ignored QA artifacts. Independent fresh Vitest Storybook validation passed all 21 affected interaction cases. Restart only the task-owned Storybook server after dependency replacement, then run the same canonical visual tests without retries or baseline regeneration; do not treat HMR state on an already-open tab as proof a fresh page loads. No remote trigger occurred.

- `local_graduate_cohort_promotion_contracts_issue432` (validated) — Phase-1 source commit `1ba24f66` passed the full local release gate: 1,684 Node tests with eight configured skips, 129 unit cases, 103 built routes, and all 98 Playwright cases in 3.3 minutes with zero retries or browser console errors. The complete log was inspected. There were 24 occurrences of the previously reviewed mock promotion-image null diagnostic and the same two between-admin-test Next development reload notices; no readiness, assertion, teardown, or process failure occurred. Fresh-server canonical visual verification passed all 36 cases without updating baselines, and 21 affected Storybook interaction cases passed. PostgreSQL 17 synthetic legacy rows retained cohorts and correction targets; a generation-only insert succeeded and a missing-cohort/period fixture failed closed with its transaction intact. These local results are not remote Preview or Production proof; exact-SHA remote checks remain required.

- `e2e_partner_introduction_shared_viewport_budget_pr433` — PR #433 Public Readiness run `33961612701` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/33961612701/attempts/1)), local workflow `.github/workflows/public-readiness.yml`, `pull_request`, exact head `541f5e0cf39722e0e67cb1287b42d1d17aac32ec`, ran from `2026-09-05T10:47:54Z` through `11:00:28Z` and concluded `failure`. Classifier job index 0 succeeded. Verification job index 1 / database ID `101294379952` passed setup and Quick verification, intentionally skipped smoke step index 5 / number 6, failed full build/E2E step index 6 / number 7, skipped post-setup index 7 / number 13, and completed cleanup. Policy job index 2 / `101295740352` failed closed at step index 3 / number 4 and took its expected post-setup skip. Annotation, identity, terminal metadata and full-log collection were complete; two failure annotations at lines 400 and 19 had message/path present, title absent and no fixed annotation signatures. Fixed signatures were `failed_attempt_marker` once at line 2893, `test_failure` once at 2928, `github_error` twice at 2943/3160, `timeout` once at 2912, and the two reviewed between-admin-test `next_dev_full_reload` notices at 2902/2904. The full suite reported 97 passes and one whole-test 30-second timeout in the partner-detail introduction scenario, without an assertion, call log, transport error or available uploaded trace artifact. Source inspection found six independent viewport journeys sharing that single budget. A fresh local execution of the unchanged scenario passed in 12.4 seconds including setup, so no deterministic product failure was reproduced; the precise final blocked browser operation is unavailable. The narrow repository correction registers one independent test per viewport with all original assertions, semantic readiness and the unchanged default timeout. It adds no retry, sleep, slow mode or timeout inflation and adds a source contract for that separation. This addresses the shared-budget weakness while making any future individual viewport failure identifiable; it does not claim to prove an unavailable trace's exact operation. Same-SHA Windows/macOS, Supabase Preview and Vercel passed. The failed attempt is preserved without rerun. Replacement local full verification and new-SHA first-attempt remote evidence are required before merge; dev and main remain unchanged.

- `e2e_partner_introduction_shared_viewport_budget_pr433` (local correction verified) — The source regression failed on the original single-test loop, then all four focused Node assertions and changed-file lint passed after the split. Review confirmed the original browser assertions are identical apart from test registration and indentation; the added contract uses the repository's Node test runner. Full local `verify:release` passed 1,686 Node tests with eight configured skips, 129 unit cases, all 103 built routes and 103 retry-free Playwright cases in 3.1 minutes. The six independent viewport cases all passed. Complete log inspection found only the same 24 mock promotion-image null diagnostics, two between-passing-admin-test reload notices, and the existing ignored prototype lint warning; there was no test timeout, failure or browser console error. This validates the local correction, not the pending replacement-SHA remote run. No failed remote attempt was retried or deleted.

- `setup_node_cache_save_collision` (graduate Phase 1) — Apply Preview Supabase Migrations run `33963601631` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/33963601631/attempts/1)), local workflow `.github/workflows/preview-migrations.yml`, `push`, exact dev SHA `55082c824bb48d16548f073154f471ab4e58dc11`, ran `2026-09-05T11:32:28Z`–`11:33:38Z` and succeeded. The sole job and all 16 executed steps succeeded; annotations were empty and identity, terminal, annotation and full-log collection were complete. Structural signatures were `cache_collision` once at line 865 and eight lexical `migration_failure` matches: seven at 40/54/321/329/396/402/412 were echoed workflow/action failure-control source, while 865 was the same nonfatal post-setup cache reservation collision, not a database failure. The fixed provider wait at step index 8 / number 9 ran `11:33:04Z`–`11:33:27Z`; the unique pinned App ID 330661 check completed successfully at `11:33:22Z`, before migration list/apply/verification at `11:33:27Z`–`11:33:34Z`. Read-only Preview verification confirmed migration `20260905192117`, four nullable period columns, zero invalid cohorts and retained correction targets. Thus the provider-writer wait was exercised and schema parity succeeded; the remaining cache collision is the established optional-cache contention class. No cache topology, source contract, required check or data safety rule is weakened, and no rerun or artificial corrective source commit is required. Any real migration diagnostic, failed step, changed publisher or missing parity remains blocking. PR #433 is merged into dev, Vercel is READY on the same SHA, and main is unchanged.

- `e2e_partner_introduction_shared_viewport_budget_pr433` (remote correction verified) — Replacement head `58e917c3b53d792374a4515f2b382a2c200f9afa` passed Public Readiness run `33962990341` ([attempt 1](https://github.com/MyKnow/ssartnership/actions/runs/33962990341/attempts/1)): all 103 E2E cases passed, annotations and failed jobs/steps were absent, the only signatures were the two reviewed between-passing-admin-test reload notices, and full audit collection was complete. Same-SHA Windows/macOS, Supabase Preview and Vercel also succeeded. This replaces the pending acceptance boundary without overwriting the preserved failed attempt.

- `local_self_host_recovery_operator_path` — Issue #435's local Release on source HEAD `fd77cc07e741352d0f4858b94f4894454b652d66` plus uncommitted recovery tooling stopped on 2026-09-07 at cross-platform policy before lint, tests, build or E2E. The new Mac-only SSH pull tool embedded a developer home directory instead of resolving it at runtime. The correction uses the runtime home path while retaining the fixed existing wrapper and no-forwarding transport. Focused cross-platform policy and recovery contracts passed. The failed local log is retained; no push, PR, Actions trigger, cloud deployment or Production data change occurred. A fresh complete local Release remains required before publication.
- `local_lint_retained_qa_artifact_contamination` — The next Issue #435 local Release passed cross-platform policy, canonical lock and 199 migrations, then ESLint traversed previously preserved development bundles under the repository-approved ignored temporary QA directory. It reported 4,124 errors and 41,209 warnings from generated code; application and controller source were not the failing class. The flat ESLint configuration did not inherit the Git ignore boundary. Correction adds only the temporary-artifact directory to global ignores and tests the actual ESLint API: retained build output is ignored, while application and CI controller source remain checked. The regression failed before the change and passed afterwards; full lint then passed with the retained artifacts still present. The source correction preceded this complete ledger read, a local sequencing miss now recorded before another gate. No remote trigger or branch/provider mutation occurred. Preserve the failed log and require the full replacement local gate; do not remove failed artifacts or disable source rules to manufacture a pass.
- `local_self_host_ci_controller_and_cold_compile` — Issue #435's native AMD64 rootless CI used approved source `fd77cc07e741352d0f4858b94f4894454b652d66`, root-owned versioned controller inputs, 1.5 CPU, 3.5 GiB container memory and the existing 30-minute job boundary. Initial controller setup failed because it used the wrong builder home; a subsequent full gate failed E2E with video encoding active; another stopped before tests because its external Playwright configuration resolved relative paths under the controller directory. The corrections preserve actual builder home, absolute work/report paths, screenshots/traces and retry 0, and disable only video encoding. Controller v4's fresh job `202609070704` then passed install/Quick/Production build but failed E2E case 2 on page navigation: 1 passed, 1 failed, 101 unrun, total job 15m44.855s, no artifact result. Trace inspection found reset and login HTTP 200, an unfinished partner-document request, and a development compiler span of 21.794 seconds for that route; no remote image request had begun. This establishes a cold-compilation cost inside the unchanged 30-second behavior budget, not a shown product assertion or provider failure. All failed jobs and traces remain preserved. Prepare read-only route modules separately, then require a fresh complete retry-free gate with unchanged CPU/memory/test deadlines; partial or focused recovery never authorizes import. No GitHub Actions attempt exists for these home-server jobs, and Production is unchanged.
- `next_dev_fast_refresh_full_reload` (Issue #435 local diagnosis) — The captured local Release had three diagnostics: one before the first passed authentication case and two in the previously reviewed administrator transitions. Two independent fresh-server browser diagnoses, including an empty development cache, captured the startup WebSocket event with runtime-error flag false and zero page/console errors. During the explicit login-to-setup navigation, the development HMR update-manifest request was cancelled and Next's HMR fetch rejection initiated the reload. A warm-cache independent diagnosis had only an aborted HMR chunk and no reload. The pinned Next implementation confirms this non-runtime-error branch; the generic documentation's component-export examples do not prove this incident's cause. This narrows the local startup condition without suppressing logs, changing production code, or treating any runtime error as benign. A fresh full gate and exact-SHA provider/Actions review remain required for any future publication; the historical remote incident is not retroactively relabeled clean.
- `local_self_host_gate_port_collision` — Issue #435's next local Release passed Quick and Production build but refused E2E startup because port 3100 already served the existing local Compose app. The command omitted the separately selected local E2E port. Keep the app and failed log, check an unused port and use the repository's existing E2E_PORT option; never enable reuseExistingServer or stop an unrelated listener. A native focused diagnostic also accidentally used the gate image's default entrypoint, began dependency installation and was stopped without publishing artifacts; its partial dependency tree is not reused. An intact previous work directory is used only for diagnostics, never for a fresh release artifact. No Actions trigger or Production change occurred.
- `local_self_host_next_dev_parent_cgroup_pressure` — Native controller v5's empty-cache focused diagnostic prepared 19 GET modules, then stalled while compiling the next route. The dedicated builder slice reported MemoryCurrent 3,233,476,608 bytes against MemoryHigh 3,221,225,472, 57,047 high events, 731,516,928 swap bytes and no OOM kill; Next RSS was about 2.97 GiB. Next 16.2.11's actual dev CLI selects half of host os.totalmem() as max-old-space-size when no explicit value is supplied, exceeding the lower parent slice boundary. After SIGINT and TERM did not promptly stop the thrashing diagnostic, only its verified Next process was killed; work/cache/logs remain. Result was timeout, 9m52.255s, no test acceptance or artifact. The correction bounds only the CI dev compiler heap to 1536 MiB, preserves existing server/container limits and test deadlines, and requires a new cold-cache diagnostic followed by a full gate. Separately the corrected local Release on port 3150 completed: 1,769 Node passes and eight configured skips, 133 unit passes, 103 E2E passes, no retry; full-log signature scan found only the already diagnosed development reload notices. No remote CI, cloud or Production state was changed.
- `local_self_host_next_dev_parent_cgroup_pressure` (bounded batches) — Controller v7's 1536 MiB heap alone timed out during registration preparation after 4m7.788s. Controller v8 also disabled Node development stack source maps: registration prepared in 48.115s, but accumulated modules subsequently exhausted the V8 heap; the fatal allocation failure and 4m55.354s unsuccessful run remain preserved. This is not an application acceptance result. Keep the host's mandated resource limits and split the complete E2E inventory into sequential fresh-server processes with batch-specific read-only compiler preparation. The actual 103-test inventory was verified as an exact, duplicate-free partition; passing results must cover precisely those IDs once, without retries or skips. Unknown files require an explicit preparation plan. Full native execution and complete release artifacts are still pending; no failed result authorizes import or deployment.
- `local_self_host_native_browser_load_pressure` — Diagnostic v9 timed out with a cold Web Vitals module; v10 reported empty manifests, and v11 returned a login 500 with incomplete JSON after one passing case. The actual Next implementation can restart its dev process above its heap threshold, but the available v10/v11 logs do not prove that specific cause. Controller v12 uses sixteen exact-coverage batches, test-specific preparation, health-route readiness, a 2048 MiB compiler heap and captured server stdout with restart/manifest/OOM rejection. It passed one case but failed the next at the initial partner navigation (30-second test deadline): document 200, local page chunk approximately 10.4 seconds, 12 MiB development main-app chunk unfinished. Continuous trace JPEG frames were also recorded throughout; their causal contribution is not yet proven. The next diagnostic disables only trace screencasting, preserving failure screenshots, DOM/network/action/source traces, all assertions and original deadlines. No diagnostic pass substitutes for a fresh full gate, and no artifacts were accepted or deployed.
- `local_self_host_native_browser_load_pressure` (entry lifecycle) — Controller v13 still failed with a login JSON/manifest error after one passing case (2m16.076s), so disabling trace screencasting did not resolve the failure. The installed Next 16.2.11 source retains five recent pages and disposes older entries after 60 seconds; cold preparation plus browser setup can exceed that interval. A new isolated-CI-development-only config retains sixteen pages for the ten-minute batch bound. Production and ordinary CI/dev defaults are protected by an executable subprocess config test (expected red before implementation). This is a targeted lifecycle hypothesis, not a proven root cause or deployment acceptance; server restart/error guards and original test deadlines remain required.
- `local_self_host_ci_config_test_type` — The local Release stopped at semantic typecheck because the new subprocess config test accepted only string-valued records while its inferred environment union included absent keys. A focused correction using NodeJS.ProcessEnv exposed Next's narrower NODE_ENV augmentation; the helper now accepts explicit string-or-undefined environment values, matching the child-process boundary. The focused behavior and semantic checks pass; the original log remains, and a fresh Release is required. No build, E2E, remote trigger or deployment was reached in the first failed invocation.
- `local_self_host_native_browser_load_pressure` (retention not sufficient) — Diagnostic v14 retained prepared entries but still returned the login JSON/manifest error after one pass (2m25.789s). This disproves retention alone as a sufficient fix. A content-free parse diagnostic records only input byte count and call frames while rethrowing the original exception; it neither retries parsing nor accepts a failed test. Native release output and deployment remain unavailable.
- `local_self_host_native_browser_load_pressure` (confirmed empty build manifest) — v15 failed while loading a manifest for mock reset. v16 recorded an empty read of Next's generated `build-manifest.json` and a zero-byte JSON.parse input in `loadManifest` while preparing the login page; it failed after one pass in 2m18.629s. The ineffective onDemandEntries config and its experiment-only test were removed. Separately the root layout mounted the new self-hosted telemetry component in every non-Vercel environment, including mock E2E, causing unnecessary configuration GETs and background dev compilation. The reviewed correction mounts it only for non-Vercel Supabase builds, preserves runtime enablement in real self-hosted mode, and removes that unused module from mock compiler preparation. This fixes a demonstrable mock telemetry contract violation; its causal effect on the manifest race still needs fresh native evidence. No parse retries, framework patches, timeout relaxation or partial release acceptance were added.
- `local_partner_login_redirect_race` — The local Release after the mock telemetry guard failed the company-scoped flow with ERR_ABORTED on its explicit company-page goto. The trace showed the preceding URL assertion matched the still-present login path because it accepted any /partner prefix, followed by the real login redirect through /partner while the test started another navigation. The existing application redirects a single-company account directly to its company page. Correct the test to require that exact final pathname before checking the session cookie and rendered heading; remove only the redundant competing goto, preserving all company/service/edit assertions and deadlines. Retain the failed full log and trace; a fresh focused flow and full retry-free Release are required. The native v17 diagnostic separately timed out at partner-page readiness after one pass, so the mock guard is not yet proof of native CI recovery.
- `local_partner_login_redirect_race` (local verification) — The corrected fresh-server focused flow passed 1/1; its direct invocation retained the NO_COLOR/FORCE_COLOR tooling notices. The canonical fresh Release then passed: 1773 Node tests plus eight existing skips, 133 unit tests, 103 E2E tests in 3.1 minutes, retry 0. Full 204595-byte/2328-line signature scan found only two between-passing-test development reload notices. Original failed logs/traces remain. Final compiler-route alignment additionally passed five focused tests and lint. This closes the local redirect-race verification only; native CI remains unsuccessful and no remote trigger or deployment occurred.
- `local_self_host_mac_precompile_manifest` — The explicitly approved Mac Docker AMD64 run on source ed93c05ab271c59a055665ca5cd7cc5c3cc3fcc6 passed trusted installation, Quick and the standalone build, then failed its first E2E shard with an empty development manifest, login 500 and a missing control. Six tests did not run and an out-of-test error was reported. The original archive, log and trace remain; no result or deployment artifact was published. This disproves the home-server hardware limit as the sole sufficient explanation. The next Mac-only controller uses the ordinary full-suite development-server lifecycle without preparatory page requests or 16 fresh shards; it preserves every test/project, assertion deadline, zero retries and the fail-closed report/log audit. Native server resource limits/controller behavior remain unchanged. This is an isolated lifecycle correction to verify, not a confirmed framework fix or permission to retry a failed test into green.
- `local_self_host_mac_emulated_browser` — The subsequent single-server AMD64 gate passed Quick/build and its first E2E case, then failed the canonical signup navigation's unchanged five-second URL assertion; 101 cases did not run. The retained log also reports a React state update before mount. No manifest error occurred in this run, but it is not clean evidence and no artifact was published. A separate ordinary macOS run passed all 103 tests without retry. The next bounded diagnostic separates a native ARM64 browser from the AMD64 app/test runner using the same pinned Playwright version and private shared container network. Tests/deadlines remain unchanged, and the React browser diagnostic explicitly invalidates a nominally passing gate. This isolates browser emulation as a variable; it is not a proven product fix.
- `local_self_host_mac_navigation_history` — Native-browser/AMD64-server full gate passed Quick/build and one E2E case, then failed the canonical flow's back-navigation assertion: the browser returned to the partner page rather than login. The trace records the signup URL assertion finishing about four milliseconds before goBack; markup alone does not prove a hydrated interactive destination. No result was published. A bounded diagnostic strengthens the same test with actual login/signup tab state changes before the next navigation, retaining the exact back-navigation assertion and all original deadlines. This is a readiness hypothesis under investigation, not an accepted product fix. The diagnostic uses a separately identified modified work copy; its previous approved source archive and failed trace remain unchanged.
- `local_self_host_mac_navigation_history` (focused readiness evidence) — The strengthened complete canonical flow passed 1/1 against the AMD64 development server with the native ARM64 browser in 16.4 seconds (39.8 seconds including fresh server preparation). The original back/history/returnTo/demo-login assertions and deadlines remain intact; no retry, timeout expansion or framework patch was used. This closes the focused diagnostic only. A new source commit and fresh full gate are required before artifact acceptance.
- `local_self_host_mac_full_gate_json_failure` — Exact source 76b6d963876ca12c4603b0ff63c198a8b806bc3e passed trusted install, docs, Quick (1779 Node passes, eight configured skips, 133 unit passes) and the AMD64 standalone build. At 2026-09-07 04:55 UTC the complete AMD64-server/native-ARM64-browser suite stopped after one pass: the next test's initial login preparation GET returned HTTP 500 with SyntaxError: Unexpected end of JSON input, before its strengthened interactive-readiness assertions. There was one failed case, 101 unrun cases and one out-of-test error. The server-stability audit rejected the gate; no gate/result receipt or component archives were published. This run confirms a server JSON failure, but does not independently instrument the earlier empty-manifest read or establish its exact cause. Preserve the full source/work/log/trace and do not retry parsing, suppress diagnostics or approve the successful build alone. The same SHA's ordinary macOS Release independently passed 103 E2E cases without retries in 8.7 minutes; its complete 205796-byte/2334-line scan contained only two previously diagnosed Fast Refresh notices and no manifest/transport/uncaught/before-mount signatures. The focused history correction and native browser separation are not sufficient to make the AMD64 full gate reliable. No Actions trigger, push, remote runtime deployment or Production mutation occurred.
- `local_self_host_mac_full_gate_json_failure` (bounded writer correction) — After the user reopened access, the operator verified active SSH/sudo with expiry 2026-09-07T13:00:00Z and retained the server's resource limits. The next correction uses webpack's supported outputFileSystem extension only under an explicit self-host development flag: manifest assets publish through exclusive same-directory temporary files and rename, with a shared per-path write queue. Readers, JSON parsing, asset bytes, node_modules, Production builds and every E2E assertion/deadline/retry policy remain unchanged. Eight focused cases cover old-content visibility until rename, errors/cleanup, cross-compiler order, queue recovery, collision preservation, actual Next config gating and the pinned webpack compiler path. A fresh full AMD64 gate remains required; deterministic filesystem tests do not retroactively make the failed source acceptable. The separate Next-owned prerender read-modify-write is outside this adapter's scope. No remote GitHub mutation is part of this correction.
- `local_self_host_mac_navigation_during_hmr` — Exact source ecc04568dba0bd365f936314460e3038b0d6c17a passed Quick (1787 Node passes, eight existing skips, 133 unit passes) and the AMD64 standalone build, then failed the unchanged five-second signup URL assertion at 2026-09-07 05:39 UTC. One case passed, one failed and 101 did not run; the report-level error is the explicit max-failures stop. Unlike the preceding run, this complete log has no manifest parse failure or React before-mount error. The retained trace shows the signup RSC response completed successfully in 1042 ms and its page chunk in 313 ms, followed by overlapping development HMR updates, while the URL and rendered page stayed at login. Login tab state changes had already passed. This establishes a stalled client transition, not a five-second server response or an unhydrated login form; the exact router/HMR cause remains unproven. No result or component archive was published and no server runtime was started. The same source's ordinary macOS Release passed all 103 E2E cases without retry in seven minutes; its complete 206173-byte/2342-line audit found only two reviewed between-test reload warnings. Preserve both evidence sets. A bounded separate diagnostic may compare the already reviewed read-only compiler preparation with the unchanged flow, but neither a diagnostic pass nor the atomic writer tests authorize deployment or erase this failure.
- `local_self_host_mac_navigation_during_hmr` (canonical-entry boundary) — Separate prepared-module diagnostics still failed the back-navigation assertion (one pass, one failure, five unrun), so preparation alone is not a sufficient fix. An instrumented fresh-server history observation subsequently passed two cases but changed timing and is not release evidence. Source and retained traces establish that the initial legacy partner URL performs a streamed canonical redirect; the original test waited only for visible links/fonts, allowing the next action before the replacement document finished. The correction adds a bounded five-second wait for the exact canonical URL with the default document-load condition before existing readiness/assertions; total test deadlines, the original back assertion and retry zero remain unchanged. Its source-order regression failed before the correction. Fresh uninstrumented diagnostics and a new exact-source full gate are still required; this narrows a demonstrable test lifecycle gap without claiming it proves every previous router failure's cause.
- `local_self_host_mac_navigation_during_hmr` (focused verification) — Two independent fresh-server AMD64/native-ARM64-browser runs of the corrected original first two cases passed 2/2 each, retry zero, without history instrumentation or module preparation (41.8 and 41.9 seconds total; 15.0 and 14.8 seconds for the canonical journey). Their full logs contain no manifest, transport, React before-mount or full-reload error. The second process reused its diagnostic cache, so only the first is a cold-cache observation. Both remain diagnostic evidence, not a substitute for the new commit's full 103-case gate and immutable artifact validation. Previous failed work and reports are preserved.
- `local_self_host_mac_navigation_during_hmr` (full gate still blocked) — Exact replacement source fbd7ccca79bb1883ccf76c1b55a8307f4fa87439 passed trusted install, docs, Quick and the AMD64 standalone build, but its full suite again failed browser history: after signup interactivity and goBack, the URL was the partner detail instead of login. The canonical-entry wait itself passed. The suite ended at one pass, one failure, 101 unrun and the explicit max-failures report error in 53.5 seconds; the failed flow took 21.7 seconds, so it was not the total test deadline. Repeated successful signup/partner GETs remain in the log, with no manifest parse, connection or React before-mount signature. The gate rejected it and published no result or component archives. The two prior diagnostic passes therefore do not establish a sufficient fix; neither module preparation nor canonical/hydration waits are an accepted explanation for every failure. The unchanged replacement SHA's ordinary macOS Release passed all 103 E2E cases without retry in 5.6 minutes, with a complete 206240-byte/2342-line audit showing only two reviewed administrator-transition reload warnings. Preserve the failed full source/work/trace and keep server runtime deployment blocked. Do not introduce more timing waits or change assertion/deadline/retry policy based only on a passing diagnostic. A production-runtime test architecture remains an unimplemented, separately reviewed alternative with required fixture/security/image-isolation parity.

- `live_self_host_public_auth_redirect_origin` (application correction) — The original Preview at exact deployed source `4bba2b78863b5fa8b160a1074f6763c1e059fa99` returned HTTP 307 from the public `/admin/session` bridge at `2026-09-08T11:04:57Z`, but its Location used the standalone bind origin `https://0.0.0.0:3000` instead of the public Preview origin. Public page, TLS and API parity success therefore did not establish authentication completion. The correction reuses the strictly validated operator-configured real-mode origin for proxy, admin bridge and partner logout redirects, preserves Vercel behavior and required-member gate priority, and rejects unsafe destinations without trusting Host or forwarded headers. The regression failed before the helper existed; the actual Next proxy test initially exposed a Node test-loader missing `next/server` extension mapping, corrected using the existing cache/navigation alias convention. All 44 focused redirect, origin, admin bridge and member-gate cases then passed, as did focused TypeScript and lint. Full Release, exact-SHA first-attempt Actions, immutable-image rollout and genuine public authentication checks remain required before activating the continuous receiver. No existing password, Production data or DNS record was changed by this correction; the original live failure remains distinct evidence.

- `local_auth_redirect_stale_csrf_source_contract` — The first complete local Release for the uncommitted public-origin correction stopped in Quick with 1,867 Node passes, one failure and eight configured skips. The sole failure at `tests/csrf-route-contracts.test.mts:160` hardcoded the previous `new URL(..., request.url)` spelling, although the route retained the exact same-origin-before-mutation guard and HTTP 303 See Other behavior. Unit/build/E2E did not run. Update only that source assertion to the new trusted-origin helper, retaining the exact destination, 303 status, POST-only forms and CSRF ordering, and include the whole CSRF contract file in the focused gate. Preserve `.tmp/public-auth-redirect-release-20260908.log`; do not rerun unchanged code or treat this stopped Release as success. This failure belongs to the local auth branch, not independently passing schema-guard PR #449, whose exact `f6704e1` checks were audited before its merge. A new complete auth Release and first-attempt remote verification remain required.

Correction verification for `local_auth_redirect_stale_csrf_source_contract`: all 23 combined redirect/member-gate/CSRF contracts passed, and the replacement full Release exited 0 with 1,868 Node passes/eight configured skips, 133 unit passes, standalone-capable Production build and all 103 E2E cases without retry in 3.9 minutes. Complete 214,078-byte/2,423-line output contained only the two previously reviewed between-admin Fast Refresh notices at lines 2,415 and 2,417. The previous failed 190,929-byte/2,075-line gate was fully inspected and preserved; all nine error-bearing lines belonged to that one stale assertion and its wrapper propagation. Runtime policies, status assertions and deadlines were not relaxed. Exact-source remote CI and actual public authentication remain separate release gates.

- `public_auth_qa_company_scope_fixture` — At `2026-09-08T12:00:48Z`, the first original-Preview public authentication rehearsal on deployed `29a3ca0c9af9c7ac64e59b91ddbd44ed28f774ae` stopped in its fixture-creation transaction before any login request. The preserved proof (`36bfcbe2-67de-44ee-822d-d689eb57a950`) confirms exact test UUID cleanup and unchanged original member/partner credential material. Read-only live catalog inspection established that `partner_companies.managed_campus_slugs` defaults to an empty array but its active check requires a non-empty subset of supported campuses; the test INSERT omitted this required business field. Correct only the temporary isolated company to `ARRAY['seoul']`, preserving the constraint and original rows. Psql diagnostics are limited to fixed SQLSTATE codes, and credentials/cookies stay in server memory. Do not treat the stopped fixture transaction as product authentication failure or success. Fresh actual login/bridge/logout proof remains required before enabling the receiver timer.

- `public_auth_qa_fetch_navigation_metadata` — The corrected campus fixture transaction succeeded at `2026-09-08T12:02:28Z`, and real member password login/Secure cookie plus cross-origin rejection passed, but the admin navigation bridge returned 403. The preserved proof is `16a7e646-a6c4-44d2-b5f0-91d7db25eda8`; all exact fixtures were removed and original credentials remained unchanged. A separate Node 24.18.1 loopback header-capture experiment proved that native fetch overwrites a requested `sec-fetch-mode:navigate` with `cors`. The bridge intentionally accepts document navigation only when Fetch Metadata is present, so the 403 is correct product enforcement. Use native HTTPS only for this bounded same-origin navigation GET, preserving valid TLS, actual signed cookies, mode/site/destination and response limits; do not weaken the application guard or call the server HTTP rehearsal an authenticated browser test. A new corrected rehearsal remains required.

- `public_auth_qa_admin_display_marker` — The corrected navigation rehearsal at `2026-09-08T12:04:19Z` passed real member/admin password login, non-admin denial and admin cookie issuance with public-origin bridge redirects. It stopped on the authenticated admin HTML assertion at HTTP 200 because the temporary probe expected the account display name. The deployed `AdminShell` does not pass or display that name; the established E2E contract instead checks the management-home heading and permission-filtered member navigation. Preserve proof `21a42e0f-0cc8-4efd-a5af-5eec35248b33` with successful exact cleanup and unchanged original credentials. Align only the uncommitted HTTP probe with the existing UI contract, additionally requiring the admin navigation landmark/member link and absence of admin-management navigation, streamed login redirect and server-500 marker. This is not a UI change or an authenticated browser pass; complete partner/login/logout verification is still pending.

- `public_auth_qa_readonly_navigation_contract` — The next probe (`795c45c6-daaa-49ce-8e23-d362188a3c0d`, `2026-09-08T12:05:53Z`) and separate fixed-boolean diagnostic (`5f82cb18-cb3b-41ca-b914-cea8c1e2002e`, `12:08:31Z`) confirmed both password logins/public bridges and HTTP 200 management HTML, with the heading, landmark and member link present, no server error/setup fallback/streamed redirect, and an admin-list link present. The probe's assumption that read-only must hide that link was wrong. `mapAdminSessionSnapshot` obtains canonical permissions from the code template: `readonly` grants read (including admin-management) but never create/update/delete; legacy DB template JSON is not this runtime source of truth. Preserve both stopped proofs/cleanup and do not change product permissions. Replace the ungrounded hidden-link assertion with an actual signed read-only POST to the temporary member's reset endpoint, requiring its permission-specific 403. The empty delivery body cannot perform a reset even if permission regresses, and no original account is targeted. Full HTTP authentication and cleanup must still complete before acceptance.

Final corrected HTTP rehearsal at `2026-09-08T12:10:18Z` completed on source `29a3ca0c9af9c7ac64e59b91ddbd44ed28f774ae`, proof `5174d1d5-91b9-4f04-a868-ab37ef6ac12b`: actual member/admin password logins and secure cookies, member denial/admin bridge on the public origin, management HTML, permission-specific read-only write denial, real partner server-action password login/company redirect/dashboard/POST logout and member/admin logout all passed. Exact fixtures were deleted/requeried and original member/partner password hash/salt/session-version digest remained unchanged. All preceding stopped proofs remain preserved; no product gate or permission was changed to pass the probe. This verifies authenticated HTTP behavior, not authenticated browser interaction or external email/Mattermost delivery. Receiver timer enablement followed at `12:11:16Z`; actual scheduled execution is a separate check.

The first actual scheduled receiver execution finished at `12:11:19Z` with `unchanged` and exit 0. The read-only comparison at `12:14:01Z` verified a new scheduled service start but identical container ID, image, start time and saved deployment state versus the pre-enable receipt. Proof `ssartnership-receiver-first-poll-20260908.json` is preserved on the host. No manual service start supplied this scheduled result; a subsequent new-SHA deployment remains a separate end-to-end gate.

- `local_production_export_missing_dependencies` — The fresh Issue #453 migration worktree initially ran built-in Node tests without materialized dependencies; 27 tests passed and the existing YAML workflow test failed to resolve js-yaml. Source-only production boundary tests passed. The focused rerun explicitly resolved the already trusted security worktree dependency through NODE_PATH and passed all 28. This is focused verification only; a fresh trusted install and complete gates remain required before any export workflow trigger. No Cloud capture or GitHub mutation was performed.

- `local_production_security_release_port_collision` — The first local Release for Issue #453 passed Node 1,868 tests/eight existing skips, unit 133 tests, zero-advisory dependency audit and Next 16.3.4 Production build. E2E refused to start because a pre-existing Docker listener owns loopback port 3100. No tests ran and no remote mutation occurred. Preserve the failed log, retain the unrelated container and use the existing E2E_PORT option with a verified unused port. The upgraded lint rule also warned on the existing .pkpass download; a line-specific documented exemption preserves the browser download rather than using a page router. Require clean lint and a fresh Release with zero-retry full E2E before delivery.

- `local_production_security_pwa_effect_replay` — The replacement Issue #453 Release on port 3137 passed Quick/build but failed the first installation-recommendation assertion in the existing mobile PWA scenario (102 E2E passed, one failed, zero retries). The pathname ref was updated synchronously before a scheduled animation frame; effect cleanup cancelled the frame, and replay skipped the same pathname with visible still false. Commit the pathname only inside the successful animation frame, preserving dismissal, suppressed routes and standalone policy. Preserve the failed trace/log and require independent focused PWA runs followed by the full Release. No provider, data, DNS or Production traffic change occurred.

- `local_production_admin_hydration_deadline` — Issue #453 final security Release passed Quick/build and the corrected PWA flow, but the first admin beforeEach exceeded 30 seconds waiting for the existing hydrated-shell marker. The page snapshot contained the admin shell; 102 E2E cases passed and one failed without retries. At inspection the host load was 25.91 on 15 logical CPUs while another task-owned semantic check and system indexers were active. This is contention evidence, not a proven sole cause. Preserve the trace/log, finish task-owned parallel gates, and verify independent admin runs plus the full gate sequentially without changing assertions or deadlines. No production mutation occurred.

- `local_production_auth_warmup_hmr_fetch` — Issue #453 combined local Release on 2026-09-09 completed normally: 1,878 Node passes plus eight intentional skips, 133 unit passes, zero dependency advisories, Production build and 103/103 retry-free E2E passes. The log nevertheless contained three Next development full reloads rather than the reviewed two: one before the first auth case and two between passing administrator cases. The retained Next trace identifies all three as a failed webpack HMR-manifest fetch, without a product-runtime-error flag. The first auth beforeEach warmed login through browser navigation, then immediately replaced that document with the actual scenario; this creates an unnecessary HMR client whose fetch can be interrupted. The correction warms only that route through the request client and checks its HTTP result; scenario navigation, rendered assertions, deadlines and retry policy remain unchanged. Preserve the original passing-but-abnormal log. Fresh focused auth and complete retry-free suite evidence are required before any remote trigger. No remote ref, Production data, DNS or deployment changed.

- `local_production_auth_warmup_source_contract` — The next Issue #453 Release stopped in Quick: the old public-readiness source test required the browser-navigation warmup that the HMR correction intentionally removed. Focused auth had already passed 12/12 without the startup reload; Node reported one stale source assertion and no product regression. Update the existing contract to require login preparation GET, its successful response before mock reset, and no disposable browser navigation or generic wait in beforeEach. Preserve this failed Release, and require the corrected contract and full Release before any remote action. No remote state changed.

- `local_production_storybook_stale_contracts` — Issue #453 first full Storybook run after the stable security dependency update exited 1: 149/156 files passed and 483/485 executed cases passed. Four suites could not import the absent getImageProps named export from the existing next/image mock; one notification Story imported the server push aggregation and reached node:crypto; two cases expected obsolete signup placeholder/raw error text. The application Release already passed 103/103, and source inspection locates these failures in stale Storybook fixtures/imports rather than production components. Add getImageProps to the existing unoptimized image mock, import push defaults from their shared types owner, align the exact current placeholder, and simulate native fetch TypeError while asserting the existing safe network-error message. Preserve all failures. Require fresh focused repetitions plus the full Storybook wrapper without retries or browser errors; no remote trigger occurred.

- `local_production_storybook_duplicate_mock_and_crop_readiness` — The second Storybook invocation ran the full suite because the wrapper does not forward CLI file filters: 151/156 files and 485/486 executed cases passed. Four imports still failed although the served image module exported getImageProps. Inspection found a second explicit vi.mock factory in vitest.setup.tsx returning only default; this is a duplicate mock contract, not proven cache corruption. Reuse the shared image mock including its named export. The manual member photo Story clicked Apply after the crop frame appeared, while the actual button remains disabled until croppedAreaPixels exists; await that existing enabled state before clicking. Preserve both failed logs and unchanged deadlines/retry policy. Use the wrapper exported args option for genuinely focused runs, then the full suite. No remote mutation occurred.

- `local_production_storybook_mock_self_import` — The first truly focused run passed the nine manual-add cases but stopped making progress in image consumers after the new mock factory dynamically imported its own aliased path. Source inspection confirmed that next/image resolves to that same module, creating mock self-resolution. The task-owned test process was explicitly terminated with its partial log retained; it is not a passing run. Use Vitest importOriginal to bypass the active factory while sharing the canonical mock. Require fresh independent affected-suite runs and the complete wrapper. No remote operation occurred.

- `local_production_storybook_gallery_accessible_name` — Once all image suites imported successfully, the focused run executed 40 cases: 39 passed and the partner-detail default Story expected an obsolete gallery button name. The rendered current TabletImageCarousel declares the indexed accessible name Image 1 enlarge in Korean. Align the fixture with that exact existing accessible name, preserving visibility and gallery placement assertions. Keep the failed run, then verify fresh affected runs and the full Storybook suite. No product component or remote state changed.

- `local_production_storybook_command_name` — After two independent 40/40 affected-suite passes, an operator invocation used the nonexistent package script test:storybook. npm exited before starting any test. Preserve that invocation log and use the verified package.json script test-storybook; this supplies no test evidence and changed no remote state.

- `production_storybook_tsconfck_deprecation_baseline` — The trusted dependency installation reports the registry deprecation unmaintained for tsconfck 3.1.6. npm explain identifies it solely under the development chain Storybook Next Vite 10.6.0 → vite-plugin-storybook-nextjs 10.6.0 → vite-tsconfig-paths 5.1.4. The registry metadata independently confirms that exact deprecation; the full dependency audit has zero advisories. This specific development-tool maintenance notice is a reviewed observation, not a failed installation or runtime warning. Keep it visible and reevaluate if the package, path, severity or audit result changes; do not force an incompatible transitive major override merely to hide the notice.

- `local_production_storybook_email_browser_resolution` — The full suite passed all 508 cases across 156 files but emitted 22 browser externalization warnings twice (Vite forwarding and stderr) while loading the email template preview. Its existing sanitize-html dependency imports PostCSS; PostCSS explicitly maps path, fs, url and source-map-js to false in its browser package manifest. Unbundled Vite proxies warn on accessing those intentionally absent server exports. Preserve this passing-but-abnormal run and prebundle sanitize-html through the Storybook dependency optimizer so the package browser mapping is resolved consistently. The sanitizer and its allowlist remain unchanged. Require an isolated email preview check and complete wrapper before delivery; no remote trigger occurred.

- `production_storybook_postcss_browser_stub_baseline` — Isolated email preview passed 4/4 with the same 22 externalized accesses duplicated through Vite forwarding/stderr; explicit prebundling did not alter the signature and was reverted. Inspection of the generated bundle and PostCSS 8.5.23 package browser map proves these modules are intentionally false, not missing application polyfills. PostCSS input/map-generator guard unavailable path/source-map functions with capability booleans; application email sanitization does not allow style attributes, and the production build plus all application E2E already passed. The source-matched full Storybook run passed 508/508 with only this exact Vite development proxy notice, zero failed tests, runtime exceptions, retries or teardown failures. Treat only these 22 known import-time accesses (44 displayed lines) to the manifest-listed false modules as a reviewed tooling observation. Keep every warning visible; new modules, runtime use, severity or count changes reopen investigation. No global warning filter or sanitizer behavior change was introduced.

- `local_production_linux_storybook_alt_attribute` — The isolated AMD64 image gate for preparation SHA 5011b521f88eeca64c3e7fd5aa37bafff4199e13 reported one jsx-a11y alt-text warning for the shared image mock: alt existed only inside a returned spread, so the static rule could not prove it. The Mac change gate had not emitted it; its absence did not establish Linux parity. Stop the exact task-owned gate container, preserve its termination/build evidence, and make the existing fallback alt prop explicit in JSX. No image from this attempt is deployable. Require clean Linux lint plus affected UI verification and a new-SHA image gate. The independently running original-data read-only export remains bound to its first source SHA and is not interrupted or retried.
